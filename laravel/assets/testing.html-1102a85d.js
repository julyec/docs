import{_ as o}from"./plugin-vue_export-helper-c27b6911.js";import{r as i,o as r,c as l,b as a,d as e,e as s,a as n}from"./app-8cdff07c.js";const c={},d=n(`<h1 id="testing-getting-started" tabindex="-1"><a class="header-anchor" href="#testing-getting-started" aria-hidden="true">#</a> Testing: Getting Started</h1><ul><li><a href="#introduction">Introduction</a></li><li><a href="#environment">Environment</a></li><li><a href="#creating-tests">Creating Tests</a></li><li><a href="#running-tests">Running Tests</a><ul><li><a href="#running-tests-in-parallel">Running Tests In Parallel</a></li><li><a href="#reporting-test-coverage">Reporting Test Coverage</a></li><li><a href="#profiling-tests">Profiling Tests</a></li></ul></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2><p>Laravel is built with testing in mind. In fact, support for testing with PHPUnit is included out of the box and a <code>phpunit.xml</code> file is already set up for your application. The framework also ships with convenient helper methods that allow you to expressively test your applications.</p><p>By default, your application&#39;s <code>tests</code> directory contains two directories: <code>Feature</code> and <code>Unit</code>. Unit tests are tests that focus on a very small, isolated portion of your code. In fact, most unit tests probably focus on a single method. Tests within your &quot;Unit&quot; test directory do not boot your Laravel application and therefore are unable to access your application&#39;s database or other framework services.</p><p>Feature tests may test a larger portion of your code, including how several objects interact with each other or even a full HTTP request to a JSON endpoint. <strong>Generally, most of your tests should be feature tests. These types of tests provide the most confidence that your system as a whole is functioning as intended.</strong></p><p>An <code>ExampleTest.php</code> file is provided in both the <code>Feature</code> and <code>Unit</code> test directories. After installing a new Laravel application, execute the <code>vendor/bin/phpunit</code> or <code>php artisan test</code> commands to run your tests.</p><p><a name="environment"></a></p><h2 id="environment" tabindex="-1"><a class="header-anchor" href="#environment" aria-hidden="true">#</a> Environment</h2><p>When running tests, Laravel will automatically set the <a href="./configuration#environment-configuration">configuration environment</a> to <code>testing</code> because of the environment variables defined in the <code>phpunit.xml</code> file. Laravel also automatically configures the session and cache to the <code>array</code> driver while testing, meaning no session or cache data will be persisted while testing.</p><p>You are free to define other testing environment configuration values as necessary. The <code>testing</code> environment variables may be configured in your application&#39;s <code>phpunit.xml</code> file, but make sure to clear your configuration cache using the <code>config:clear</code> Artisan command before running your tests!</p><p><a name="the-env-testing-environment-file"></a></p><h4 id="the-env-testing-environment-file" tabindex="-1"><a class="header-anchor" href="#the-env-testing-environment-file" aria-hidden="true">#</a> The <code>.env.testing</code> Environment File</h4><p>In addition, you may create a <code>.env.testing</code> file in the root of your project. This file will be used instead of the <code>.env</code> file when running PHPUnit tests or executing Artisan commands with the <code>--env=testing</code> option.</p><p><a name="the-creates-application-trait"></a></p><h4 id="the-createsapplication-trait" tabindex="-1"><a class="header-anchor" href="#the-createsapplication-trait" aria-hidden="true">#</a> The <code>CreatesApplication</code> Trait</h4><p>Laravel includes a <code>CreatesApplication</code> trait that is applied to your application&#39;s base <code>TestCase</code> class. This trait contains a <code>createApplication</code> method that bootstraps the Laravel application before running your tests. It&#39;s important that you leave this trait at its original location as some features, such as Laravel&#39;s parallel testing feature, depend on it.</p><p><a name="creating-tests"></a></p><h2 id="creating-tests" tabindex="-1"><a class="header-anchor" href="#creating-tests" aria-hidden="true">#</a> Creating Tests</h2><p>To create a new test case, use the <code>make:test</code> Artisan command. By default, tests will be placed in the <code>tests/Feature</code> directory:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan make:test UserTest
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you would like to create a test within the <code>tests/Unit</code> directory, you may use the <code>--unit</code> option when executing the <code>make:test</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan make:test UserTest <span class="token parameter variable">--unit</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,24),p={href:"https://pestphp.com",target:"_blank",rel:"noopener noreferrer"},u=a("code",null,"--pest",-1),h=a("code",null,"make:test",-1),m=n(`<div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan make:test UserTest <span class="token parameter variable">--pest</span>
php artisan make:test UserTest <span class="token parameter variable">--unit</span> <span class="token parameter variable">--pest</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>Note</strong><br> Test stubs may be customized using <a href="./artisan#stub-customization">stub publishing</a>.</p></blockquote>`,2),g={href:"https://phpunit.de",target:"_blank",rel:"noopener noreferrer"},v=a("code",null,"vendor/bin/phpunit",-1),b=a("code",null,"php artisan test",-1),f=n(`<pre><code>&lt;?php

namespace Tests\\Unit;

use PHPUnit\\Framework\\TestCase;

class ExampleTest extends TestCase
{
    /**
     * A basic test example.
     */
    public function test_basic_test(): void
    {
        $this-&gt;assertTrue(true);
    }
}
</code></pre><blockquote><p><strong>Warning</strong><br> If you define your own <code>setUp</code> / <code>tearDown</code> methods within a test class, be sure to call the respective <code>parent::setUp()</code> / <code>parent::tearDown()</code> methods on the parent class.</p></blockquote><p><a name="running-tests"></a></p><h2 id="running-tests" tabindex="-1"><a class="header-anchor" href="#running-tests" aria-hidden="true">#</a> Running Tests</h2><p>As mentioned previously, once you&#39;ve written tests, you may run them using <code>phpunit</code>:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>./vendor/bin/phpunit
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>In addition to the <code>phpunit</code> command, you may use the <code>test</code> Artisan command to run your tests. The Artisan test runner provides verbose test reports in order to ease development and debugging:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Any arguments that can be passed to the <code>phpunit</code> command may also be passed to the Artisan <code>test</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--testsuite</span><span class="token operator">=</span>Feature --stop-on-failure
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="running-tests-in-parallel"></a></p><h3 id="running-tests-in-parallel" tabindex="-1"><a class="header-anchor" href="#running-tests-in-parallel" aria-hidden="true">#</a> Running Tests In Parallel</h3><p>By default, Laravel and PHPUnit execute your tests sequentially within a single process. However, you may greatly reduce the amount of time it takes to run your tests by running tests simultaneously across multiple processes. To get started, you should install the <code>brianium/paratest</code> Composer package as a &quot;dev&quot; dependency. Then, include the <code>--parallel</code> option when executing the <code>test</code> Artisan command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">composer</span> require brianium/paratest <span class="token parameter variable">--dev</span>

php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--parallel</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>By default, Laravel will create as many processes as there are available CPU cores on your machine. However, you may adjust the number of processes using the <code>--processes</code> option:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--parallel</span> <span class="token parameter variable">--processes</span><span class="token operator">=</span><span class="token number">4</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p><strong>Warning</strong><br> When running tests in parallel, some PHPUnit options (such as <code>--do-not-cache-result</code>) may not be available.</p></blockquote><p><a name="parallel-testing-and-databases"></a></p><h4 id="parallel-testing-databases" tabindex="-1"><a class="header-anchor" href="#parallel-testing-databases" aria-hidden="true">#</a> Parallel Testing &amp; Databases</h4><p>As long as you have configured a primary database connection, Laravel automatically handles creating and migrating a test database for each parallel process that is running your tests. The test databases will be suffixed with a process token which is unique per process. For example, if you have two parallel test processes, Laravel will create and use <code>your_db_test_1</code> and <code>your_db_test_2</code> test databases.</p><p>By default, test databases persist between calls to the <code>test</code> Artisan command so that they can be used again by subsequent <code>test</code> invocations. However, you may re-create them using the <code>--recreate-databases</code> option:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--parallel</span> --recreate-databases
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="parallel-testing-hooks"></a></p><h4 id="parallel-testing-hooks" tabindex="-1"><a class="header-anchor" href="#parallel-testing-hooks" aria-hidden="true">#</a> Parallel Testing Hooks</h4><p>Occasionally, you may need to prepare certain resources used by your application&#39;s tests so they may be safely used by multiple test processes.</p><p>Using the <code>ParallelTesting</code> facade, you may specify code to be executed on the <code>setUp</code> and <code>tearDown</code> of a process or test case. The given closures receive the <code>$token</code> and <code>$testCase</code> variables that contain the process token and the current test case, respectively:</p><pre><code>&lt;?php

namespace App\\Providers;

use Illuminate\\Support\\Facades\\Artisan;
use Illuminate\\Support\\Facades\\ParallelTesting;
use Illuminate\\Support\\ServiceProvider;
use PHPUnit\\Framework\\TestCase;

class AppServiceProvider extends ServiceProvider
{
    /**
     * Bootstrap any application services.
     */
    public function boot(): void
    {
        ParallelTesting::setUpProcess(function (int $token) {
            // ...
        });

        ParallelTesting::setUpTestCase(function (int $token, TestCase $testCase) {
            // ...
        });

        // Executed when a test database is created...
        ParallelTesting::setUpTestDatabase(function (string $database, int $token) {
            Artisan::call(&#39;db:seed&#39;);
        });

        ParallelTesting::tearDownTestCase(function (int $token, TestCase $testCase) {
            // ...
        });

        ParallelTesting::tearDownProcess(function (int $token) {
            // ...
        });
    }
}
</code></pre><p><a name="accessing-the-parallel-testing-token"></a></p><h4 id="accessing-the-parallel-testing-token" tabindex="-1"><a class="header-anchor" href="#accessing-the-parallel-testing-token" aria-hidden="true">#</a> Accessing The Parallel Testing Token</h4><p>If you would like to access the current parallel process &quot;token&quot; from any other location in your application&#39;s test code, you may use the <code>token</code> method. This token is a unique, string identifier for an individual test process and may be used to segment resources across parallel test processes. For example, Laravel automatically appends this token to the end of the test databases created by each parallel testing process:</p><pre><code>$token = ParallelTesting::token();
</code></pre><p><a name="reporting-test-coverage"></a></p><h3 id="reporting-test-coverage" tabindex="-1"><a class="header-anchor" href="#reporting-test-coverage" aria-hidden="true">#</a> Reporting Test Coverage</h3>`,33),y=a("strong",null,"Warning",-1),k=a("br",null,null,-1),T={href:"https://xdebug.org",target:"_blank",rel:"noopener noreferrer"},w={href:"https://pecl.php.net/package/pcov",target:"_blank",rel:"noopener noreferrer"},x=n(`<p>When running your application tests, you may want to determine whether your test cases are actually covering the application code and how much application code is used when running your tests. To accomplish this, you may provide the <code>--coverage</code> option when invoking the <code>test</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--coverage</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="enforcing-a-minimum-coverage-threshold"></a></p><h4 id="enforcing-a-minimum-coverage-threshold" tabindex="-1"><a class="header-anchor" href="#enforcing-a-minimum-coverage-threshold" aria-hidden="true">#</a> Enforcing A Minimum Coverage Threshold</h4><p>You may use the <code>--min</code> option to define a minimum test coverage threshold for your application. The test suite will fail if this threshold is not met:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--coverage</span> <span class="token parameter variable">--min</span><span class="token operator">=</span><span class="token number">80.3</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="profiling-tests"></a></p><h3 id="profiling-tests" tabindex="-1"><a class="header-anchor" href="#profiling-tests" aria-hidden="true">#</a> Profiling Tests</h3><p>The Artisan test runner also includes a convenient mechanism for listing your application&#39;s slowest tests. Invoke the <code>test</code> command with the <code>--profile</code> option to be presented with a list of your ten slowest tests, allowing you to easily investigate which tests can be improved to speed up your test suite:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan <span class="token builtin class-name">test</span> <span class="token parameter variable">--profile</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div>`,10);function _(P,U){const t=i("ExternalLinkIcon");return r(),l("div",null,[d,a("p",null,[e("If you would like to create a "),a("a",p,[e("Pest PHP"),s(t)]),e(" test, you may provide the "),u,e(" option to the "),h,e(" command:")]),m,a("p",null,[e("Once the test has been generated, you may define test methods as you normally would using "),a("a",g,[e("PHPUnit"),s(t)]),e(". To run your tests, execute the "),v,e(" or "),b,e(" command from your terminal:")]),f,a("blockquote",null,[a("p",null,[y,k,e(" This feature requires "),a("a",T,[e("Xdebug"),s(t)]),e(" or "),a("a",w,[e("PCOV"),s(t)]),e(".")])]),x])}const q=o(c,[["render",_],["__file","testing.html.vue"]]);export{q as default};
