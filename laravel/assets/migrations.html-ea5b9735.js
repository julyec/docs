import{_ as t}from"./plugin-vue_export-helper-c27b6911.js";import{r as n,o as c,c as i,b as e,d as o,e as r,a as d}from"./app-06635a3b.js";const l={},s=d(`<h1 id="数据库-迁移" tabindex="-1"><a class="header-anchor" href="#数据库-迁移" aria-hidden="true">#</a> 数据库: 迁移</h1><ul><li><a href="#introduction">介绍</a></li><li><a href="#generating-migrations">生成迁移</a><ul><li><a href="#squashing-migrations">整合迁移</a></li></ul></li><li><a href="#migration-structure">迁移结构</a></li><li><a href="#running-migrations">执行迁移</a><ul><li><a href="#rolling-back-migrations">回滚迁移</a></li></ul></li><li><a href="#tables">数据表</a><ul><li><a href="#creating-tables">创建数据表</a></li><li><a href="#updating-tables">更新数据表</a></li><li><a href="#renaming-and-dropping-tables">重命名 / 删除表</a></li></ul></li><li><a href="#columns">字段</a><ul><li><a href="#creating-columns">创建字段</a></li><li><a href="#available-column-types">可用的字段类型</a></li><li><a href="#column-modifiers">字段修饰符</a></li><li><a href="#modifying-columns">修改字段</a></li><li><a href="#renaming-columns">重命名字段</a></li><li><a href="#dropping-columns">删除字段</a></li></ul></li><li><a href="#indexes">索引</a><ul><li><a href="#creating-indexes">创建索引</a></li><li><a href="#renaming-indexes">重命名索引</a></li><li><a href="#dropping-indexes">删除索引</a></li><li><a href="#foreign-key-constraints">外键约束</a></li></ul></li><li><a href="#events">事件</a></li></ul><p><a name="introduction"></a></p><h2 id="介绍" tabindex="-1"><a class="header-anchor" href="#介绍" aria-hidden="true">#</a> 介绍</h2><p>迁移就像数据库的版本控制，允许你的团队定义和共享应用程序的数据库架构定义。 如果你曾经不得不告诉团队成员在从代码控制中拉取更新后手动添加字段到他们的本地数据库，那么你就遇到了数据库迁移解决的问题。</p><p>Laravel <code>Schema</code> <a href="./facades">facade</a> 为所有 Laravel 支持的数据库系统的创建和操作表提供了不依赖于数据库的支持。通常情况下，迁移会使用 facade 来创建和修改数据表和字段。</p><p><a name="generating-migrations"></a></p><h2 id="生成迁移" tabindex="-1"><a class="header-anchor" href="#生成迁移" aria-hidden="true">#</a> 生成迁移</h2><p>你可以使用 <code>make:migration</code> <a href="./artisan">Artisan 命令</a> 来生成数据库迁移。新的迁移文件将放在你的 <code>database/migrations</code> 目录下。每个迁移文件名都包含一个时间戳来使 Laravel 确定迁移的顺序：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan make:migration create_flights_table
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Laravel 将使用迁移文件的名称来猜测表名以及迁移是否会创建一个新表。如果 Laravel 能够从迁移文件的名称中确定表的名称，它将在生成的迁移文件中预填入指定的表，或者，你也可以直接在迁移文件中手动指定表名。</p><p>如果要为生成的迁移指定自定义路径，你可以在执行 <code>make:migration</code> 命令时使用 <code>--path</code> 选项。给定的路径应该相对于应用程序的基本路径。</p><blockquote><p><strong>技巧</strong><br> 可以使用 <a href="./artisan#stub-customization">stub publishing</a> 自定义发布。</p></blockquote><p><a name="squashing-migrations"></a></p><h3 id="整合迁移" tabindex="-1"><a class="header-anchor" href="#整合迁移" aria-hidden="true">#</a> 整合迁移</h3><p>在构建应用程序时，可能会随着时间的推移积累越来越多的迁移。这可能会导致你的 <code>database/migrations</code> 目录因为数百次迁移而变得臃肿。你如果愿意的话，可以将迁移「压缩」到单个 SQL 文件中。如果你想这样做，请先执行<code>schema:dump</code> 命令：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan schema:dump

<span class="token comment"># 转储当前数据库架构并删除所有现有迁移...</span>
php artisan schema:dump <span class="token parameter variable">--prune</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>执行此命令时，Laravel 将向应用程序的 <code>database/schema</code> 目录写入一个「schema」文件。现在，当你尝试迁移数据库而没有执行其他迁移时，Laravel 将首先执行模式文件的 SQL 语句。在执行数据库结构文件的语句之后，Laravel 将执行不属于数据库结构的剩余的所有迁移。</p><p>如果你的应用程序的测试使用的数据库连接与你在本地开发过程中通常使用的不同，你应该确保你已经使用该数据库连接转储了一个 schema 文件，以便你的测试能够建立你的数据库。你可能希望在切换（dump）你在本地开发过程中通常使用的数据库连接之后再做这件事。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan schema:dump
php artisan schema:dump <span class="token parameter variable">--database</span><span class="token operator">=</span>testing <span class="token parameter variable">--prune</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>你应该将数据库模式文件提交给源代码管理，以便团队中的其他新开发人员可以快速创建应用程序的初始数据库结构。</p><blockquote><p><strong>注意</strong><br> 整合迁移仅适用于 MySQL、PostgreSQL 和 SQLite 数据库，并使用数据库命令行的客户端。另外，数据库结构不能还原到内存中的 SQLite 数据库。</p></blockquote><p><a name="migration-structure"></a></p><h2 id="迁移结构" tabindex="-1"><a class="header-anchor" href="#迁移结构" aria-hidden="true">#</a> 迁移结构</h2><p>迁移类包含两个方法：<code>up</code> 和 <code>down</code> 。<code>up</code> 方法用于向数据库中添加新表、列或索引，而 down 方法用于撤销 <code>up</code> 方法执行的操作。.</p><p>在这两种方法中，可以使用 Laravel 模式构建器来富有表现力地创建和修改表。要了解 <code>Schema</code> 构建器上可用的所有方法，<a href="#creating-tables">查看其文档</a>。例如，以下迁移会创建一个 <code>flights</code> 表：</p><pre><code>&lt;?php

use Illuminate\\Database\\Migrations\\Migration;
use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

return new class extends Migration
{
    /**
     * 执行迁移
     */
    public function up(): void
    {
        Schema::create(&#39;flights&#39;, function (Blueprint $table) {
            $table-&gt;id();
            $table-&gt;string(&#39;name&#39;);
            $table-&gt;string(&#39;airline&#39;);
            $table-&gt;timestamps();
        });
    }

    /**
     * 回滚迁移
     */
    public function down(): void
    {
        Schema::drop(&#39;flights&#39;);
    }
};
</code></pre><p><a name="setting-the-migration-connection"></a></p><h4 id="设置迁移连接" tabindex="-1"><a class="header-anchor" href="#设置迁移连接" aria-hidden="true">#</a> 设置迁移连接</h4><p>如果你的迁移将与应用程序默认数据库连接以外的数据库连接进行交互，你应该设置迁移的 <code>$connection</code> 属性：</p><pre><code>/**
 * The database connection that should be used by the migration.
 *
 * @var string
 */
protected $connection = &#39;pgsql&#39;;

/**
 * 执行迁移
 */
public function up(): void
{
    // ...
}
</code></pre><p><a name="running-migrations"></a></p><h2 id="执行迁移" tabindex="-1"><a class="header-anchor" href="#执行迁移" aria-hidden="true">#</a> 执行迁移</h2><p>执行 Artisan 命令 <code>migrate</code>，来运行所有未执行过的迁移：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你想查看目前已经执行了哪些迁移，可以使用 <code>migrate:status</code> Artisan 命令：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:status
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>如果你希望在不实际运行迁移的情况下看到将被执行的SQL语句，你可以在 <code>migrate</code> 命令中提供 <code>--pretend</code> 选项。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--pretend</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h4 id="在隔离的环境中执行迁移" tabindex="-1"><a class="header-anchor" href="#在隔离的环境中执行迁移" aria-hidden="true">#</a> 在隔离的环境中执行迁移</h4><p>如果你在多个服务器上部署你的应用程序，并在部署过程中运行迁移，你可能不希望两个服务器同时尝试迁移数据库。为了避免这种情况，你可以在调用 <code>migrate</code> 命令时使用 <code>isolated</code> 选项。</p><p>当提供 <code>isolated</code> 选项时, Laravel 将使用你的应用程序缓存驱动获得一个原子锁，然后再尝试运行你的迁移。所有其他试图运行 <code>migrate</code> 命令的尝试在锁被持有时都不会执行; 然而, 命令仍然会以成功的退出状态码退出:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--isolated</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p><strong>注意</strong><br> 要使用这个功能，你的应用程序必须使用 <code>memcached</code> / <code>redis</code> / <code>dynamodb</code> / <code>database</code> / <code>file</code> 或 <code>array</code> 缓存驱动作为你应用程序的默认缓存驱动。此外，所有的服务器必须与同一个中央缓存服务器进行通信。</p></blockquote><p><a name="forcing-migrations-to-run-in-production"></a></p><h4 id="在生产环境中执行强制迁移" tabindex="-1"><a class="header-anchor" href="#在生产环境中执行强制迁移" aria-hidden="true">#</a> 在生产环境中执行强制迁移</h4><p>有些迁移操作是破坏性的，这意味着它们可能会导致数据丢失。为了防止你对生产数据库运行这些命令，在执行这些命令之前，系统将提示你进行确认。如果要在运行强制命令的时候去掉提示，需要加上 <code>--force</code> 标志：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--force</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="rolling-back-migrations"></a></p><h3 id="回滚迁移" tabindex="-1"><a class="header-anchor" href="#回滚迁移" aria-hidden="true">#</a> 回滚迁移</h3><p>如果要回滚最后一次迁移操作，可以使用 Artisan 命令 <code>rollback</code>。该命令会回滚最后「一批」的迁移，这可能包含多个迁移文件：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>通过向 <code>rollback</code> 命令加上 <code>step</code> 参数，可以回滚指定数量的迁移。例如，以下命令将回滚最后五个迁移：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback <span class="token parameter variable">--step</span><span class="token operator">=</span><span class="token number">5</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>你可以通过向 <code>rollback</code> 命令提供 <code>batch</code> 选项来回滚特定的批次迁移，其中 <code>batch</code> 选项对应于应用程序中 <code>migrations</code> 数据库表中的一个批次值。例如，下面的命令将回滚第三批中的所有迁移。</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback <span class="token parameter variable">--batch</span><span class="token operator">=</span><span class="token number">3</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>命令 <code>migrate:reset</code> 会回滚应用已运行过的所有迁移：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:reset
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="roll-back-migrate-using-a-single-command"></a></p><h4 id="使用单个命令同时进行回滚和迁移操作" tabindex="-1"><a class="header-anchor" href="#使用单个命令同时进行回滚和迁移操作" aria-hidden="true">#</a> 使用单个命令同时进行回滚和迁移操作</h4><p>命令 <code>migrate:refresh</code> 首先会回滚已运行过的所有迁移，随后会执行 <code>migrate</code>。这一命令可以高效地重建你的整个数据库：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:refresh

<span class="token comment"># 重置数据库，并运行所有的 seeds...</span>
php artisan migrate:refresh <span class="token parameter variable">--seed</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>通过在命令 <code>refresh</code> 中使用 <code>step</code> 参数，你可以回滚并重新执行指定数量的迁移操作。例如，下列命令会回滚并重新执行最后五个迁移操作：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:refresh <span class="token parameter variable">--step</span><span class="token operator">=</span><span class="token number">5</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="drop-all-tables-migrate"></a></p><h4 id="删除所有表然后执行迁移" tabindex="-1"><a class="header-anchor" href="#删除所有表然后执行迁移" aria-hidden="true">#</a> 删除所有表然后执行迁移</h4><p>命令 <code>migrate:fresh</code> 会删去数据库中的所有表，随后执行命令 <code>migrate</code>：</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:fresh

php artisan migrate:fresh <span class="token parameter variable">--seed</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>注意</strong><br> 该命令 <code>migrate:fresh</code> 在删去所有数据表的过程中，会无视它们的前缀。如果数据库涉及到其它应用，使用该命令须十分小心。</p></blockquote><p><a name="tables"></a></p><h2 id="数据表" tabindex="-1"><a class="header-anchor" href="#数据表" aria-hidden="true">#</a> 数据表</h2><p><a name="creating-tables"></a></p><h3 id="创建数据表" tabindex="-1"><a class="header-anchor" href="#创建数据表" aria-hidden="true">#</a> 创建数据表</h3><p>接下来我们将使用 Schema 的 <code>create</code> 方法创建一个新的数据表。<code>create</code> 接受两个参数：第一个参数是表名，而第二个参数是一个闭包，该闭包接受一个用来定义新数据表的 <code>Blueprint</code> 对象：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;id();
    $table-&gt;string(&#39;name&#39;);
    $table-&gt;string(&#39;email&#39;);
    $table-&gt;timestamps();
});
</code></pre><p>创建表时，可以使用数据库结构构建器的 <a href="#creating-columns">列方法</a> 来定义表的列。</p><p><a name="checking-for-table-column-existence"></a></p><h4 id="检查表-列是否存在" tabindex="-1"><a class="header-anchor" href="#检查表-列是否存在" aria-hidden="true">#</a> 检查表 / 列是否存在</h4><p>你可以使用 <code>hasTable</code> 和 <code>hasColumn</code> 方法检查表或列是否存在：</p><pre><code>if (Schema::hasTable(&#39;users&#39;)) {
    // 「users」表存在...
}

if (Schema::hasColumn(&#39;users&#39;, &#39;email&#39;)) {
    // 「users」表存在，并且有「email」列...
}
</code></pre><p><a name="database-connection-table-options"></a></p><h4 id="数据库连接和表选项" tabindex="-1"><a class="header-anchor" href="#数据库连接和表选项" aria-hidden="true">#</a> 数据库连接和表选项</h4><p>如果要对不是应用程序默认的数据库连接执行数据库结构的操作，请使用 <code>connection</code> 方法：</p><pre><code>Schema::connection(&#39;sqlite&#39;)-&gt;create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;id();
});
</code></pre><p>此外，还可以使用其他一些属性和方法来定义表创建的其他地方。使用 MySQL 时，可以使用 <code>engine</code> 属性指定表的存储引擎：</p><pre><code>Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;engine = &#39;InnoDB&#39;;

    // ...
});
</code></pre><p><code>charset</code> 和 <code>collation</code> 属性可用于在使用 MySQL 时为创建的表指定字符集和排序规则：</p><pre><code>Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;charset = &#39;utf8mb4&#39;;
    $table-&gt;collation = &#39;utf8mb4_unicode_ci&#39;;

    // ...
});
</code></pre><p><code>temporary</code> 方法可用于将表标识为「临时」状态。临时表仅对当前连接的数据库会话可见，当连接关闭时会自动删除：</p><pre><code>Schema::create(&#39;calculations&#39;, function (Blueprint $table) {
    $table-&gt;temporary();

    // ...
});
</code></pre><p>如果你想给数据库表添加「注释」，你可以在表实例上调用<code>comment</code>方法。目前只有 MySQL 和 Postgres 支持表注释：</p><pre><code>Schema::create(&#39;calculations&#39;, function (Blueprint $table) {
    $table-&gt;comment(&#39;Business calculations&#39;);

    // ...
});
</code></pre><p><a name="更新数据表"></a></p><h3 id="更新数据表" tabindex="-1"><a class="header-anchor" href="#更新数据表" aria-hidden="true">#</a> 更新数据表</h3><p><code>Schema</code> 门面的 <code>table</code> 方法可用于更新现有表。与 <code>create</code> 方法一样，<code>table</code> 方法接受两个参数：表的名称和接收可用于向表添加列或索引的 <code>Blueprint</code> 实例的闭包：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;);
});
</code></pre><p><a name="renaming-and-dropping-tables"></a></p><h3 id="重命名-删除表" tabindex="-1"><a class="header-anchor" href="#重命名-删除表" aria-hidden="true">#</a> 重命名 / 删除表</h3><p>要重命名已存在的数据表，使用 <code>rename</code> 方法：</p><pre><code>use Illuminate\\Support\\Facades\\Schema;

Schema::rename($from, $to);
</code></pre><p>要删除已存在的表，你可以使用 <code>drop</code> 或 <code>dropIfExists</code> 方法：</p><pre><code>Schema::drop(&#39;users&#39;);

Schema::dropIfExists(&#39;users&#39;);
</code></pre><p><a name="renaming-tables-with-foreign-keys"></a></p><h4 id="使用外键重命名表" tabindex="-1"><a class="header-anchor" href="#使用外键重命名表" aria-hidden="true">#</a> 使用外键重命名表</h4><p>在重命名表之前，应该确认表的所有外键约束在迁移文件中有一个显式的名称，而不是让 Laravel 去指定。否则，外键约束名称将引用旧表名。</p><p><a name="columns"></a></p><h2 id="字段" tabindex="-1"><a class="header-anchor" href="#字段" aria-hidden="true">#</a> 字段</h2><p><a name="creating-columns"></a></p><h3 id="创建字段" tabindex="-1"><a class="header-anchor" href="#创建字段" aria-hidden="true">#</a> 创建字段</h3><p>门面 <code>Schema</code> 的 <code>table</code> 方法可用于更新表。与 <code>create</code> 方法一样， <code>table</code> 方法接受两个参数：表名和一个闭包，该闭包接收一个 <code>illumb\\Database\\Schema\\Blueprint</code> 实例，可以使用该实例向表中添加列：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;);
});
</code></pre><p><a name="可用的字段类型"></a></p><h3 id="可用的字段类型" tabindex="-1"><a class="header-anchor" href="#可用的字段类型" aria-hidden="true">#</a> 可用的字段类型</h3><p>Schema 构建器 <code>Illuminate\\Database\\Schema\\Blueprint</code> 提供了多种方法，用来创建表中对应类型的列。下面列出了所有可用的方法：</p>`,114),h=e("div",{class:"collection-method-list",markdown:"1"},[e("p",null,[e("a",{href:"#column-method-bigIncrements"},"bigIncrements"),e("a",{href:"#column-method-bigInteger"},"bigInteger"),e("a",{href:"#column-method-binary"},"binary"),e("a",{href:"#column-method-boolean"},"boolean"),e("a",{href:"#column-method-char"},"char"),e("a",{href:"#column-method-dateTimeTz"},"dateTimeTz"),e("a",{href:"#column-method-dateTime"},"dateTime"),e("a",{href:"#column-method-date"},"date"),e("a",{href:"#column-method-decimal"},"decimal"),e("a",{href:"#column-method-double"},"double"),e("a",{href:"#column-method-enum"},"enum"),e("a",{href:"#column-method-float"},"float"),e("a",{href:"#column-method-foreignId"},"foreignId"),e("a",{href:"#column-method-foreignIdFor"},"foreignIdFor"),e("a",{href:"#column-method-foreignUlid"},"foreignUlid"),e("a",{href:"#column-method-foreignUuid"},"foreignUuid"),e("a",{href:"#column-method-geometryCollection"},"geometryCollection"),e("a",{href:"#column-method-geometry"},"geometry"),e("a",{href:"#column-method-id"},"id"),e("a",{href:"#column-method-increments"},"increments"),e("a",{href:"#column-method-integer"},"integer"),e("a",{href:"#column-method-ipAddress"},"ipAddress"),e("a",{href:"#column-method-json"},"json"),e("a",{href:"#column-method-jsonb"},"jsonb"),e("a",{href:"#column-method-lineString"},"lineString"),e("a",{href:"#column-method-longText"},"longText"),e("a",{href:"#column-method-macAddress"},"macAddress"),e("a",{href:"#column-method-mediumIncrements"},"mediumIncrements"),e("a",{href:"#column-method-mediumInteger"},"mediumInteger"),e("a",{href:"#column-method-mediumText"},"mediumText"),e("a",{href:"#column-method-morphs"},"morphs"),e("a",{href:"#column-method-multiLineString"},"multiLineString"),e("a",{href:"#column-method-multiPoint"},"multiPoint"),e("a",{href:"#column-method-multiPolygon"},"multiPolygon"),e("a",{href:"#column-method-nullableMorphs"},"nullableMorphs"),e("a",{href:"#column-method-nullableTimestamps"},"nullableTimestamps"),e("a",{href:"#column-method-nullableUlidMorphs"},"nullableUlidMorphs"),e("a",{href:"#column-method-nullableUuidMorphs"},"nullableUuidMorphs"),e("a",{href:"#column-method-point"},"point"),e("a",{href:"#column-method-polygon"},"polygon"),e("a",{href:"#column-method-rememberToken"},"rememberToken"),e("a",{href:"#column-method-set"},"set"),e("a",{href:"#column-method-smallIncrements"},"smallIncrements"),e("a",{href:"#column-method-smallInteger"},"smallInteger"),e("a",{href:"#column-method-softDeletesTz"},"softDeletesTz"),e("a",{href:"#column-method-softDeletes"},"softDeletes"),e("a",{href:"#column-method-string"},"string"),e("a",{href:"#column-method-text"},"text"),e("a",{href:"#column-method-timeTz"},"timeTz"),e("a",{href:"#column-method-time"},"time"),e("a",{href:"#column-method-timestampTz"},"timestampTz"),e("a",{href:"#column-method-timestamp"},"timestamp"),e("a",{href:"#column-method-timestampsTz"},"timestampsTz"),e("a",{href:"#column-method-timestamps"},"timestamps"),e("a",{href:"#column-method-tinyIncrements"},"tinyIncrements"),e("a",{href:"#column-method-tinyInteger"},"tinyInteger"),e("a",{href:"#column-method-tinyText"},"tinyText"),e("a",{href:"#column-method-unsignedBigInteger"},"unsignedBigInteger"),e("a",{href:"#column-method-unsignedDecimal"},"unsignedDecimal"),e("a",{href:"#column-method-unsignedInteger"},"unsignedInteger"),e("a",{href:"#column-method-unsignedMediumInteger"},"unsignedMediumInteger"),e("a",{href:"#column-method-unsignedSmallInteger"},"unsignedSmallInteger"),e("a",{href:"#column-method-unsignedTinyInteger"},"unsignedTinyInteger"),e("a",{href:"#column-method-ulidMorphs"},"ulidMorphs"),e("a",{href:"#column-method-uuidMorphs"},"uuidMorphs"),e("a",{href:"#column-method-ulid"},"ulid"),e("a",{href:"#column-method-uuid"},"uuid"),e("a",{href:"#column-method-year"},"year")])],-1),m=d(`<p><a name="column-method-bigIncrements"></a></p><h4 id="bigincrements-collection-method-first-collection-method" tabindex="-1"><a class="header-anchor" href="#bigincrements-collection-method-first-collection-method" aria-hidden="true">#</a> <code>bigIncrements()</code> {.collection-method .first-collection-method}</h4><p><code>bigIncrements</code> 方法用于在数据表中创建一个自增的 <code>UNSIGNED BIGINT</code> 类型（主键）的列：</p><pre><code>$table-&gt;bigIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-bigInteger"></a></p><h4 id="biginteger-collection-method" tabindex="-1"><a class="header-anchor" href="#biginteger-collection-method" aria-hidden="true">#</a> <code>bigInteger()</code> {.collection-method}</h4><p><code>bigInteger</code> 方法用于在数据表中创建一个 <code>BIGINT</code> 类型的列：</p><pre><code>$table-&gt;bigInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-binary"></a></p><h4 id="binary-collection-method" tabindex="-1"><a class="header-anchor" href="#binary-collection-method" aria-hidden="true">#</a> <code>binary()</code> {.collection-method}</h4><p><code>binary</code> 方法用于在数据表中创建一个 <code>BLOB</code> 类型的列：</p><pre><code>$table-&gt;binary(&#39;photo&#39;);
</code></pre><p><a name="column-method-boolean"></a></p><h4 id="boolean-collection-method" tabindex="-1"><a class="header-anchor" href="#boolean-collection-method" aria-hidden="true">#</a> <code>boolean()</code> {.collection-method}</h4><p><code>boolean</code> 方法用于在数据表中创建一个 <code>BOOLEAN</code> 类型的列：</p><pre><code>$table-&gt;boolean(&#39;confirmed&#39;);
</code></pre><p><a name="column-method-char"></a></p><h4 id="char-collection-method" tabindex="-1"><a class="header-anchor" href="#char-collection-method" aria-hidden="true">#</a> <code>char()</code> {.collection-method}</h4><p><code>char</code> 方法用于在数据表中创建一个 <code>CHAR</code> 类型的列，长度由参数指定：</p><pre><code>$table-&gt;char(&#39;name&#39;, 100);
</code></pre><p><a name="column-method-dateTimeTz"></a></p><h4 id="datetimetz-collection-method" tabindex="-1"><a class="header-anchor" href="#datetimetz-collection-method" aria-hidden="true">#</a> <code>dateTimeTz()</code> {.collection-method}</h4><p><code>dateTimeTz</code> 方法用于在数据表中创建一个 <code>DATETIME</code> 类型（附有 timezone）的列，可选参数为精度的总位数：</p><pre><code>$table-&gt;dateTimeTz(&#39;created_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-dateTime"></a></p><h4 id="datetime-collection-method" tabindex="-1"><a class="header-anchor" href="#datetime-collection-method" aria-hidden="true">#</a> <code>dateTime()</code> {.collection-method}</h4><p><code>dateTime</code> 方法用于在数据表中创建一个 <code>DATETIME</code> 类型的列，可选参数为精度的总位数：</p><pre><code>$table-&gt;dateTime(&#39;created_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-date"></a></p><h4 id="date-collection-method" tabindex="-1"><a class="header-anchor" href="#date-collection-method" aria-hidden="true">#</a> <code>date()</code> {.collection-method}</h4><p><code>date</code> 方法用于在数据表中创建一个 <code>DATE</code> 类型的列：</p><pre><code>$table-&gt;date(&#39;created_at&#39;);
</code></pre><p><a name="column-method-decimal"></a></p><h4 id="decimal-collection-method" tabindex="-1"><a class="header-anchor" href="#decimal-collection-method" aria-hidden="true">#</a> <code>decimal()</code> {.collection-method}</h4><p><code>decimal</code> 方法用于在数据表中创建一个 <code>DECIMAL</code> 类型的列，可选参数分别为有效字数总位数、小数部分总位数：</p><pre><code>$table-&gt;decimal(&#39;amount&#39;, $precision = 8, $scale = 2);
</code></pre><p><a name="column-method-double"></a></p><h4 id="double-collection-method" tabindex="-1"><a class="header-anchor" href="#double-collection-method" aria-hidden="true">#</a> <code>double()</code> {.collection-method}</h4><p><code>double</code> 方法用于在数据表中创建一个 <code>DOUBLE</code> 类型的列，可选参数分别为有效字数总位数、小数部分总位数：</p><pre><code>$table-&gt;double(&#39;amount&#39;, 8, 2);
</code></pre><p><a name="column-method-enum"></a></p><h4 id="enum-collection-method" tabindex="-1"><a class="header-anchor" href="#enum-collection-method" aria-hidden="true">#</a> <code>enum()</code> {.collection-method}</h4><p><code>enum</code> 方法用于在数据表中创建一个 <code>ENUM</code> 类型的列，合法的值列表由参数指定：</p><pre><code>$table-&gt;enum(&#39;difficulty&#39;, [&#39;easy&#39;, &#39;hard&#39;]);
</code></pre><p><a name="column-method-float"></a></p><h4 id="float-collection-method" tabindex="-1"><a class="header-anchor" href="#float-collection-method" aria-hidden="true">#</a> <code>float()</code> {.collection-method}</h4><p><code>float</code> 方法用于在数据表中创建一个 <code>FLOAT</code> 类型的列，可选参数分别为有效字数总位数、小数部分总位数：</p><pre><code>$table-&gt;float(&#39;amount&#39;, 8, 2);
</code></pre><p><a name="column-method-foreignId"></a></p><h4 id="foreignid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignid-collection-method" aria-hidden="true">#</a> <code>foreignId()</code> {.collection-method}</h4><p><code>foreignId</code> 方法是 <code>unsignedBigInteger</code> 的别名：</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;);
</code></pre><p><a name="column-method-foreignIdFor"></a></p><h4 id="foreignidfor-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignidfor-collection-method" aria-hidden="true">#</a> <code>foreignIdFor()</code> {.collection-method}</h4><p><code>foreignIdFor</code> 方法为给定模型类添加了 <code>{column}_id UNSIGNED BIGINT</code> 等效列：</p><pre><code>$table-&gt;foreignIdFor(User::class);
</code></pre><p><a name="column-method-foreignUlid"></a></p><h4 id="foreignulid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignulid-collection-method" aria-hidden="true">#</a> <code>foreignUlid()</code> {.collection-method}</h4><p><code>foreignUlid</code> 方法创建一个 <code>ULID</code> 等效列：</p><pre><code>$table-&gt;foreignUlid(&#39;user_id&#39;);
</code></pre><p><a name="column-method-foreignUuid"></a></p><h4 id="foreignuuid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignuuid-collection-method" aria-hidden="true">#</a> <code>foreignUuid()</code> {.collection-method}</h4><p><code>foreignUuid</code> 方法创建一个 <code>UUID</code> 等效列：</p><div class="language-text line-numbers-mode" data-ext="text"><pre class="language-text"><code>$table-&gt;foreignUuid(&#39;user_id&#39;);
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="column-method-geometryCollection"></a></p><h4 id="geometrycollection-collection-method" tabindex="-1"><a class="header-anchor" href="#geometrycollection-collection-method" aria-hidden="true">#</a> <code>geometryCollection()</code> {.collection-method}</h4><p><code>geometryCollection</code> 方法相当于 <code>GEOMETRYCOLLECTION</code> :</p><pre><code>$table-&gt;geometryCollection(&#39;positions&#39;);
</code></pre><p><a name="column-method-geometry"></a></p><h4 id="geometry-collection-method" tabindex="-1"><a class="header-anchor" href="#geometry-collection-method" aria-hidden="true">#</a> <code>geometry()</code> {.collection-method}</h4><p><code>geometry</code> 方法相当于 <code>GEOMETRY</code> :</p><pre><code>$table-&gt;geometry(&#39;positions&#39;);
</code></pre><p><a name="column-method-id"></a></p><h4 id="id-collection-method" tabindex="-1"><a class="header-anchor" href="#id-collection-method" aria-hidden="true">#</a> <code>id()</code> {.collection-method}</h4><p><code>id</code> 方法是<code>bigIncrements</code> 的别名。默认情况下，该方法将创建一个 <code>id</code> 列; 但是，如果要为列指定不同的名称，则可以传递列名：</p><pre><code>$table-&gt;id();
</code></pre><p><a name="column-method-increments"></a></p><h4 id="increments-collection-method" tabindex="-1"><a class="header-anchor" href="#increments-collection-method" aria-hidden="true">#</a> <code>increments()</code> {.collection-method}</h4><p><code>increments</code> 方法创建一个自动递增相当于 <code>UNSIGNED INTEGER</code> 的列作为主键：</p><pre><code>$table-&gt;increments(&#39;id&#39;);
</code></pre><p><a name="column-method-integer"></a></p><h4 id="integer-collection-method" tabindex="-1"><a class="header-anchor" href="#integer-collection-method" aria-hidden="true">#</a> <code>integer()</code> {.collection-method}</h4><p><code>integer</code> 方法相当于 <code>INTEGER</code> ：</p><pre><code>$table-&gt;integer(&#39;votes&#39;);
</code></pre><p><a name="column-method-ipAddress"></a></p><h4 id="ipaddress-collection-method" tabindex="-1"><a class="header-anchor" href="#ipaddress-collection-method" aria-hidden="true">#</a> <code>ipAddress()</code> {.collection-method}</h4><p><code>ipAddress</code> 方法相当于 <code>VARCHAR</code> ：</p><pre><code>$table-&gt;ipAddress(&#39;visitor&#39;);
</code></pre><p><a name="column-method-json"></a></p><h4 id="json-collection-method" tabindex="-1"><a class="header-anchor" href="#json-collection-method" aria-hidden="true">#</a> <code>json()</code> {.collection-method}</h4><p><code>json</code> 方法相当于 <code>JSON</code>：</p><pre><code>$table-&gt;json(&#39;options&#39;);
</code></pre><p><a name="column-method-jsonb"></a></p><h4 id="jsonb-collection-method" tabindex="-1"><a class="header-anchor" href="#jsonb-collection-method" aria-hidden="true">#</a> <code>jsonb()</code> {.collection-method}</h4><p><code>jsonb</code> 方法相当于 <code>JSONB</code>：</p><pre><code>$table-&gt;jsonb(&#39;options&#39;);
</code></pre><p><a name="column-method-lineString"></a></p><h4 id="linestring-collection-method" tabindex="-1"><a class="header-anchor" href="#linestring-collection-method" aria-hidden="true">#</a> <code>lineString()</code> {.collection-method}</h4><p><code>lineString</code> 方法相当于 <code>LINESTRING</code>：</p><pre><code>$table-&gt;lineString(&#39;positions&#39;);
</code></pre><p><a name="column-method-longText"></a></p><h4 id="longtext-collection-method" tabindex="-1"><a class="header-anchor" href="#longtext-collection-method" aria-hidden="true">#</a> <code>longText()</code> {.collection-method}</h4><p><code>longText</code> 方法相当于 <code>LONGTEXT</code>：</p><pre><code>$table-&gt;longText(&#39;description&#39;);
</code></pre><p><a name="column-method-macAddress"></a></p><h4 id="macaddress-collection-method" tabindex="-1"><a class="header-anchor" href="#macaddress-collection-method" aria-hidden="true">#</a> <code>macAddress()</code> {.collection-method}</h4><p><code>macAddress</code> 方法创建一个用于保存 MAC 地址的列。一些数据库系统（如 PostgreSQL），为这种类型的数据提供了专用的类型。其他数据库系统相当于使用字符串类型：</p><pre><code>$table-&gt;macAddress(&#39;device&#39;);
</code></pre><p><a name="column-method-mediumIncrements"></a></p><h4 id="mediumincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#mediumincrements-collection-method" aria-hidden="true">#</a> <code>mediumIncrements()</code> {.collection-method}</h4><p><code>mediumIncrements</code> 方法用于创建一个 <code>UNSIGNED MEDIUMINT</code> 类型的自动递增的列作为主键：</p><pre><code>$table-&gt;mediumIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-mediumInteger"></a></p><h4 id="mediuminteger-collection-method" tabindex="-1"><a class="header-anchor" href="#mediuminteger-collection-method" aria-hidden="true">#</a> <code>mediumInteger()</code> {.collection-method}</h4><p><code>mediumInteger</code> 方法用于创建一个 <code>MEDIUMINT</code> 类型的列：</p><pre><code>$table-&gt;mediumInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-mediumText"></a></p><h4 id="mediumtext-collection-method" tabindex="-1"><a class="header-anchor" href="#mediumtext-collection-method" aria-hidden="true">#</a> <code>mediumText()</code> {.collection-method}</h4><p><code>mediumText</code> 方法用于创建一个 <code>MEDIUMTEXT</code> 类型的列：</p><pre><code>$table-&gt;mediumText(&#39;description&#39;);
</code></pre><p><a name="column-method-morphs"></a></p><h4 id="morphs-collection-method" tabindex="-1"><a class="header-anchor" href="#morphs-collection-method" aria-hidden="true">#</a> <code>morphs()</code> {.collection-method}</h4><p><code>morphs</code> 方法用于快速创建一个名称为 <code>{column}_id</code> ，类型为 <code>UNSIGNED BIGINT</code> 的列和一个名称为 <code>{column}_type</code> ，类型为 <code>VARCHAR</code> 的列。</p><p>这个方法在定义<a href="./eloquent-relationships">多态关联</a>所需的列时使用。在下面的例子中， <code>taggable_id</code> 和 <code>taggable_type</code> 这两个列会被创建：</p><pre><code>$table-&gt;morphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-multiLineString"></a></p><h4 id="multilinestring-collection-method" tabindex="-1"><a class="header-anchor" href="#multilinestring-collection-method" aria-hidden="true">#</a> <code>multiLineString()</code> {.collection-method}</h4><p><code>multiLineString</code> 方法用于创建一个 <code>MULTILINESTRING</code> 类型的列：</p><pre><code>$table-&gt;multiLineString(&#39;positions&#39;);
</code></pre><p><a name="column-method-multiPoint"></a></p><h4 id="multipoint-collection-method" tabindex="-1"><a class="header-anchor" href="#multipoint-collection-method" aria-hidden="true">#</a> <code>multiPoint()</code> {.collection-method}</h4><p><code>multiPoint</code> 方法用于创建一个 <code>MULTIPOINT</code> 类型的列：</p><pre><code>$table-&gt;multiPoint(&#39;positions&#39;);
</code></pre><p><a name="column-method-multiPolygon"></a></p><h4 id="multipolygon-collection-method" tabindex="-1"><a class="header-anchor" href="#multipolygon-collection-method" aria-hidden="true">#</a> <code>multiPolygon()</code> {.collection-method}</h4><p><code>multiPolygon</code> 方法用于创建一个 <code>MULTIPOLYGON</code> 类型的列：</p><pre><code>$table-&gt;multiPolygon(&#39;positions&#39;);
</code></pre><p><a name="column-method-nullableTimestamps"></a></p><h4 id="nullabletimestamps-collection-method" tabindex="-1"><a class="header-anchor" href="#nullabletimestamps-collection-method" aria-hidden="true">#</a> <code>nullableTimestamps()</code> {.collection-method}</h4><p>这个方法和 <a href="#column-method-timestamps">timestamps</a> 方法类似；需要注意的是此方法创建的列是 <code>nullable</code> 的：</p><pre><code>$table-&gt;nullableTimestamps(0);
</code></pre><p><a name="column-method-nullableMorphs"></a></p><h4 id="nullablemorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullablemorphs-collection-method" aria-hidden="true">#</a> <code>nullableMorphs()</code> {.collection-method}</h4><p>这个方法和 <a href="#column-method-morphs">morphs</a> 方法类似；需要注意的是此方法创建的列是 <code>nullable</code> 的：</p><pre><code>$table-&gt;nullableMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-nullableUlidMorphs"></a></p><h4 id="nullableulidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullableulidmorphs-collection-method" aria-hidden="true">#</a> <code>nullableUlidMorphs()</code> {.collection-method}</h4><p>这个方法和 <a href="#column-method-ulidMorphs">ulidMorphs</a> 方法类似；需要注意的是此方法创建的列是 <code>nullable</code>。</p><pre><code>$table-&gt;nullableUlidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-nullableUuidMorphs"></a></p><h4 id="nullableuuidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullableuuidmorphs-collection-method" aria-hidden="true">#</a> <code>nullableUuidMorphs()</code> {.collection-method}</h4><p>这个方法和 <a href="#column-method-uuidMorphs">uuidMorphs</a> 方法类似；需要注意的是此方法创建的列是 <code>nullable</code> 的：</p><pre><code>$table-&gt;nullableUuidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-point"></a></p><h4 id="point-collection-method" tabindex="-1"><a class="header-anchor" href="#point-collection-method" aria-hidden="true">#</a> <code>point()</code> {.collection-method}</h4><p><code>point</code> 方法相当于 <code>POINT</code>：</p><pre><code>$table-&gt;point(&#39;position&#39;);
</code></pre><p><a name="column-method-polygon"></a></p><h4 id="polygon-collection-method" tabindex="-1"><a class="header-anchor" href="#polygon-collection-method" aria-hidden="true">#</a> <code>polygon()</code> {.collection-method}</h4><p>The <code>polygon</code> method creates a <code>POLYGON</code> equivalent column:</p><pre><code>$table-&gt;polygon(&#39;position&#39;);
</code></pre><p><a name="column-method-rememberToken"></a></p><h4 id="remembertoken-collection-method" tabindex="-1"><a class="header-anchor" href="#remembertoken-collection-method" aria-hidden="true">#</a> <code>rememberToken()</code> {.collection-method}</h4><p>添加一个允许空值的 <code>VARCHAR (100)</code> 类型的 <code>remember_token</code> 字段，用于存储 <a href="./authentication#remembering-users">记住用户</a>：</p><pre><code>$table-&gt;rememberToken();
</code></pre><p><a name="column-method-set"></a></p><h4 id="set-collection-method" tabindex="-1"><a class="header-anchor" href="#set-collection-method" aria-hidden="true">#</a> <code>set()</code> {.collection-method}</h4><p><code>set</code> 方法使用给定的有效值列表创建一个 <code>SET</code> 等效列：</p><pre><code>$table-&gt;set(&#39;flavors&#39;, [&#39;strawberry&#39;, &#39;vanilla&#39;]);
</code></pre><p><a name="column-method-smallIncrements"></a></p><h4 id="smallincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#smallincrements-collection-method" aria-hidden="true">#</a> <code>smallIncrements()</code> {.collection-method}</h4><p><code>smallIncrements</code> 方法创建一个自动递增的 <code>UNSIGNED SMALLINT</code> 等效列作为主键：</p><pre><code>$table-&gt;smallIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-smallInteger"></a></p><h4 id="smallinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#smallinteger-collection-method" aria-hidden="true">#</a> <code>smallInteger()</code> {.collection-method}</h4><p><code>smallInteger</code> 方法创建一个 <code>SMALLINT</code> 等效列：</p><pre><code>$table-&gt;smallInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-softDeletesTz"></a></p><h4 id="softdeletestz-collection-method" tabindex="-1"><a class="header-anchor" href="#softdeletestz-collection-method" aria-hidden="true">#</a> <code>softDeletesTz()</code> {.collection-method}</h4><p><code>softDeletesTz</code> 方法添加了一个可为空的 <code>deleted_at</code> <code>TIMESTAMP</code>（带时区）等效列，具有可选精度（总位数）。此列旨在存储 Eloquent 的“软删除”功能所需的 <code>deleted_at</code> 时间戳：</p><pre><code>$table-&gt;softDeletesTz($column = &#39;deleted_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-softDeletes"></a></p><h4 id="softdeletes-collection-method" tabindex="-1"><a class="header-anchor" href="#softdeletes-collection-method" aria-hidden="true">#</a> <code>softDeletes()</code> {.collection-method}</h4><p><code>softDeletes</code> 方法添加了一个可为空的 <code>deleted_at</code> <code>TIMESTAMP</code> 等效列，具有可选精度（总位数）。此列旨在存储 Eloquent 的「软删除」功能所需的 <code>deleted_at</code> 时间戳，相当于为软删除添加一个可空的 <code>deleted_at</code> 字段：</p><pre><code>$table-&gt;softDeletes($column = &#39;deleted_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-string"></a></p><h4 id="string-collection-method" tabindex="-1"><a class="header-anchor" href="#string-collection-method" aria-hidden="true">#</a> <code>string()</code> {.collection-method}</h4><p><code>string</code> 方法创建一个给定长度的 <code>VARCHAR</code> 等效列，相当于指定长度的 VARCHAR：</p><pre><code>$table-&gt;string(&#39;name&#39;, 100);
</code></pre><p><a name="column-method-text"></a></p><h4 id="text-collection-method" tabindex="-1"><a class="header-anchor" href="#text-collection-method" aria-hidden="true">#</a> <code>text()</code> {.collection-method}</h4><p><code>text</code> 方法创建一个 <code>TEXT</code> 等效列：</p><pre><code>$table-&gt;text(&#39;description&#39;);
</code></pre><p><a name="column-method-timeTz"></a></p><h4 id="timetz-collection-method" tabindex="-1"><a class="header-anchor" href="#timetz-collection-method" aria-hidden="true">#</a> <code>timeTz()</code> {.collection-method}</h4><p><code>timeTz</code> 方法创建一个具有可选精度（总位数）的 <code>TIME</code>（带时区）等效列：</p><pre><code>$table-&gt;timeTz(&#39;sunrise&#39;, $precision = 0);
</code></pre><p><a name="column-method-time"></a></p><h4 id="time-collection-method" tabindex="-1"><a class="header-anchor" href="#time-collection-method" aria-hidden="true">#</a> <code>time()</code> {.collection-method}</h4><p><code>time</code> 方法创建一个具有可选精度（总位数）的 <code>TIME</code> 等效列：</p><pre><code>$table-&gt;time(&#39;sunrise&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestampTz"></a></p><h4 id="timestamptz-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamptz-collection-method" aria-hidden="true">#</a> <code>timestampTz()</code> {.collection-method}</h4><p><code>timestampTz</code> 方法创建一个具有可选精度（总位数）的 <code>TIMESTAMP</code>（带时区）等效列：</p><pre><code>$table-&gt;timestampTz(&#39;added_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestamp"></a></p><h4 id="timestamp-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamp-collection-method" aria-hidden="true">#</a> <code>timestamp()</code> {.collection-method}</h4><p><code>timestamp</code> 方法创建一个具有可选精度（总位数）的 <code>TIMESTAMP</code> 等效列：</p><pre><code>$table-&gt;timestamp(&#39;added_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestampsTz"></a></p><h4 id="timestampstz-collection-method" tabindex="-1"><a class="header-anchor" href="#timestampstz-collection-method" aria-hidden="true">#</a> <code>timestampsTz()</code> {.collection-method}</h4><p><code>timestampsTz</code> 方法创建 <code>created_at</code> 和 <code>updated_at</code> <code>TIMESTAMP</code>（带时区）等效列，具有可选精度（总位数）：</p><pre><code>$table-&gt;timestampsTz($precision = 0);
</code></pre><p><a name="column-method-timestamps"></a></p><h4 id="timestamps-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamps-collection-method" aria-hidden="true">#</a> <code>timestamps()</code> {.collection-method}</h4><p><code>timestamps</code> 方法创建具有可选精度（总位数）的 <code>created_at</code> 和 <code>updated_at</code> <code>TIMESTAMP</code> 等效列：</p><pre><code>$table-&gt;timestamps($precision = 0);
</code></pre><p><a name="column-method-tinyIncrements"></a></p><h4 id="tinyincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#tinyincrements-collection-method" aria-hidden="true">#</a> <code>tinyIncrements()</code> {.collection-method}</h4><p><code>tinyIncrements</code> 方法创建一个自动递增的 <code>UNSIGNED TINYINT</code> 等效列作为主键：</p><pre><code>$table-&gt;tinyIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-tinyInteger"></a></p><h4 id="tinyinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#tinyinteger-collection-method" aria-hidden="true">#</a> <code>tinyInteger()</code> {.collection-method}</h4><p><code>tinyInteger</code> 方法用于创建一个 <code>TINYINT</code> 等效列：</p><pre><code>$table-&gt;tinyInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-tinyText"></a></p><h4 id="tinytext-collection-method" tabindex="-1"><a class="header-anchor" href="#tinytext-collection-method" aria-hidden="true">#</a> <code>tinyText()</code> {.collection-method}</h4><p><code>tinyText</code> 方法用于创建一个 <code>TINYTEXT</code> 等效列：</p><pre><code>$table-&gt;tinyText(&#39;notes&#39;);
</code></pre><p><a name="column-method-unsignedBigInteger"></a></p><h4 id="unsignedbiginteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedbiginteger-collection-method" aria-hidden="true">#</a> <code>unsignedBigInteger()</code> {.collection-method}</h4><p><code>unsignedBigInteger</code> 方法用于创建一个 <code>UNSIGNED BIGINT</code> 等效列：</p><pre><code>$table-&gt;unsignedBigInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedDecimal"></a></p><h4 id="unsigneddecimal-collection-method" tabindex="-1"><a class="header-anchor" href="#unsigneddecimal-collection-method" aria-hidden="true">#</a> <code>unsignedDecimal()</code> {.collection-method}</h4><p><code>unsignedDecimal</code> 方法用于创建一个 <code>UNSIGNED DECIMAL</code> 等效列，具有可选的精度（总位数）和小数位数（小数位数）：</p><pre><code>$table-&gt;unsignedDecimal(&#39;amount&#39;, $precision = 8, $scale = 2);
</code></pre><p><a name="column-method-unsignedInteger"></a></p><h4 id="unsignedinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedinteger-collection-method" aria-hidden="true">#</a> <code>unsignedInteger()</code> {.collection-method}</h4><p><code>unsignedInteger</code> 方法用于创建一个 <code>UNSIGNED INTEGER</code> 等效列：</p><pre><code>$table-&gt;unsignedInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedMediumInteger"></a></p><h4 id="unsignedmediuminteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedmediuminteger-collection-method" aria-hidden="true">#</a> <code>unsignedMediumInteger()</code> {.collection-method}</h4><p><code>unsignedMediumInteger</code> 方法用于创建一个 <code>UNSIGNED MEDIUMINT</code> 等效列：</p><pre><code>$table-&gt;unsignedMediumInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedSmallInteger"></a></p><h4 id="unsignedsmallinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedsmallinteger-collection-method" aria-hidden="true">#</a> <code>unsignedSmallInteger()</code> {.collection-method}</h4><p><code>unsignedSmallInteger</code> 方法用于创建一个 <code>UNSIGNED SMALLINT</code> 等效列：</p><pre><code>$table-&gt;unsignedSmallInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedTinyInteger"></a></p><h4 id="unsignedtinyinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedtinyinteger-collection-method" aria-hidden="true">#</a> <code>unsignedTinyInteger()</code> {.collection-method}</h4><p><code>unsignedTinyInteger</code> 方法用于创建一个 <code>UNSIGNED TINYINT</code> 等效列：</p><pre><code>$table-&gt;unsignedTinyInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-ulidMorphs"></a></p><h4 id="ulidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#ulidmorphs-collection-method" aria-hidden="true">#</a> <code>ulidMorphs()</code> {.collection-method}</h4><p><code>ulidMorphs</code> 方法用于快速创建一个名称为 <code>{column}_id</code> ，类型为 <code>CHAR(26)</code> 的列和一个名称为 <code>{column}_type</code> ，类型为 <code>VARCHAR</code> 的列。</p><p>这个方法用于定义使用 UUID 标识符的<a href="./eloquent-relationships">多态关联</a>所需的列时使用。在下面的例子中，<code>taggable_id</code> 和 <code>taggable_type</code> 这两个列将会被创建：</p><pre><code>$table-&gt;ulidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-uuidMorphs"></a></p><h4 id="uuidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#uuidmorphs-collection-method" aria-hidden="true">#</a> <code>uuidMorphs()</code> {.collection-method}</h4><p><code>uuidMorphs</code> 方法用于快速创建一个名称为 <code>{column}_id</code> ，类型为 <code>CHAR(36)</code> 的列和一个名称为 <code>{column}_type</code> ，类型为 <code>VARCHAR</code> 的列。</p><p>这个方法用于定义使用 UUID 标识符的<a href="./eloquent-relationships">多态关联</a>所需的列时使用。在下面的例子中，<code>taggable_id</code> 和 <code>taggable_type</code> 这两个列将会被创建：</p><pre><code>$table-&gt;uuidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-ulid"></a></p><h4 id="ulid-collection-method" tabindex="-1"><a class="header-anchor" href="#ulid-collection-method" aria-hidden="true">#</a> <code>ulid()</code> {.collection-method}</h4><p><code>ulid</code> 方法用于创建一个 <code>ULID</code> 类型的列：</p><pre><code>$table-&gt;ulid(&#39;id&#39;);
</code></pre><p><a name="column-method-uuid"></a></p><h4 id="uuid-collection-method" tabindex="-1"><a class="header-anchor" href="#uuid-collection-method" aria-hidden="true">#</a> <code>uuid()</code> {.collection-method}</h4><p><code>uuid</code> 方法用于创建一个 <code>UUID</code> 类型的列：</p><pre><code>$table-&gt;uuid(&#39;id&#39;);
</code></pre><p><a name="column-method-year"></a></p><h4 id="year-collection-method" tabindex="-1"><a class="header-anchor" href="#year-collection-method" aria-hidden="true">#</a> <code>year()</code> {.collection-method}</h4><p><code>year</code> 方法用于创建一个 <code>YEAR</code> 类型的列：</p><pre><code>$table-&gt;year(&#39;birth_year&#39;);
</code></pre><p><a name="column-modifiers"></a></p><h3 id="字段修饰符" tabindex="-1"><a class="header-anchor" href="#字段修饰符" aria-hidden="true">#</a> 字段修饰符</h3><p>除了上面列出的列类型外，在向数据库表添加列时还有几个可以使用的「修饰符」。例如，如果要把列设置为要使列为「可空」，你可以使用 <code>nullable</code> 方法：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;email&#39;)-&gt;nullable();
});
</code></pre><p>下表时所有可用的列修饰符。此列表不包括<a href="#creating-indexes">索引修饰符</a>:</p><table><thead><tr><th>修饰符</th><th>说明</th></tr></thead><tbody><tr><td><code>-&gt;after(&#39;column&#39;)</code></td><td>将该列放在其它字段「之后」(MySQL)</td></tr><tr><td><code>-&gt;autoIncrement()</code></td><td>设置 INTEGER 类型的列为自动递增 (主键)</td></tr><tr><td><code>-&gt;charset(&#39;utf8mb4&#39;)</code></td><td>为该列指定字符集 (MySQL)</td></tr><tr><td><code>-&gt;collation(&#39;utf8mb4_unicode_ci&#39;)</code></td><td>为该列指定排序规则 (MySQL/PostgreSQL/SQL Server)</td></tr><tr><td><code>-&gt;comment(&#39;my comment&#39;)</code></td><td>为该列添加注释 (MySQL/PostgreSQL)</td></tr><tr><td><code>-&gt;default($value)</code></td><td>为该列指定一个「默认值」</td></tr><tr><td><code>-&gt;first()</code></td><td>将该列放在该表「首位」 (MySQL)</td></tr><tr><td><code>-&gt;from($integer)</code></td><td>设置自动递增字段的起始值 (MySQL / PostgreSQL)</td></tr><tr><td><code>-&gt;invisible()</code></td><td>使列对「SELECT *」查询不可见（MySQL）。</td></tr><tr><td><code>-&gt;nullable($value = true)</code></td><td>允许 NULL 值插入到该列</td></tr><tr><td><code>-&gt;storedAs($expression)</code></td><td>创建一个存储生成的列 (MySQL)</td></tr><tr><td><code>-&gt;unsigned()</code></td><td>设置 INTEGER 类型的字段为 UNSIGNED (MySQL)</td></tr><tr><td><code>-&gt;useCurrent()</code></td><td>设置 TIMESTAMP 类型的列使用 CURRENT_TIMESTAMP 作为默认值</td></tr><tr><td><code>-&gt;useCurrentOnUpdate()</code></td><td>将 TIMESTAMP 类型的列设置为在更新时使用 CURRENT_TIMESTAMP 作为新值</td></tr><tr><td><code>-&gt;virtualAs($expression)</code></td><td>创建一个虚拟生成的列 (MySQL)</td></tr><tr><td><code>-&gt;generatedAs($expression)</code></td><td>使用指定的序列选项创建标识列 (PostgreSQL)</td></tr><tr><td><code>-&gt;always()</code></td><td>定义序列值优先于标识列的输入 (PostgreSQL)</td></tr><tr><td><code>-&gt;isGeometry()</code></td><td>将空间列类型设置为 <code>geometry</code> - 默认类型为 <code>geography</code> (PostgreSQL)。</td></tr></tbody></table><p><a name="default-expressions"></a></p><h4 id="默认值表达式" tabindex="-1"><a class="header-anchor" href="#默认值表达式" aria-hidden="true">#</a> 默认值表达式</h4><p><code>default</code> 修饰符接收一个变量或者一个 <code>\\Illuminate\\Database\\Query\\Expression</code> 实例。使用 <code>Expression</code> 实例可以避免使用包含在引号中的值，并且允许你使用特定数据库函数。这在当你需要给 <code>JSON</code> 字段指定默认值的时候特别有用：</p><pre><code>&lt;?php

use Illuminate\\Support\\Facades\\Schema;
use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Database\\Query\\Expression;
use Illuminate\\Database\\Migrations\\Migration;

return new class extends Migration
{
    /**
     * 运行迁移
     */
    public function up(): void
    {
        Schema::create(&#39;flights&#39;, function (Blueprint $table) {
            $table-&gt;id();
            $table-&gt;json(&#39;movies&#39;)-&gt;default(new Expression(&#39;(JSON_ARRAY())&#39;));
            $table-&gt;timestamps();
        });
    }
};
</code></pre><blockquote><p><strong>注意</strong><br> 支持哪些默认值的表示方式取决于你的数据库驱动、数据库版本、还有字段类型。请参考合适的文档使用。还有一点要注意的是，使用数据库特定函数，可能会将你绑牢到特定的数据库驱动上。</p></blockquote><p><a name="column-order"></a></p><h4 id="字段顺序" tabindex="-1"><a class="header-anchor" href="#字段顺序" aria-hidden="true">#</a> 字段顺序</h4><p>使用 MySQL 数据库时，可以使用 <code>after</code> 方法在模式中的现有列后添加列：</p><pre><code>$table-&gt;after(&#39;password&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;address_line1&#39;);
    $table-&gt;string(&#39;address_line2&#39;);
    $table-&gt;string(&#39;city&#39;);
});
</code></pre><p><a name="modifying-columns"></a></p><h3 id="修改字段" tabindex="-1"><a class="header-anchor" href="#修改字段" aria-hidden="true">#</a> 修改字段</h3><p><code>change</code> 方法可以将现有的字段类型修改为新的类型或修改属性。比如，你可能想增加 <code>string</code> 字段的长度，可以使用 <code>change</code> 方法把 <code>name</code> 字段的长度从 25 增加到 50。所以，我们可以简单的更新字段属性然后调用 <code>change</code> 方法：</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;name&#39;, 50)-&gt;change();
});
</code></pre><p>当修改一个列时，你必须明确包括所有你想在列定义上保留的修改器 —— 任何缺失的属性都将被丢弃。例如，为了保留 <code>unsigned</code>、<code>default</code> 和 <code>comment</code> 属性，你必须在修改列时明确每个属性的修改。</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;)-&gt;unsigned()-&gt;default(1)-&gt;comment(&#39;my comment&#39;)-&gt;change();
});
</code></pre><p><a name="modifying-columns-on-sqlite"></a></p><h4 id="在-sqlite-上修改列" tabindex="-1"><a class="header-anchor" href="#在-sqlite-上修改列" aria-hidden="true">#</a> 在 SQLite 上修改列</h4><p>如果应用程序使用的是 SQLite 数据库，请确保你已经通过 Composer 包管理器安装了 <code>doctrine/dbal</code> 包。Doctrine DBAL 库用于确定字段的当前状态，并创建对该字段进行指定调整所需的 SQL 查询：</p><pre><code>composer require doctrine/dbal
</code></pre><p>如果你打算修改 <code>timestamp</code> 方法来创建列，你还需要将以下配置添加到应用程序的<code>config/database.php</code>配置文件中：</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Database<span class="token punctuation">\\</span>DBAL<span class="token punctuation">\\</span>TimestampType</span><span class="token punctuation">;</span>

<span class="token string single-quoted-string">&#39;dbal&#39;</span> <span class="token operator">=&gt;</span> <span class="token punctuation">[</span>
    <span class="token string single-quoted-string">&#39;types&#39;</span> <span class="token operator">=&gt;</span> <span class="token punctuation">[</span>
        <span class="token string single-quoted-string">&#39;timestamp&#39;</span> <span class="token operator">=&gt;</span> <span class="token class-name static-context">TimestampType</span><span class="token operator">::</span><span class="token keyword">class</span><span class="token punctuation">,</span>
    <span class="token punctuation">]</span><span class="token punctuation">,</span>
<span class="token punctuation">]</span><span class="token punctuation">,</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>注意</strong><br> 当使用 <code>doctrine/dbal</code> 包时，你可以修改以下列类型：<code>bigInteger</code>、<code>binary</code>、<code>boolean</code>、<code>char</code>、<code>date</code>、<code>dateTime</code>、<code>dateTimeTz</code>、<code>decimal</code>、<code>double</code>、<code>integer</code>、<code>json</code>、<code>longText</code>、<code>mediumText</code>、<code>smallInteger</code>、<code>string</code>、<code>text</code>、<code>time</code>、<code>tinyText</code>、<code>unsignedBigInteger</code>、<code>unsignedInteger</code>、<code>unsignedSmallInteger</code>、<code>ulid</code>、和 <code>uuid</code>。</p></blockquote><p><a name="renaming-columns"></a></p><h4 id="重命名字段" tabindex="-1"><a class="header-anchor" href="#重命名字段" aria-hidden="true">#</a> 重命名字段</h4><p>要重命名一个列，你可以使用模式构建器提供的 <code>renameColumn</code> 方法：</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;renameColumn(&#39;from&#39;, &#39;to&#39;);
});
</code></pre><p><a name="renaming-columns-on-legacy-databases"></a></p><h4 id="在较低版本数据库上重命名列" tabindex="-1"><a class="header-anchor" href="#在较低版本数据库上重命名列" aria-hidden="true">#</a> 在较低版本数据库上重命名列</h4><p>如果你运行的数据库低于以下版本，你应该确保在重命名列之前通过 Composer 软件包管理器安装了 <code>doctrine/dbal</code> 库。</p>`,310),p=e("div",{class:"content-list",markdown:"1"},[e("ul",null,[e("li",null,[o("MySQL < "),e("code",null,"8.0.3")]),e("li",null,[o("MariaDB < "),e("code",null,"10.5.2")]),e("li",null,[o("SQLite < "),e("code",null,"3.25.0")])])],-1),u=d(`<p><a name="dropping-columns"></a></p><h3 id="删除字段" tabindex="-1"><a class="header-anchor" href="#删除字段" aria-hidden="true">#</a> 删除字段</h3><p>要删除一个列，你可以使用 <code>dropColumn</code> 方法。</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;dropColumn(&#39;votes&#39;);
});
</code></pre><p>你可以传递一个字段数组给 <code>dropColumn</code> 方法来删除多个字段：</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;dropColumn([&#39;votes&#39;, &#39;avatar&#39;, &#39;location&#39;]);
});
</code></pre><p><a name="dropping-columns-on-legacy-databases"></a></p><h4 id="在较低版本的数据库中删除列的内容" tabindex="-1"><a class="header-anchor" href="#在较低版本的数据库中删除列的内容" aria-hidden="true">#</a> 在较低版本的数据库中删除列的内容</h4><p>如果你运行的 SQLite 版本在 <code>3.35.0</code> 之前，你必须通过 Composer 软件包管理器安装 <code>doctrine/dbal</code> 包，然后才能使用 <code>dropColumn</code> 方法。不支持在使用该包时在一次迁移中删除或修改多个列。</p><p><a name="available-command-aliases"></a></p><h4 id="可用的命令别名" tabindex="-1"><a class="header-anchor" href="#可用的命令别名" aria-hidden="true">#</a> 可用的命令别名</h4><p>Laravel 提供了几种常用的删除相关列的便捷方法。如下表所示：</p><table><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td><code>$table-&gt;dropMorphs(&#39;morphable&#39;);</code></td><td>删除 <code>morphable_id</code> 和 <code>morphable_type</code> 字段</td></tr><tr><td><code>$table-&gt;dropRememberToken();</code></td><td>删除 <code>remember_token</code> 字段</td></tr><tr><td><code>$table-&gt;dropSoftDeletes();</code></td><td>删除 <code>deleted_at</code> 字段</td></tr><tr><td><code>$table-&gt;dropSoftDeletesTz();</code></td><td><code>dropSoftDeletes()</code> 方法的别名</td></tr><tr><td><code>$table-&gt;dropTimestamps();</code></td><td>删除 <code>created_at</code> 和 <code>updated_at</code> 字段</td></tr><tr><td><code>$table-&gt;dropTimestampsTz();</code></td><td><code>dropTimestamps()</code> 方法别名</td></tr></tbody></table><p><a name="indexes"></a></p><h2 id="索引" tabindex="-1"><a class="header-anchor" href="#索引" aria-hidden="true">#</a> 索引</h2><p><a name="creating-indexes"></a></p><h3 id="创建索引" tabindex="-1"><a class="header-anchor" href="#创建索引" aria-hidden="true">#</a> 创建索引</h3><p>结构生成器支持多种类型的索引。下面的例子中新建了一个值唯一的 <code>email</code> 字段。我们可以将 <code>unique</code> 方法链式地添加到字段定义上来创建索引：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;email&#39;)-&gt;unique();
});
</code></pre><p>或者，你也可以在定义完字段之后创建索引。为此，你应该调用结构生成器上的 <code>unique</code> 方法，此方法应该传入唯一索引的列名称：</p><pre><code>$table-&gt;unique(&#39;email&#39;);
</code></pre><p>你甚至可以将数组传递给索引方法来创建一个复合（或合成）索引：</p><pre><code>$table-&gt;index([&#39;account_id&#39;, &#39;created_at&#39;]);
</code></pre><p>创建索引时，Laravel 会自动生成一个合理的索引名称，但你也可以传递第二个参数来自定义索引名称：</p><pre><code>$table-&gt;unique(&#39;email&#39;, &#39;unique_email&#39;);
</code></pre><p><a name="available-index-types"></a></p><h4 id="可用的索引类型" tabindex="-1"><a class="header-anchor" href="#可用的索引类型" aria-hidden="true">#</a> 可用的索引类型</h4><p>Laravel 的结构生成器提供了 Laravel 支持的所有类型的索引方法。每个索引方法都接受一个可选的第二个参数来指定索引的名称。如果省略，名称将根据表和列的名称生成。下面是所有可用的索引方法：</p><table><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td><code>$table-&gt;primary(&#39;id&#39;);</code></td><td>添加主键</td></tr><tr><td><code>$table-&gt;primary([&#39;id&#39;, &#39;parent_id&#39;]);</code></td><td>添加复合主键</td></tr><tr><td><code>$table-&gt;unique(&#39;email&#39;);</code></td><td>添加唯一索引</td></tr><tr><td><code>$table-&gt;index(&#39;state&#39;);</code></td><td>添加普通索引</td></tr><tr><td><code>$table-&gt;fullText(&#39;body&#39;);</code></td><td>添加全文索引 (MySQL/PostgreSQL)</td></tr><tr><td><code>$table-&gt;fullText(&#39;body&#39;)-&gt;language(&#39;english&#39;);</code></td><td>添加指定语言 (PostgreSQL) 的全文索引</td></tr><tr><td><code>$table-&gt;spatialIndex(&#39;location&#39;);</code></td><td>添加空间索引（不支持 SQLite）</td></tr></tbody></table><p><a name="index-lengths-mysql-mariadb"></a></p><h4 id="索引长度-mysql-mariadb" tabindex="-1"><a class="header-anchor" href="#索引长度-mysql-mariadb" aria-hidden="true">#</a> 索引长度 &amp; MySQL / MariaDB</h4><p>默认情况下，Laravel 使用 <code>utf8mb4</code> 编码。如果你是在版本低于 5.7.7 的 MySQL 或者版本低于 10.2.2 的 MariaDB 上创建索引，那你就需要手动配置数据库迁移的默认字符串长度。也就是说，你可以通过在 <code>App\\Providers\\AppServiceProvider</code> 类的 <code>boot</code> 方法中调用 <code>Schema::defaultStringLength</code> 方法来配置默认字符串长度：</p><pre><code>use Illuminate\\Support\\Facades\\Schema;

/**
 * 引导任何应用程序「全局配置」
 */
public function boot(): void
{
    Schema::defaultStringLength(191);
}
</code></pre><p>当然，你也可以选择开启数据库的 <code>innodb_large_prefix</code> 选项。至于如何正确开启，请自行查阅数据库文档。</p><p><a name="renaming-indexes"></a></p><h3 id="重命名索引" tabindex="-1"><a class="header-anchor" href="#重命名索引" aria-hidden="true">#</a> 重命名索引</h3><p>若要重命名索引，你需要调用 <code>renameIndex</code> 方法。此方法接受当前索引名称作为其第一个参数，并将所需名称作为其第二个参数：</p><pre><code>$table-&gt;renameIndex(&#39;from&#39;, &#39;to&#39;)
</code></pre><blockquote><p><strong>注意</strong><br> 如果你的应用程序使用的是 SQLite 数据库，你必须通过 Composer 软件包管理器安装 <code>doctrine/dbal</code> 包，然后才能使用 <code>renameIndex</code> 方法。</p></blockquote><p><a name="dropping-indexes"></a></p><h3 id="删除索引" tabindex="-1"><a class="header-anchor" href="#删除索引" aria-hidden="true">#</a> 删除索引</h3><p>若要删除索引，则必须指定索引的名称。Laravel 默认会自动将数据表名称、索引的字段名及索引类型简单地连接在一起作为名称。举例如下：</p><table><thead><tr><th>命令</th><th>说明</th></tr></thead><tbody><tr><td><code>$table-&gt;dropPrimary(&#39;users_id_primary&#39;);</code></td><td>从「users」表中删除主键</td></tr><tr><td><code>$table-&gt;dropUnique(&#39;users_email_unique&#39;);</code></td><td>从「users」表中删除 unique 索引</td></tr><tr><td><code>$table-&gt;dropIndex(&#39;geo_state_index&#39;);</code></td><td>从「geo」表中删除基本索引</td></tr><tr><td><code>$table-&gt;dropFullText(&#39;posts_body_fulltext&#39;);</code></td><td>从「post」表中删除一个全文索引</td></tr><tr><td><code>$table-&gt;dropSpatialIndex(&#39;geo_location_spatialindex&#39;);</code></td><td>从「geo」表中删除空间索引（不支持 SQLite）</td></tr></tbody></table><p>如果将字段数组传给 <code>dropIndex</code> 方法，会删除根据表名、字段和键类型生成的索引名称。</p><pre><code>Schema::table(&#39;geo&#39;, function (Blueprint $table) {
    $table-&gt;dropIndex([&#39;state&#39;]); // 删除 &#39;geo_state_index&#39; 索引
});
</code></pre><p><a name="foreign-key-constraints"></a></p><h3 id="外键约束" tabindex="-1"><a class="header-anchor" href="#外键约束" aria-hidden="true">#</a> 外键约束</h3><p>Laravel 还支持创建用于在数据库层中的强制引用完整性的外键约束。例如，让我们在 <code>posts</code> 表上定义一个引用 <code>users</code> 表的 <code>id</code> 字段的 <code>user_id</code> 字段：</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;unsignedBigInteger(&#39;user_id&#39;);

    $table-&gt;foreign(&#39;user_id&#39;)-&gt;references(&#39;id&#39;)-&gt;on(&#39;users&#39;);
});
</code></pre><p>由于这种外键约束的定义方式过于繁复，Laravel 额外提供了更简洁的方法，基于约定来提供更好的开发人员体验。当使用 <code>foreignId</code> 方法来创建列时，上面的示例还可以这么写：</p><pre><code>Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;foreignId(&#39;user_id&#39;)-&gt;constrained();
});
</code></pre><p><code>foreignId</code> 方法是 <code>unsignedBigInteger</code> 的别名，而 <code>constrained</code> 方法将使用约定来确定所引用的表名和列名。如果表名与约定不匹配，可以通过将表名作为参数传递给 <code>constrained</code> 方法来指定表名：</p><pre><code>Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;foreignId(&#39;user_id&#39;)-&gt;constrained(&#39;users&#39;);
});
</code></pre><p>你可以为约束的「on delete」和「on update」属性指定所需的操作：</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;)
      -&gt;constrained()
      -&gt;onUpdate(&#39;cascade&#39;)
      -&gt;onDelete(&#39;cascade&#39;);
</code></pre><p>还为这些操作提供了另一种表达性语法：</p><table><thead><tr><th>方法</th><th>说明</th></tr></thead><tbody><tr><td><code>$table-&gt;cascadeOnUpdate();</code></td><td>更新应该级联</td></tr><tr><td><code>$table-&gt;restrictOnUpdate();</code></td><td>应该限制更新</td></tr><tr><td><code>$table-&gt;cascadeOnDelete();</code></td><td>删除应该级联</td></tr><tr><td><code>$table-&gt;restrictOnDelete();</code></td><td>应该限制删除</td></tr><tr><td><code>$table-&gt;nullOnDelete();</code></td><td>删除应将外键值设置为空</td></tr></tbody></table><p>当使用任意 <a href="#column-modifiers">字段修饰符</a> 的时候，必须在调用 <code>constrained</code> 之前调用：</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;)
      -&gt;nullable()
      -&gt;constrained();
</code></pre><p><a name="dropping-foreign-keys"></a></p><h4 id="删除外键" tabindex="-1"><a class="header-anchor" href="#删除外键" aria-hidden="true">#</a> 删除外键</h4><p>要删除一个外键，你需要使用 <code>dropForeign</code> 方法，将要删除的外键约束作为参数传递。外键约束采用的命名方式与索引相同。即，将数据表名称和约束的字段连接起来，再加上 <code>_foreign</code> 后缀：</p><pre><code>$table-&gt;dropForeign(&#39;posts_user_id_foreign&#39;);
</code></pre><p>或者，可以给 <code>dropForeign</code> 方法传递一个数组，该数组包含要删除的外键的列名。数组将根据 Laravel 的 结构生成器使用的约束名称约定自动转换：</p><pre><code>$table-&gt;dropForeign([&#39;user_id&#39;]);
</code></pre><p><a name="toggling-foreign-key-constraints"></a></p><h4 id="更改外键约束" tabindex="-1"><a class="header-anchor" href="#更改外键约束" aria-hidden="true">#</a> 更改外键约束</h4><p>你可以在迁移文件中使用以下方法来开启或关闭外键约束：</p><pre><code>Schema::enableForeignKeyConstraints();

Schema::disableForeignKeyConstraints();

Schema::withoutForeignKeyConstraints(function () {
    // 闭包中禁用的约束…
});
</code></pre>`,69),g=e("a",{href:"./database#configuration"},"启用外键支持",-1),b={href:"https://www.sqlite.org/omitted.html",target:"_blank",rel:"noopener noreferrer"},f=d('<p><a name="事件"></a></p><h2 id="事件" tabindex="-1"><a class="header-anchor" href="#事件" aria-hidden="true">#</a> 事件</h2><p>为方便起见，每个迁移操作都会派发一个 <a href="./events">事件</a>。以下所有事件都扩展了基础 <code>Illuminate\\Database\\Events\\MigrationEvent</code> 类：</p><table><thead><tr><th>类</th><th>描述</th></tr></thead><tbody><tr><td><code>Illuminate\\Database\\Events\\MigrationsStarted</code></td><td>即将执行一批迁移。</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationsEnded</code></td><td>一批迁移已完成执行。</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationStarted</code></td><td>即将执行单个迁移。</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationEnded</code></td><td>单个迁移已完成执行。</td></tr><tr><td><code>Illuminate\\Database\\Events\\SchemaDumped</code></td><td>数据库结构转储已完成。</td></tr><tr><td><code>Illuminate\\Database\\Events\\SchemaLoaded</code></td><td>已加载现有数据库结构转储。</td></tr></tbody></table>',4);function I(v,x){const a=n("ExternalLinkIcon");return c(),i("div",null,[s,h,m,p,u,e("blockquote",null,[e("p",null,[o("注意：SQLite 默认禁用外键约束。使用 SQLite 时，请确保在数据库配置中"),g,o(" 然后再尝试在迁移中创建它们。另外，SQLite 只在创建表时支持外键，并且"),e("a",b,[o("将在修改表时不会支持"),r(a)]),o("。")])]),f])}const T=t(l,[["render",I],["__file","migrations.html.vue"]]);export{T as default};
