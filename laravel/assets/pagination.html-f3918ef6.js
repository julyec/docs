import{_ as i}from"./plugin-vue_export-helper-c27b6911.js";import{r,o as s,c as d,b as t,d as e,e as o,a}from"./app-8cdff07c.js";const l={},p=a('<h1 id="database-pagination" tabindex="-1"><a class="header-anchor" href="#database-pagination" aria-hidden="true">#</a> Database: Pagination</h1><ul><li><a href="#introduction">Introduction</a></li><li><a href="#basic-usage">Basic Usage</a><ul><li><a href="#paginating-query-builder-results">Paginating Query Builder Results</a></li><li><a href="#paginating-eloquent-results">Paginating Eloquent Results</a></li><li><a href="#cursor-pagination">Cursor Pagination</a></li><li><a href="#manually-creating-a-paginator">Manually Creating A Paginator</a></li><li><a href="#customizing-pagination-urls">Customizing Pagination URLs</a></li></ul></li><li><a href="#displaying-pagination-results">Displaying Pagination Results</a><ul><li><a href="#adjusting-the-pagination-link-window">Adjusting The Pagination Link Window</a></li><li><a href="#converting-results-to-json">Converting Results To JSON</a></li></ul></li><li><a href="#customizing-the-pagination-view">Customizing The Pagination View</a><ul><li><a href="#using-bootstrap">Using Bootstrap</a></li></ul></li><li><a href="#paginator-instance-methods">Paginator and LengthAwarePaginator Instance Methods</a></li><li><a href="#cursor-paginator-instance-methods">Cursor Paginator Instance Methods</a></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2><p>In other frameworks, pagination can be very painful. We hope Laravel&#39;s approach to pagination will be a breath of fresh air. Laravel&#39;s paginator is integrated with the <a href="./queries">query builder</a> and <a href="./eloquent">Eloquent ORM</a> and provides convenient, easy-to-use pagination of database records with zero configuration.</p>',5),c={href:"https://tailwindcss.com/",target:"_blank",rel:"noopener noreferrer"},u=a(`<p><a name="tailwind-jit"></a></p><h4 id="tailwind-jit" tabindex="-1"><a class="header-anchor" href="#tailwind-jit" aria-hidden="true">#</a> Tailwind JIT</h4><p>If you are using Laravel&#39;s default Tailwind pagination views and the Tailwind JIT engine, you should ensure your application&#39;s <code>tailwind.config.js</code> file&#39;s <code>content</code> key references Laravel&#39;s pagination views so that their Tailwind classes are not purged:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code><span class="token literal-property property">content</span><span class="token operator">:</span> <span class="token punctuation">[</span>
    <span class="token string">&#39;./resources/**/*.blade.php&#39;</span><span class="token punctuation">,</span>
    <span class="token string">&#39;./resources/**/*.js&#39;</span><span class="token punctuation">,</span>
    <span class="token string">&#39;./resources/**/*.vue&#39;</span><span class="token punctuation">,</span>
    <span class="token string">&#39;./vendor/laravel/framework/src/Illuminate/Pagination/resources/views/*.blade.php&#39;</span><span class="token punctuation">,</span>
<span class="token punctuation">]</span><span class="token punctuation">,</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="basic-usage"></a></p><h2 id="basic-usage" tabindex="-1"><a class="header-anchor" href="#basic-usage" aria-hidden="true">#</a> Basic Usage</h2><p><a name="paginating-query-builder-results"></a></p><h3 id="paginating-query-builder-results" tabindex="-1"><a class="header-anchor" href="#paginating-query-builder-results" aria-hidden="true">#</a> Paginating Query Builder Results</h3><p>There are several ways to paginate items. The simplest is by using the <code>paginate</code> method on the <a href="./queries">query builder</a> or an <a href="./eloquent">Eloquent query</a>. The <code>paginate</code> method automatically takes care of setting the query&#39;s &quot;limit&quot; and &quot;offset&quot; based on the current page being viewed by the user. By default, the current page is detected by the value of the <code>page</code> query string argument on the HTTP request. This value is automatically detected by Laravel, and is also automatically inserted into links generated by the paginator.</p><p>In this example, the only argument passed to the <code>paginate</code> method is the number of items you would like displayed &quot;per page&quot;. In this case, let&#39;s specify that we would like to display <code>15</code> items per page:</p><pre><code>&lt;?php

namespace App\\Http\\Controllers;

use App\\Http\\Controllers\\Controller;
use Illuminate\\Support\\Facades\\DB;
use Illuminate\\View\\View;

class UserController extends Controller
{
    /**
     * Show all application users.
     */
    public function index(): View
    {
        return view(&#39;user.index&#39;, [
            &#39;users&#39; =&gt; DB::table(&#39;users&#39;)-&gt;paginate(15)
        ]);
    }
}
</code></pre><p><a name="simple-pagination"></a></p><h4 id="simple-pagination" tabindex="-1"><a class="header-anchor" href="#simple-pagination" aria-hidden="true">#</a> Simple Pagination</h4><p>The <code>paginate</code> method counts the total number of records matched by the query before retrieving the records from the database. This is done so that the paginator knows how many pages of records there are in total. However, if you do not plan to show the total number of pages in your application&#39;s UI then the record count query is unnecessary.</p><p>Therefore, if you only need to display simple &quot;Next&quot; and &quot;Previous&quot; links in your application&#39;s UI, you may use the <code>simplePaginate</code> method to perform a single, efficient query:</p><pre><code>$users = DB::table(&#39;users&#39;)-&gt;simplePaginate(15);
</code></pre><p><a name="paginating-eloquent-results"></a></p><h3 id="paginating-eloquent-results" tabindex="-1"><a class="header-anchor" href="#paginating-eloquent-results" aria-hidden="true">#</a> Paginating Eloquent Results</h3><p>You may also paginate <a href="./eloquent">Eloquent</a> queries. In this example, we will paginate the <code>App\\Models\\User</code> model and indicate that we plan to display 15 records per page. As you can see, the syntax is nearly identical to paginating query builder results:</p><pre><code>use App\\Models\\User;

$users = User::paginate(15);
</code></pre><p>Of course, you may call the <code>paginate</code> method after setting other constraints on the query, such as <code>where</code> clauses:</p><pre><code>$users = User::where(&#39;votes&#39;, &#39;&gt;&#39;, 100)-&gt;paginate(15);
</code></pre><p>You may also use the <code>simplePaginate</code> method when paginating Eloquent models:</p><pre><code>$users = User::where(&#39;votes&#39;, &#39;&gt;&#39;, 100)-&gt;simplePaginate(15);
</code></pre><p>Similarly, you may use the <code>cursorPaginate</code> method to cursor paginate Eloquent models:</p><pre><code>$users = User::where(&#39;votes&#39;, &#39;&gt;&#39;, 100)-&gt;cursorPaginate(15);
</code></pre><p><a name="multiple-paginator-instances-per-page"></a></p><h4 id="multiple-paginator-instances-per-page" tabindex="-1"><a class="header-anchor" href="#multiple-paginator-instances-per-page" aria-hidden="true">#</a> Multiple Paginator Instances Per Page</h4><p>Sometimes you may need to render two separate paginators on a single screen that is rendered by your application. However, if both paginator instances use the <code>page</code> query string parameter to store the current page, the two paginator&#39;s will conflict. To resolve this conflict, you may pass the name of the query string parameter you wish to use to store the paginator&#39;s current page via the third argument provided to the <code>paginate</code>, <code>simplePaginate</code>, and <code>cursorPaginate</code> methods:</p><pre><code>use App\\Models\\User;

$users = User::where(&#39;votes&#39;, &#39;&gt;&#39;, 100)-&gt;paginate(
    $perPage = 15, $columns = [&#39;*&#39;], $pageName = &#39;users&#39;
);
</code></pre><p><a name="cursor-pagination"></a></p><h3 id="cursor-pagination" tabindex="-1"><a class="header-anchor" href="#cursor-pagination" aria-hidden="true">#</a> Cursor Pagination</h3><p>While <code>paginate</code> and <code>simplePaginate</code> create queries using the SQL &quot;offset&quot; clause, cursor pagination works by constructing &quot;where&quot; clauses that compare the values of the ordered columns contained in the query, providing the most efficient database performance available amongst all of Laravel&#39;s pagination methods. This method of pagination is particularly well-suited for large data-sets and &quot;infinite&quot; scrolling user interfaces.</p><p>Unlike offset based pagination, which includes a page number in the query string of the URLs generated by the paginator, cursor based pagination places a &quot;cursor&quot; string in the query string. The cursor is an encoded string containing the location that the next paginated query should start paginating and the direction that it should paginate:</p><div class="language-nothing line-numbers-mode" data-ext="nothing"><pre class="language-nothing"><code>http://localhost/users?cursor=eyJpZCI6MTUsIl9wb2ludHNUb05leHRJdGVtcyI6dHJ1ZX0
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You may create a cursor based paginator instance via the <code>cursorPaginate</code> method offered by the query builder. This method returns an instance of <code>Illuminate\\Pagination\\CursorPaginator</code>:</p><pre><code>$users = DB::table(&#39;users&#39;)-&gt;orderBy(&#39;id&#39;)-&gt;cursorPaginate(15);
</code></pre><p>Once you have retrieved a cursor paginator instance, you may <a href="#displaying-pagination-results">display the pagination results</a> as you typically would when using the <code>paginate</code> and <code>simplePaginate</code> methods. For more information on the instance methods offered by the cursor paginator, please consult the <a href="#cursor-paginator-instance-methods">cursor paginator instance method documentation</a>.</p><blockquote><p><strong>Warning</strong><br> Your query must contain an &quot;order by&quot; clause in order to take advantage of cursor pagination.</p></blockquote><p><a name="cursor-vs-offset-pagination"></a></p><h4 id="cursor-vs-offset-pagination" tabindex="-1"><a class="header-anchor" href="#cursor-vs-offset-pagination" aria-hidden="true">#</a> Cursor vs. Offset Pagination</h4><p>To illustrate the differences between offset pagination and cursor pagination, let&#39;s examine some example SQL queries. Both of the following queries will both display the &quot;second page&quot; of results for a <code>users</code> table ordered by <code>id</code>:</p><div class="language-sql line-numbers-mode" data-ext="sql"><pre class="language-sql"><code><span class="token comment"># Offset Pagination...</span>
<span class="token keyword">select</span> <span class="token operator">*</span> <span class="token keyword">from</span> users <span class="token keyword">order</span> <span class="token keyword">by</span> id <span class="token keyword">asc</span> <span class="token keyword">limit</span> <span class="token number">15</span> <span class="token keyword">offset</span> <span class="token number">15</span><span class="token punctuation">;</span>

<span class="token comment"># Cursor Pagination...</span>
<span class="token keyword">select</span> <span class="token operator">*</span> <span class="token keyword">from</span> users <span class="token keyword">where</span> id <span class="token operator">&gt;</span> <span class="token number">15</span> <span class="token keyword">order</span> <span class="token keyword">by</span> id <span class="token keyword">asc</span> <span class="token keyword">limit</span> <span class="token number">15</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The cursor pagination query offers the following advantages over offset pagination:</p><ul><li>For large data-sets, cursor pagination will offer better performance if the &quot;order by&quot; columns are indexed. This is because the &quot;offset&quot; clause scans through all previously matched data.</li><li>For data-sets with frequent writes, offset pagination may skip records or show duplicates if results have been recently added to or deleted from the page a user is currently viewing.</li></ul><p>However, cursor pagination has the following limitations:</p><ul><li>Like <code>simplePaginate</code>, cursor pagination can only be used to display &quot;Next&quot; and &quot;Previous&quot; links and does not support generating links with page numbers.</li><li>It requires that the ordering is based on at least one unique column or a combination of columns that are unique. Columns with <code>null</code> values are not supported.</li><li>Query expressions in &quot;order by&quot; clauses are supported only if they are aliased and added to the &quot;select&quot; clause as well.</li><li>Query expressions with parameters are not supported.</li></ul><p><a name="manually-creating-a-paginator"></a></p><h3 id="manually-creating-a-paginator" tabindex="-1"><a class="header-anchor" href="#manually-creating-a-paginator" aria-hidden="true">#</a> Manually Creating A Paginator</h3><p>Sometimes you may wish to create a pagination instance manually, passing it an array of items that you already have in memory. You may do so by creating either an <code>Illuminate\\Pagination\\Paginator</code>, <code>Illuminate\\Pagination\\LengthAwarePaginator</code> or <code>Illuminate\\Pagination\\CursorPaginator</code> instance, depending on your needs.</p><p>The <code>Paginator</code> and <code>CursorPaginator</code> classes do not need to know the total number of items in the result set; however, because of this, these classes do not have methods for retrieving the index of the last page. The <code>LengthAwarePaginator</code> accepts almost the same arguments as the <code>Paginator</code>; however, it requires a count of the total number of items in the result set.</p><p>In other words, the <code>Paginator</code> corresponds to the <code>simplePaginate</code> method on the query builder, the <code>CursorPaginator</code> corresponds to the <code>cursorPaginate</code> method, and the <code>LengthAwarePaginator</code> corresponds to the <code>paginate</code> method.</p>`,52),h=t("strong",null,"Warning",-1),g=t("br",null,null,-1),m={href:"https://secure.php.net/manual/en/function.array-slice.php",target:"_blank",rel:"noopener noreferrer"},f=a(`<p><a name="customizing-pagination-urls"></a></p><h3 id="customizing-pagination-urls" tabindex="-1"><a class="header-anchor" href="#customizing-pagination-urls" aria-hidden="true">#</a> Customizing Pagination URLs</h3><p>By default, links generated by the paginator will match the current request&#39;s URI. However, the paginator&#39;s <code>withPath</code> method allows you to customize the URI used by the paginator when generating links. For example, if you want the paginator to generate links like <code>http://example.com/admin/users?page=N</code>, you should pass <code>/admin/users</code> to the <code>withPath</code> method:</p><pre><code>use App\\Models\\User;

Route::get(&#39;/users&#39;, function () {
    $users = User::paginate(15);

    $users-&gt;withPath(&#39;/admin/users&#39;);

    // ...
});
</code></pre><p><a name="appending-query-string-values"></a></p><h4 id="appending-query-string-values" tabindex="-1"><a class="header-anchor" href="#appending-query-string-values" aria-hidden="true">#</a> Appending Query String Values</h4><p>You may append to the query string of pagination links using the <code>appends</code> method. For example, to append <code>sort=votes</code> to each pagination link, you should make the following call to <code>appends</code>:</p><pre><code>use App\\Models\\User;

Route::get(&#39;/users&#39;, function () {
    $users = User::paginate(15);

    $users-&gt;appends([&#39;sort&#39; =&gt; &#39;votes&#39;]);

    // ...
});
</code></pre><p>You may use the <code>withQueryString</code> method if you would like to append all of the current request&#39;s query string values to the pagination links:</p><pre><code>$users = User::paginate(15)-&gt;withQueryString();
</code></pre><p><a name="appending-hash-fragments"></a></p><h4 id="appending-hash-fragments" tabindex="-1"><a class="header-anchor" href="#appending-hash-fragments" aria-hidden="true">#</a> Appending Hash Fragments</h4><p>If you need to append a &quot;hash fragment&quot; to URLs generated by the paginator, you may use the <code>fragment</code> method. For example, to append <code>#users</code> to the end of each pagination link, you should invoke the <code>fragment</code> method like so:</p><pre><code>$users = User::paginate(15)-&gt;fragment(&#39;users&#39;);
</code></pre><p><a name="displaying-pagination-results"></a></p><h2 id="displaying-pagination-results" tabindex="-1"><a class="header-anchor" href="#displaying-pagination-results" aria-hidden="true">#</a> Displaying Pagination Results</h2><p>When calling the <code>paginate</code> method, you will receive an instance of <code>Illuminate\\Pagination\\LengthAwarePaginator</code>, while calling the <code>simplePaginate</code> method returns an instance of <code>Illuminate\\Pagination\\Paginator</code>. And, finally, calling the <code>cursorPaginate</code> method returns an instance of <code>Illuminate\\Pagination\\CursorPaginator</code>.</p><p>These objects provide several methods that describe the result set. In addition to these helper methods, the paginator instances are iterators and may be looped as an array. So, once you have retrieved the results, you may display the results and render the page links using <a href="./blade">Blade</a>:</p><div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>&lt;div class=&quot;container&quot;&gt;
    @foreach ($users as $user)
        {{ $user-&gt;name }}
    @endforeach
&lt;/div&gt;

{{ $users-&gt;links() }}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div>`,19),v=t("code",null,"links",-1),y=t("code",null,"page",-1),b=t("code",null,"links",-1),w={href:"https://tailwindcss.com",target:"_blank",rel:"noopener noreferrer"},q=a(`<p><a name="adjusting-the-pagination-link-window"></a></p><h3 id="adjusting-the-pagination-link-window" tabindex="-1"><a class="header-anchor" href="#adjusting-the-pagination-link-window" aria-hidden="true">#</a> Adjusting The Pagination Link Window</h3><p>When the paginator displays pagination links, the current page number is displayed as well as links for the three pages before and after the current page. Using the <code>onEachSide</code> method, you may control how many additional links are displayed on each side of the current page within the middle, sliding window of links generated by the paginator:</p><div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>{{ $users-&gt;onEachSide(5)-&gt;links() }}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="converting-results-to-json"></a></p><h3 id="converting-results-to-json" tabindex="-1"><a class="header-anchor" href="#converting-results-to-json" aria-hidden="true">#</a> Converting Results To JSON</h3><p>The Laravel paginator classes implement the <code>Illuminate\\Contracts\\Support\\Jsonable</code> Interface contract and expose the <code>toJson</code> method, so it&#39;s very easy to convert your pagination results to JSON. You may also convert a paginator instance to JSON by returning it from a route or controller action:</p><pre><code>use App\\Models\\User;

Route::get(&#39;/users&#39;, function () {
    return User::paginate();
});
</code></pre><p>The JSON from the paginator will include meta information such as <code>total</code>, <code>current_page</code>, <code>last_page</code>, and more. The result records are available via the <code>data</code> key in the JSON array. Here is an example of the JSON created by returning a paginator instance from a route:</p><pre><code>{
   &quot;total&quot;: 50,
   &quot;per_page&quot;: 15,
   &quot;current_page&quot;: 1,
   &quot;last_page&quot;: 4,
   &quot;first_page_url&quot;: &quot;http://laravel.app?page=1&quot;,
   &quot;last_page_url&quot;: &quot;http://laravel.app?page=4&quot;,
   &quot;next_page_url&quot;: &quot;http://laravel.app?page=2&quot;,
   &quot;prev_page_url&quot;: null,
   &quot;path&quot;: &quot;http://laravel.app&quot;,
   &quot;from&quot;: 1,
   &quot;to&quot;: 15,
   &quot;data&quot;:[
        {
            // Record...
        },
        {
            // Record...
        }
   ]
}
</code></pre><p><a name="customizing-the-pagination-view"></a></p><h2 id="customizing-the-pagination-view" tabindex="-1"><a class="header-anchor" href="#customizing-the-pagination-view" aria-hidden="true">#</a> Customizing The Pagination View</h2>`,12),k={href:"https://tailwindcss.com",target:"_blank",rel:"noopener noreferrer"},P=t("code",null,"links",-1),_=a(`<div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>{{ $paginator-&gt;links(&#39;view.name&#39;) }}

&lt;!-- Passing additional data to the view... --&gt;
{{ $paginator-&gt;links(&#39;view.name&#39;, [&#39;foo&#39; =&gt; &#39;bar&#39;]) }}
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>However, the easiest way to customize the pagination views is by exporting them to your <code>resources/views/vendor</code> directory using the <code>vendor:publish</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan vendor:publish <span class="token parameter variable">--tag</span><span class="token operator">=</span>laravel-pagination
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>This command will place the views in your application&#39;s <code>resources/views/vendor/pagination</code> directory. The <code>tailwind.blade.php</code> file within this directory corresponds to the default pagination view. You may edit this file to modify the pagination HTML.</p><p>If you would like to designate a different file as the default pagination view, you may invoke the paginator&#39;s <code>defaultView</code> and <code>defaultSimpleView</code> methods within the <code>boot</code> method of your <code>App\\Providers\\AppServiceProvider</code> class:</p><pre><code>&lt;?php

namespace App\\Providers;

use Illuminate\\Pagination\\Paginator;
use Illuminate\\Support\\ServiceProvider;

class AppServiceProvider extends ServiceProvider
{
    /**
     * Bootstrap any application services.
     */
    public function boot(): void
    {
        Paginator::defaultView(&#39;view-name&#39;);

        Paginator::defaultSimpleView(&#39;view-name&#39;);
    }
}
</code></pre><p><a name="using-bootstrap"></a></p><h3 id="using-bootstrap" tabindex="-1"><a class="header-anchor" href="#using-bootstrap" aria-hidden="true">#</a> Using Bootstrap</h3>`,8),x={href:"https://getbootstrap.com/",target:"_blank",rel:"noopener noreferrer"},$=t("code",null,"useBootstrapFour",-1),T=t("code",null,"useBootstrapFive",-1),I=t("code",null,"boot",-1),U=t("code",null,"App\\Providers\\AppServiceProvider",-1),S=a(`<pre><code>use Illuminate\\Pagination\\Paginator;

/**
 * Bootstrap any application services.
 */
public function boot(): void
{
    Paginator::useBootstrapFive();
    Paginator::useBootstrapFour();
}
</code></pre><p><a name="paginator-instance-methods"></a></p><h2 id="paginator-lengthawarepaginator-instance-methods" tabindex="-1"><a class="header-anchor" href="#paginator-lengthawarepaginator-instance-methods" aria-hidden="true">#</a> Paginator / LengthAwarePaginator Instance Methods</h2><p>Each paginator instance provides additional pagination information via the following methods:</p><table><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>$paginator-&gt;count()</code></td><td>Get the number of items for the current page.</td></tr><tr><td><code>$paginator-&gt;currentPage()</code></td><td>Get the current page number.</td></tr><tr><td><code>$paginator-&gt;firstItem()</code></td><td>Get the result number of the first item in the results.</td></tr><tr><td><code>$paginator-&gt;getOptions()</code></td><td>Get the paginator options.</td></tr><tr><td><code>$paginator-&gt;getUrlRange($start, $end)</code></td><td>Create a range of pagination URLs.</td></tr><tr><td><code>$paginator-&gt;hasPages()</code></td><td>Determine if there are enough items to split into multiple pages.</td></tr><tr><td><code>$paginator-&gt;hasMorePages()</code></td><td>Determine if there are more items in the data store.</td></tr><tr><td><code>$paginator-&gt;items()</code></td><td>Get the items for the current page.</td></tr><tr><td><code>$paginator-&gt;lastItem()</code></td><td>Get the result number of the last item in the results.</td></tr><tr><td><code>$paginator-&gt;lastPage()</code></td><td>Get the page number of the last available page. (Not available when using <code>simplePaginate</code>).</td></tr><tr><td><code>$paginator-&gt;nextPageUrl()</code></td><td>Get the URL for the next page.</td></tr><tr><td><code>$paginator-&gt;onFirstPage()</code></td><td>Determine if the paginator is on the first page.</td></tr><tr><td><code>$paginator-&gt;perPage()</code></td><td>The number of items to be shown per page.</td></tr><tr><td><code>$paginator-&gt;previousPageUrl()</code></td><td>Get the URL for the previous page.</td></tr><tr><td><code>$paginator-&gt;total()</code></td><td>Determine the total number of matching items in the data store. (Not available when using <code>simplePaginate</code>).</td></tr><tr><td><code>$paginator-&gt;url($page)</code></td><td>Get the URL for a given page number.</td></tr><tr><td><code>$paginator-&gt;getPageName()</code></td><td>Get the query string variable used to store the page.</td></tr><tr><td><code>$paginator-&gt;setPageName($name)</code></td><td>Set the query string variable used to store the page.</td></tr><tr><td><code>$paginator-&gt;through($callback)</code></td><td>Transform each item using a callback.</td></tr></tbody></table><p><a name="cursor-paginator-instance-methods"></a></p><h2 id="cursor-paginator-instance-methods" tabindex="-1"><a class="header-anchor" href="#cursor-paginator-instance-methods" aria-hidden="true">#</a> Cursor Paginator Instance Methods</h2><p>Each cursor paginator instance provides additional pagination information via the following methods:</p><table><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>$paginator-&gt;count()</code></td><td>Get the number of items for the current page.</td></tr><tr><td><code>$paginator-&gt;cursor()</code></td><td>Get the current cursor instance.</td></tr><tr><td><code>$paginator-&gt;getOptions()</code></td><td>Get the paginator options.</td></tr><tr><td><code>$paginator-&gt;hasPages()</code></td><td>Determine if there are enough items to split into multiple pages.</td></tr><tr><td><code>$paginator-&gt;hasMorePages()</code></td><td>Determine if there are more items in the data store.</td></tr><tr><td><code>$paginator-&gt;getCursorName()</code></td><td>Get the query string variable used to store the cursor.</td></tr><tr><td><code>$paginator-&gt;items()</code></td><td>Get the items for the current page.</td></tr><tr><td><code>$paginator-&gt;nextCursor()</code></td><td>Get the cursor instance for the next set of items.</td></tr><tr><td><code>$paginator-&gt;nextPageUrl()</code></td><td>Get the URL for the next page.</td></tr><tr><td><code>$paginator-&gt;onFirstPage()</code></td><td>Determine if the paginator is on the first page.</td></tr><tr><td><code>$paginator-&gt;onLastPage()</code></td><td>Determine if the paginator is on the last page.</td></tr><tr><td><code>$paginator-&gt;perPage()</code></td><td>The number of items to be shown per page.</td></tr><tr><td><code>$paginator-&gt;previousCursor()</code></td><td>Get the cursor instance for the previous set of items.</td></tr><tr><td><code>$paginator-&gt;previousPageUrl()</code></td><td>Get the URL for the previous page.</td></tr><tr><td><code>$paginator-&gt;setCursorName()</code></td><td>Set the query string variable used to store the cursor.</td></tr><tr><td><code>$paginator-&gt;url($cursor)</code></td><td>Get the URL for a given cursor instance.</td></tr></tbody></table>`,9);function C(L,R){const n=r("ExternalLinkIcon");return s(),d("div",null,[p,t("p",null,[e("By default, the HTML generated by the paginator is compatible with the "),t("a",c,[e("Tailwind CSS framework"),o(n)]),e("; however, Bootstrap pagination support is also available.")]),u,t("blockquote",null,[t("p",null,[h,g,e(` When manually creating a paginator instance, you should manually "slice" the array of results you pass to the paginator. If you're unsure how to do this, check out the `),t("a",m,[e("array_slice"),o(n)]),e(" PHP function.")])]),f,t("p",null,[e("The "),v,e(" method will render the links to the rest of the pages in the result set. Each of these links will already contain the proper "),y,e(" query string variable. Remember, the HTML generated by the "),b,e(" method is compatible with the "),t("a",w,[e("Tailwind CSS framework"),o(n)]),e(".")]),q,t("p",null,[e("By default, the views rendered to display the pagination links are compatible with the "),t("a",k,[e("Tailwind CSS"),o(n)]),e(" framework. However, if you are not using Tailwind, you are free to define your own views to render these links. When calling the "),P,e(" method on a paginator instance, you may pass the view name as the first argument to the method:")]),_,t("p",null,[e("Laravel includes pagination views built using "),t("a",x,[e("Bootstrap CSS"),o(n)]),e(". To use these views instead of the default Tailwind views, you may call the paginator's "),$,e(" or "),T,e(" methods within the "),I,e(" method of your "),U,e(" class:")]),S])}const G=i(l,[["render",C],["__file","pagination.html.vue"]]);export{G as default};
