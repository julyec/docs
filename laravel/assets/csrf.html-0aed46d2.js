import{_ as s}from"./plugin-vue_export-helper-c27b6911.js";import{r as i,o as r,c,b as t,d as e,e as o,a}from"./app-8cdff07c.js";const d={},l=a('<h1 id="csrf-protection" tabindex="-1"><a class="header-anchor" href="#csrf-protection" aria-hidden="true">#</a> CSRF Protection</h1><ul><li><a href="#csrf-introduction">Introduction</a></li><li><a href="#preventing-csrf-requests">Preventing CSRF Requests</a><ul><li><a href="#csrf-excluding-uris">Excluding URIs</a></li></ul></li><li><a href="#csrf-x-csrf-token">X-CSRF-Token</a></li><li><a href="#csrf-x-xsrf-token">X-XSRF-Token</a></li></ul><p><a name="csrf-introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2>',4),u={href:"https://en.wikipedia.org/wiki/Cross-site_request_forgery",target:"_blank",rel:"noopener noreferrer"},p=a(`<p><a name="csrf-explanation"></a></p><h4 id="an-explanation-of-the-vulnerability" tabindex="-1"><a class="header-anchor" href="#an-explanation-of-the-vulnerability" aria-hidden="true">#</a> An Explanation Of The Vulnerability</h4><p>In case you&#39;re not familiar with cross-site request forgeries, let&#39;s discuss an example of how this vulnerability can be exploited. Imagine your application has a <code>/user/email</code> route that accepts a <code>POST</code> request to change the authenticated user&#39;s email address. Most likely, this route expects an <code>email</code> input field to contain the email address the user would like to begin using.</p><p>Without CSRF protection, a malicious website could create an HTML form that points to your application&#39;s <code>/user/email</code> route and submits the malicious user&#39;s own email address:</p><div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>&lt;form action=&quot;https://your-application.com/user/email&quot; method=&quot;POST&quot;&gt;
    &lt;input type=&quot;email&quot; value=&quot;malicious-email@example.com&quot;&gt;
&lt;/form&gt;

&lt;script&gt;
    document.forms[0].submit();
&lt;/script&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>If the malicious website automatically submits the form when the page is loaded, the malicious user only needs to lure an unsuspecting user of your application to visit their website and their email address will be changed in your application.</p><p>To prevent this vulnerability, we need to inspect every incoming <code>POST</code>, <code>PUT</code>, <code>PATCH</code>, or <code>DELETE</code> request for a secret session value that the malicious application is unable to access.</p><p><a name="preventing-csrf-requests"></a></p><h2 id="preventing-csrf-requests" tabindex="-1"><a class="header-anchor" href="#preventing-csrf-requests" aria-hidden="true">#</a> Preventing CSRF Requests</h2><p>Laravel automatically generates a CSRF &quot;token&quot; for each active <a href="./session">user session</a> managed by the application. This token is used to verify that the authenticated user is the person actually making the requests to the application. Since this token is stored in the user&#39;s session and changes each time the session is regenerated, a malicious application is unable to access it.</p><p>The current session&#39;s CSRF token can be accessed via the request&#39;s session or via the <code>csrf_token</code> helper function:</p><pre><code>use Illuminate\\Http\\Request;

Route::get(&#39;/token&#39;, function (Request $request) {
    $token = $request-&gt;session()-&gt;token();

    $token = csrf_token();

    // ...
});
</code></pre><p>Anytime you define a &quot;POST&quot;, &quot;PUT&quot;, &quot;PATCH&quot;, or &quot;DELETE&quot; HTML form in your application, you should include a hidden CSRF <code>_token</code> field in the form so that the CSRF protection middleware can validate the request. For convenience, you may use the <code>@csrf</code> Blade directive to generate the hidden token input field:</p><div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>&lt;form method=&quot;POST&quot; action=&quot;/profile&quot;&gt;
    @csrf

    &lt;!-- Equivalent to... --&gt;
    &lt;input type=&quot;hidden&quot; name=&quot;_token&quot; value=&quot;{{ csrf_token() }}&quot; /&gt;
&lt;/form&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>The <code>App\\Http\\Middleware\\VerifyCsrfToken</code> <a href="./middleware">middleware</a>, which is included in the <code>web</code> middleware group by default, will automatically verify that the token in the request input matches the token stored in the session. When these two tokens match, we know that the authenticated user is the one initiating the request.</p><p><a name="csrf-tokens-and-spas"></a></p><h3 id="csrf-tokens-spas" tabindex="-1"><a class="header-anchor" href="#csrf-tokens-spas" aria-hidden="true">#</a> CSRF Tokens &amp; SPAs</h3><p>If you are building a SPA that is utilizing Laravel as an API backend, you should consult the <a href="./sanctum">Laravel Sanctum documentation</a> for information on authenticating with your API and protecting against CSRF vulnerabilities.</p><p><a name="csrf-excluding-uris"></a></p><h3 id="excluding-uris-from-csrf-protection" tabindex="-1"><a class="header-anchor" href="#excluding-uris-from-csrf-protection" aria-hidden="true">#</a> Excluding URIs From CSRF Protection</h3>`,20),h={href:"https://stripe.com",target:"_blank",rel:"noopener noreferrer"},m=a(`<p>Typically, you should place these kinds of routes outside of the <code>web</code> middleware group that the <code>App\\Providers\\RouteServiceProvider</code> applies to all routes in the <code>routes/web.php</code> file. However, you may also exclude the routes by adding their URIs to the <code>$except</code> property of the <code>VerifyCsrfToken</code> middleware:</p><pre><code>&lt;?php

namespace App\\Http\\Middleware;

use Illuminate\\Foundation\\Http\\Middleware\\VerifyCsrfToken as Middleware;

class VerifyCsrfToken extends Middleware
{
    /**
     * The URIs that should be excluded from CSRF verification.
     *
     * @var array
     */
    protected $except = [
        &#39;stripe/*&#39;,
        &#39;http://example.com/foo/bar&#39;,
        &#39;http://example.com/foo/*&#39;,
    ];
}
</code></pre><blockquote><p><strong>Note</strong><br> For convenience, the CSRF middleware is automatically disabled for all routes when <a href="./testing">running tests</a>.</p></blockquote><p><a name="csrf-x-csrf-token"></a></p><h2 id="x-csrf-token" tabindex="-1"><a class="header-anchor" href="#x-csrf-token" aria-hidden="true">#</a> X-CSRF-TOKEN</h2><p>In addition to checking for the CSRF token as a POST parameter, the <code>App\\Http\\Middleware\\VerifyCsrfToken</code> middleware will also check for the <code>X-CSRF-TOKEN</code> request header. You could, for example, store the token in an HTML <code>meta</code> tag:</p><div class="language-blade line-numbers-mode" data-ext="blade"><pre class="language-blade"><code>&lt;meta name=&quot;csrf-token&quot; content=&quot;{{ csrf_token() }}&quot;&gt;
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Then, you can instruct a library like jQuery to automatically add the token to all request headers. This provides simple, convenient CSRF protection for your AJAX based applications using legacy JavaScript technology:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>$<span class="token punctuation">.</span><span class="token function">ajaxSetup</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
    <span class="token literal-property property">headers</span><span class="token operator">:</span> <span class="token punctuation">{</span>
        <span class="token string-property property">&#39;X-CSRF-TOKEN&#39;</span><span class="token operator">:</span> <span class="token function">$</span><span class="token punctuation">(</span><span class="token string">&#39;meta[name=&quot;csrf-token&quot;]&#39;</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">attr</span><span class="token punctuation">(</span><span class="token string">&#39;content&#39;</span><span class="token punctuation">)</span>
    <span class="token punctuation">}</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="csrf-x-xsrf-token"></a></p><h2 id="x-xsrf-token" tabindex="-1"><a class="header-anchor" href="#x-xsrf-token" aria-hidden="true">#</a> X-XSRF-TOKEN</h2><p>Laravel stores the current CSRF token in an encrypted <code>XSRF-TOKEN</code> cookie that is included with each response generated by the framework. You can use the cookie value to set the <code>X-XSRF-TOKEN</code> request header.</p><p>This cookie is primarily sent as a developer convenience since some JavaScript frameworks and libraries, like Angular and Axios, automatically place its value in the <code>X-XSRF-TOKEN</code> header on same-origin requests.</p><blockquote><p><strong>Note</strong><br> By default, the <code>resources/js/bootstrap.js</code> file includes the Axios HTTP library which will automatically send the <code>X-XSRF-TOKEN</code> header for you.</p></blockquote>`,14);function f(v,k){const n=i("ExternalLinkIcon");return r(),c("div",null,[l,t("p",null,[e("Cross-site request forgeries are a type of malicious exploit whereby unauthorized commands are performed on behalf of an authenticated user. Thankfully, Laravel makes it easy to protect your application from "),t("a",u,[e("cross-site request forgery"),o(n)]),e(" (CSRF) attacks.")]),p,t("p",null,[e("Sometimes you may wish to exclude a set of URIs from CSRF protection. For example, if you are using "),t("a",h,[e("Stripe"),o(n)]),e(" to process payments and are utilizing their webhook system, you will need to exclude your Stripe webhook handler route from CSRF protection since Stripe will not know what CSRF token to send to your routes.")]),m])}const y=s(d,[["render",f],["__file","csrf.html.vue"]]);export{y as default};
