import{_ as r}from"./plugin-vue_export-helper-c27b6911.js";import{r as i,o as d,c as s,b as e,d as t,e as a,a as n}from"./app-06635a3b.js";const c={},l=n('<h1 id="strings" tabindex="-1"><a class="header-anchor" href="#strings" aria-hidden="true">#</a> Strings</h1><ul><li><a href="#introduction">Introduction</a></li><li><a href="#available-methods">Available Methods</a></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2><p>Laravel includes a variety of functions for manipulating string values. Many of these functions are used by the framework itself; however, you are free to use them in your own applications if you find them convenient.</p><p><a name="available-methods"></a></p><h2 id="available-methods" tabindex="-1"><a class="header-anchor" href="#available-methods" aria-hidden="true">#</a> Available Methods</h2><p><a name="strings-method-list"></a></p><h3 id="strings-1" tabindex="-1"><a class="header-anchor" href="#strings-1" aria-hidden="true">#</a> Strings</h3>',9),h=e("div",{class:"collection-method-list",markdown:"1"},[e("p",null,[e("a",{href:"#method-__"},"__"),e("a",{href:"#method-class-basename"},"class_basename"),e("a",{href:"#method-e"},"e"),e("a",{href:"#method-preg-replace-array"},"preg_replace_array"),e("a",{href:"#method-str-after"},"Str::after"),e("a",{href:"#method-str-after-last"},"Str::afterLast"),e("a",{href:"#method-str-ascii"},"Str::ascii"),e("a",{href:"#method-str-before"},"Str::before"),e("a",{href:"#method-str-before-last"},"Str::beforeLast"),e("a",{href:"#method-str-between"},"Str::between"),e("a",{href:"#method-str-between-first"},"Str::betweenFirst"),e("a",{href:"#method-camel-case"},"Str::camel"),e("a",{href:"#method-str-contains"},"Str::contains"),e("a",{href:"#method-str-contains-all"},"Str::containsAll"),e("a",{href:"#method-ends-with"},"Str::endsWith"),e("a",{href:"#method-excerpt"},"Str::excerpt"),e("a",{href:"#method-str-finish"},"Str::finish"),e("a",{href:"#method-str-headline"},"Str::headline"),e("a",{href:"#method-str-inline-markdown"},"Str::inlineMarkdown"),e("a",{href:"#method-str-is"},"Str::is"),e("a",{href:"#method-str-is-ascii"},"Str::isAscii"),e("a",{href:"#method-str-is-json"},"Str::isJson"),e("a",{href:"#method-str-is-ulid"},"Str::isUlid"),e("a",{href:"#method-str-is-url"},"Str::isUrl"),e("a",{href:"#method-str-is-uuid"},"Str::isUuid"),e("a",{href:"#method-kebab-case"},"Str::kebab"),e("a",{href:"#method-str-lcfirst"},"Str::lcfirst"),e("a",{href:"#method-str-length"},"Str::length"),e("a",{href:"#method-str-limit"},"Str::limit"),e("a",{href:"#method-str-lower"},"Str::lower"),e("a",{href:"#method-str-markdown"},"Str::markdown"),e("a",{href:"#method-str-mask"},"Str::mask"),e("a",{href:"#method-str-ordered-uuid"},"Str::orderedUuid"),e("a",{href:"#method-str-padboth"},"Str::padBoth"),e("a",{href:"#method-str-padleft"},"Str::padLeft"),e("a",{href:"#method-str-padright"},"Str::padRight"),e("a",{href:"#method-str-password"},"Str::password"),e("a",{href:"#method-str-plural"},"Str::plural"),e("a",{href:"#method-str-plural-studly"},"Str::pluralStudly"),e("a",{href:"#method-str-position"},"Str::position"),e("a",{href:"#method-str-random"},"Str::random"),e("a",{href:"#method-str-remove"},"Str::remove"),e("a",{href:"#method-str-repeat"},"Str::repeat"),e("a",{href:"#method-str-replace"},"Str::replace"),e("a",{href:"#method-str-replace-array"},"Str::replaceArray"),e("a",{href:"#method-str-replace-first"},"Str::replaceFirst"),e("a",{href:"#method-str-replace-last"},"Str::replaceLast"),e("a",{href:"#method-str-replace-matches"},"Str::replaceMatches"),e("a",{href:"#method-str-replace-start"},"Str::replaceStart"),e("a",{href:"#method-str-replace-end"},"Str::replaceEnd"),e("a",{href:"#method-str-reverse"},"Str::reverse"),e("a",{href:"#method-str-singular"},"Str::singular"),e("a",{href:"#method-str-slug"},"Str::slug"),e("a",{href:"#method-snake-case"},"Str::snake"),e("a",{href:"#method-str-squish"},"Str::squish"),e("a",{href:"#method-str-start"},"Str::start"),e("a",{href:"#method-starts-with"},"Str::startsWith"),e("a",{href:"#method-studly-case"},"Str::studly"),e("a",{href:"#method-str-substr"},"Str::substr"),e("a",{href:"#method-str-substrcount"},"Str::substrCount"),e("a",{href:"#method-str-substrreplace"},"Str::substrReplace"),e("a",{href:"#method-str-swap"},"Str::swap"),e("a",{href:"#method-take"},"Str::take"),e("a",{href:"#method-title-case"},"Str::title"),e("a",{href:"#method-str-to-html-string"},"Str::toHtmlString"),e("a",{href:"#method-str-ucfirst"},"Str::ucfirst"),e("a",{href:"#method-str-ucsplit"},"Str::ucsplit"),e("a",{href:"#method-str-upper"},"Str::upper"),e("a",{href:"#method-str-ulid"},"Str::ulid"),e("a",{href:"#method-str-uuid"},"Str::uuid"),e("a",{href:"#method-str-word-count"},"Str::wordCount"),e("a",{href:"#method-str-word-wrap"},"Str::wordWrap"),e("a",{href:"#method-str-words"},"Str::words"),e("a",{href:"#method-str-wrap"},"Str::wrap"),e("a",{href:"#method-str"},"str"),e("a",{href:"#method-trans"},"trans"),e("a",{href:"#method-trans-choice"},"trans_choice")])],-1),p=e("p",null,[e("a",{name:"fluent-strings-method-list"})],-1),u=e("h3",{id:"fluent-strings",tabindex:"-1"},[e("a",{class:"header-anchor",href:"#fluent-strings","aria-hidden":"true"},"#"),t(" Fluent Strings")],-1),m=e("div",{class:"collection-method-list",markdown:"1"},[e("p",null,[e("a",{href:"#method-fluent-str-after"},"after"),e("a",{href:"#method-fluent-str-after-last"},"afterLast"),e("a",{href:"#method-fluent-str-append"},"append"),e("a",{href:"#method-fluent-str-ascii"},"ascii"),e("a",{href:"#method-fluent-str-basename"},"basename"),e("a",{href:"#method-fluent-str-before"},"before"),e("a",{href:"#method-fluent-str-before-last"},"beforeLast"),e("a",{href:"#method-fluent-str-between"},"between"),e("a",{href:"#method-fluent-str-between-first"},"betweenFirst"),e("a",{href:"#method-fluent-str-camel"},"camel"),e("a",{href:"#method-fluent-str-class-basename"},"classBasename"),e("a",{href:"#method-fluent-str-contains"},"contains"),e("a",{href:"#method-fluent-str-contains-all"},"containsAll"),e("a",{href:"#method-fluent-str-dirname"},"dirname"),e("a",{href:"#method-fluent-str-ends-with"},"endsWith"),e("a",{href:"#method-fluent-str-excerpt"},"excerpt"),e("a",{href:"#method-fluent-str-exactly"},"exactly"),e("a",{href:"#method-fluent-str-explode"},"explode"),e("a",{href:"#method-fluent-str-finish"},"finish"),e("a",{href:"#method-fluent-str-headline"},"headline"),e("a",{href:"#method-fluent-str-inline-markdown"},"inlineMarkdown"),e("a",{href:"#method-fluent-str-is"},"is"),e("a",{href:"#method-fluent-str-is-ascii"},"isAscii"),e("a",{href:"#method-fluent-str-is-empty"},"isEmpty"),e("a",{href:"#method-fluent-str-is-not-empty"},"isNotEmpty"),e("a",{href:"#method-fluent-str-is-json"},"isJson"),e("a",{href:"#method-fluent-str-is-ulid"},"isUlid"),e("a",{href:"#method-fluent-str-is-url"},"isUrl"),e("a",{href:"#method-fluent-str-is-uuid"},"isUuid"),e("a",{href:"#method-fluent-str-kebab"},"kebab"),e("a",{href:"#method-fluent-str-lcfirst"},"lcfirst"),e("a",{href:"#method-fluent-str-length"},"length"),e("a",{href:"#method-fluent-str-limit"},"limit"),e("a",{href:"#method-fluent-str-lower"},"lower"),e("a",{href:"#method-fluent-str-ltrim"},"ltrim"),e("a",{href:"#method-fluent-str-markdown"},"markdown"),e("a",{href:"#method-fluent-str-mask"},"mask"),e("a",{href:"#method-fluent-str-match"},"match"),e("a",{href:"#method-fluent-str-match-all"},"matchAll"),e("a",{href:"#method-fluent-str-is-match"},"isMatch"),e("a",{href:"#method-fluent-str-new-line"},"newLine"),e("a",{href:"#method-fluent-str-padboth"},"padBoth"),e("a",{href:"#method-fluent-str-padleft"},"padLeft"),e("a",{href:"#method-fluent-str-padright"},"padRight"),e("a",{href:"#method-fluent-str-pipe"},"pipe"),e("a",{href:"#method-fluent-str-plural"},"plural"),e("a",{href:"#method-fluent-str-position"},"position"),e("a",{href:"#method-fluent-str-prepend"},"prepend"),e("a",{href:"#method-fluent-str-remove"},"remove"),e("a",{href:"#method-fluent-str-repeat"},"repeat"),e("a",{href:"#method-fluent-str-replace"},"replace"),e("a",{href:"#method-fluent-str-replace-array"},"replaceArray"),e("a",{href:"#method-fluent-str-replace-first"},"replaceFirst"),e("a",{href:"#method-fluent-str-replace-last"},"replaceLast"),e("a",{href:"#method-fluent-str-replace-matches"},"replaceMatches"),e("a",{href:"#method-fluent-str-replace-start"},"replaceStart"),e("a",{href:"#method-fluent-str-replace-end"},"replaceEnd"),e("a",{href:"#method-fluent-str-rtrim"},"rtrim"),e("a",{href:"#method-fluent-str-scan"},"scan"),e("a",{href:"#method-fluent-str-singular"},"singular"),e("a",{href:"#method-fluent-str-slug"},"slug"),e("a",{href:"#method-fluent-str-snake"},"snake"),e("a",{href:"#method-fluent-str-split"},"split"),e("a",{href:"#method-fluent-str-squish"},"squish"),e("a",{href:"#method-fluent-str-start"},"start"),e("a",{href:"#method-fluent-str-starts-with"},"startsWith"),e("a",{href:"#method-fluent-str-studly"},"studly"),e("a",{href:"#method-fluent-str-substr"},"substr"),e("a",{href:"#method-fluent-str-substrreplace"},"substrReplace"),e("a",{href:"#method-fluent-str-swap"},"swap"),e("a",{href:"#method-fluent-str-take"},"take"),e("a",{href:"#method-fluent-str-tap"},"tap"),e("a",{href:"#method-fluent-str-test"},"test"),e("a",{href:"#method-fluent-str-title"},"title"),e("a",{href:"#method-fluent-str-trim"},"trim"),e("a",{href:"#method-fluent-str-ucfirst"},"ucfirst"),e("a",{href:"#method-fluent-str-ucsplit"},"ucsplit"),e("a",{href:"#method-fluent-str-upper"},"upper"),e("a",{href:"#method-fluent-str-when"},"when"),e("a",{href:"#method-fluent-str-when-contains"},"whenContains"),e("a",{href:"#method-fluent-str-when-contains-all"},"whenContainsAll"),e("a",{href:"#method-fluent-str-when-empty"},"whenEmpty"),e("a",{href:"#method-fluent-str-when-not-empty"},"whenNotEmpty"),e("a",{href:"#method-fluent-str-when-starts-with"},"whenStartsWith"),e("a",{href:"#method-fluent-str-when-ends-with"},"whenEndsWith"),e("a",{href:"#method-fluent-str-when-exactly"},"whenExactly"),e("a",{href:"#method-fluent-str-when-not-exactly"},"whenNotExactly"),e("a",{href:"#method-fluent-str-when-is"},"whenIs"),e("a",{href:"#method-fluent-str-when-is-ascii"},"whenIsAscii"),e("a",{href:"#method-fluent-str-when-is-ulid"},"whenIsUlid"),e("a",{href:"#method-fluent-str-when-is-uuid"},"whenIsUuid"),e("a",{href:"#method-fluent-str-when-test"},"whenTest"),e("a",{href:"#method-fluent-str-word-count"},"wordCount"),e("a",{href:"#method-fluent-str-words"},"words")])],-1),f=n(`<p><a name="strings"></a></p><h2 id="strings-2" tabindex="-1"><a class="header-anchor" href="#strings-2" aria-hidden="true">#</a> Strings</h2><p><a name="method-__"></a></p><h4 id="collection-method" tabindex="-1"><a class="header-anchor" href="#collection-method" aria-hidden="true">#</a> <code>__()</code> {.collection-method}</h4><p>The <code>__</code> function translates the given translation string or translation key using your <a href="./localization">language files</a>:</p><pre><code>echo __(&#39;Welcome to our application&#39;);

echo __(&#39;messages.welcome&#39;);
</code></pre><p>If the specified translation string or key does not exist, the <code>__</code> function will return the given value. So, using the example above, the <code>__</code> function would return <code>messages.welcome</code> if that translation key does not exist.</p><p><a name="method-class-basename"></a></p><h4 id="class-basename-collection-method" tabindex="-1"><a class="header-anchor" href="#class-basename-collection-method" aria-hidden="true">#</a> <code>class_basename()</code> {.collection-method}</h4><p>The <code>class_basename</code> function returns the class name of the given class with the class&#39;s namespace removed:</p><pre><code>$class = class_basename(&#39;Foo\\Bar\\Baz&#39;);

// Baz
</code></pre><p><a name="method-e"></a></p><h4 id="e-collection-method" tabindex="-1"><a class="header-anchor" href="#e-collection-method" aria-hidden="true">#</a> <code>e()</code> {.collection-method}</h4><p>The <code>e</code> function runs PHP&#39;s <code>htmlspecialchars</code> function with the <code>double_encode</code> option set to <code>true</code> by default:</p><pre><code>echo e(&#39;&lt;html&gt;foo&lt;/html&gt;&#39;);

// &amp;lt;html&amp;gt;foo&amp;lt;/html&amp;gt;
</code></pre><p><a name="method-preg-replace-array"></a></p><h4 id="preg-replace-array-collection-method" tabindex="-1"><a class="header-anchor" href="#preg-replace-array-collection-method" aria-hidden="true">#</a> <code>preg_replace_array()</code> {.collection-method}</h4><p>The <code>preg_replace_array</code> function replaces a given pattern in the string sequentially using an array:</p><pre><code>$string = &#39;The event will take place between :start and :end&#39;;

$replaced = preg_replace_array(&#39;/:[a-z_]+/&#39;, [&#39;8:30&#39;, &#39;9:00&#39;], $string);

// The event will take place between 8:30 and 9:00
</code></pre><p><a name="method-str-after"></a></p><h4 id="str-after-collection-method" tabindex="-1"><a class="header-anchor" href="#str-after-collection-method" aria-hidden="true">#</a> <code>Str::after()</code> {.collection-method}</h4><p>The <code>Str::after</code> method returns everything after the given value in a string. The entire string will be returned if the value does not exist within the string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::after(&#39;This is my name&#39;, &#39;This is&#39;);

// &#39; my name&#39;
</code></pre><p><a name="method-str-after-last"></a></p><h4 id="str-afterlast-collection-method" tabindex="-1"><a class="header-anchor" href="#str-afterlast-collection-method" aria-hidden="true">#</a> <code>Str::afterLast()</code> {.collection-method}</h4><p>The <code>Str::afterLast</code> method returns everything after the last occurrence of the given value in a string. The entire string will be returned if the value does not exist within the string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::afterLast(&#39;App\\Http\\Controllers\\Controller&#39;, &#39;\\\\&#39;);

// &#39;Controller&#39;
</code></pre><p><a name="method-str-ascii"></a></p><h4 id="str-ascii-collection-method" tabindex="-1"><a class="header-anchor" href="#str-ascii-collection-method" aria-hidden="true">#</a> <code>Str::ascii()</code> {.collection-method}</h4><p>The <code>Str::ascii</code> method will attempt to transliterate the string into an ASCII value:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::ascii(&#39;û&#39;);

// &#39;u&#39;
</code></pre><p><a name="method-str-before"></a></p><h4 id="str-before-collection-method" tabindex="-1"><a class="header-anchor" href="#str-before-collection-method" aria-hidden="true">#</a> <code>Str::before()</code> {.collection-method}</h4><p>The <code>Str::before</code> method returns everything before the given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::before(&#39;This is my name&#39;, &#39;my name&#39;);

// &#39;This is &#39;
</code></pre><p><a name="method-str-before-last"></a></p><h4 id="str-beforelast-collection-method" tabindex="-1"><a class="header-anchor" href="#str-beforelast-collection-method" aria-hidden="true">#</a> <code>Str::beforeLast()</code> {.collection-method}</h4><p>The <code>Str::beforeLast</code> method returns everything before the last occurrence of the given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::beforeLast(&#39;This is my name&#39;, &#39;is&#39;);

// &#39;This &#39;
</code></pre><p><a name="method-str-between"></a></p><h4 id="str-between-collection-method" tabindex="-1"><a class="header-anchor" href="#str-between-collection-method" aria-hidden="true">#</a> <code>Str::between()</code> {.collection-method}</h4><p>The <code>Str::between</code> method returns the portion of a string between two values:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::between(&#39;This is my name&#39;, &#39;This&#39;, &#39;name&#39;);

// &#39; is my &#39;
</code></pre><p><a name="method-str-between-first"></a></p><h4 id="str-betweenfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#str-betweenfirst-collection-method" aria-hidden="true">#</a> <code>Str::betweenFirst()</code> {.collection-method}</h4><p>The <code>Str::betweenFirst</code> method returns the smallest possible portion of a string between two values:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::betweenFirst(&#39;[a] bc [d]&#39;, &#39;[&#39;, &#39;]&#39;);

// &#39;a&#39;
</code></pre><p><a name="method-camel-case"></a></p><h4 id="str-camel-collection-method" tabindex="-1"><a class="header-anchor" href="#str-camel-collection-method" aria-hidden="true">#</a> <code>Str::camel()</code> {.collection-method}</h4><p>The <code>Str::camel</code> method converts the given string to <code>camelCase</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::camel(&#39;foo_bar&#39;);

// fooBar
</code></pre><p><a name="method-str-contains"></a></p><h4 id="str-contains-collection-method" tabindex="-1"><a class="header-anchor" href="#str-contains-collection-method" aria-hidden="true">#</a> <code>Str::contains()</code> {.collection-method}</h4><p>The <code>Str::contains</code> method determines if the given string contains the given value. This method is case sensitive:</p><pre><code>use Illuminate\\Support\\Str;

$contains = Str::contains(&#39;This is my name&#39;, &#39;my&#39;);

// true
</code></pre><p>You may also pass an array of values to determine if the given string contains any of the values in the array:</p><pre><code>use Illuminate\\Support\\Str;

$contains = Str::contains(&#39;This is my name&#39;, [&#39;my&#39;, &#39;foo&#39;]);

// true
</code></pre><p><a name="method-str-contains-all"></a></p><h4 id="str-containsall-collection-method" tabindex="-1"><a class="header-anchor" href="#str-containsall-collection-method" aria-hidden="true">#</a> <code>Str::containsAll()</code> {.collection-method}</h4><p>The <code>Str::containsAll</code> method determines if the given string contains all of the values in a given array:</p><pre><code>use Illuminate\\Support\\Str;

$containsAll = Str::containsAll(&#39;This is my name&#39;, [&#39;my&#39;, &#39;name&#39;]);

// true
</code></pre><p><a name="method-ends-with"></a></p><h4 id="str-endswith-collection-method" tabindex="-1"><a class="header-anchor" href="#str-endswith-collection-method" aria-hidden="true">#</a> <code>Str::endsWith()</code> {.collection-method}</h4><p>The <code>Str::endsWith</code> method determines if the given string ends with the given value:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::endsWith(&#39;This is my name&#39;, &#39;name&#39;);

// true
</code></pre><p>You may also pass an array of values to determine if the given string ends with any of the values in the array:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::endsWith(&#39;This is my name&#39;, [&#39;name&#39;, &#39;foo&#39;]);

// true

$result = Str::endsWith(&#39;This is my name&#39;, [&#39;this&#39;, &#39;foo&#39;]);

// false
</code></pre><p><a name="method-excerpt"></a></p><h4 id="str-excerpt-collection-method" tabindex="-1"><a class="header-anchor" href="#str-excerpt-collection-method" aria-hidden="true">#</a> <code>Str::excerpt()</code> {.collection-method}</h4><p>The <code>Str::excerpt</code> method extracts an excerpt from a given string that matches the first instance of a phrase within that string:</p><pre><code>use Illuminate\\Support\\Str;

$excerpt = Str::excerpt(&#39;This is my name&#39;, &#39;my&#39;, [
    &#39;radius&#39; =&gt; 3
]);

// &#39;...is my na...&#39;
</code></pre><p>The <code>radius</code> option, which defaults to <code>100</code>, allows you to define the number of characters that should appear on each side of the truncated string.</p><p>In addition, you may use the <code>omission</code> option to define the string that will be prepended and appended to the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

$excerpt = Str::excerpt(&#39;This is my name&#39;, &#39;name&#39;, [
    &#39;radius&#39; =&gt; 3,
    &#39;omission&#39; =&gt; &#39;(...) &#39;
]);

// &#39;(...) my name&#39;
</code></pre><p><a name="method-str-finish"></a></p><h4 id="str-finish-collection-method" tabindex="-1"><a class="header-anchor" href="#str-finish-collection-method" aria-hidden="true">#</a> <code>Str::finish()</code> {.collection-method}</h4><p>The <code>Str::finish</code> method adds a single instance of the given value to a string if it does not already end with that value:</p><pre><code>use Illuminate\\Support\\Str;

$adjusted = Str::finish(&#39;this/string&#39;, &#39;/&#39;);

// this/string/

$adjusted = Str::finish(&#39;this/string/&#39;, &#39;/&#39;);

// this/string/
</code></pre><p><a name="method-str-headline"></a></p><h4 id="str-headline-collection-method" tabindex="-1"><a class="header-anchor" href="#str-headline-collection-method" aria-hidden="true">#</a> <code>Str::headline()</code> {.collection-method}</h4><p>The <code>Str::headline</code> method will convert strings delimited by casing, hyphens, or underscores into a space delimited string with each word&#39;s first letter capitalized:</p><pre><code>use Illuminate\\Support\\Str;

$headline = Str::headline(&#39;steve_jobs&#39;);

// Steve Jobs

$headline = Str::headline(&#39;EmailNotificationSent&#39;);

// Email Notification Sent
</code></pre><p><a name="method-str-inline-markdown"></a></p><h4 id="str-inlinemarkdown-collection-method" tabindex="-1"><a class="header-anchor" href="#str-inlinemarkdown-collection-method" aria-hidden="true">#</a> <code>Str::inlineMarkdown()</code> {.collection-method}</h4>`,84),g=e("code",null,"Str::inlineMarkdown",-1),S={href:"https://commonmark.thephpleague.com/",target:"_blank",rel:"noopener noreferrer"},b=e("code",null,"markdown",-1),v=n(`<pre><code>use Illuminate\\Support\\Str;

$html = Str::inlineMarkdown(&#39;**Laravel**&#39;);

// &lt;strong&gt;Laravel&lt;/strong&gt;
</code></pre><p><a name="method-str-is"></a></p><h4 id="str-is-collection-method" tabindex="-1"><a class="header-anchor" href="#str-is-collection-method" aria-hidden="true">#</a> <code>Str::is()</code> {.collection-method}</h4><p>The <code>Str::is</code> method determines if a given string matches a given pattern. Asterisks may be used as wildcard values:</p><pre><code>use Illuminate\\Support\\Str;

$matches = Str::is(&#39;foo*&#39;, &#39;foobar&#39;);

// true

$matches = Str::is(&#39;baz*&#39;, &#39;foobar&#39;);

// false
</code></pre><p><a name="method-str-is-ascii"></a></p><h4 id="str-isascii-collection-method" tabindex="-1"><a class="header-anchor" href="#str-isascii-collection-method" aria-hidden="true">#</a> <code>Str::isAscii()</code> {.collection-method}</h4><p>The <code>Str::isAscii</code> method determines if a given string is 7 bit ASCII:</p><pre><code>use Illuminate\\Support\\Str;

$isAscii = Str::isAscii(&#39;Taylor&#39;);

// true

$isAscii = Str::isAscii(&#39;ü&#39;);

// false
</code></pre><p><a name="method-str-is-json"></a></p><h4 id="str-isjson-collection-method" tabindex="-1"><a class="header-anchor" href="#str-isjson-collection-method" aria-hidden="true">#</a> <code>Str::isJson()</code> {.collection-method}</h4><p>The <code>Str::isJson</code> method determines if the given string is valid JSON:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::isJson(&#39;[1,2,3]&#39;);

// true

$result = Str::isJson(&#39;{&quot;first&quot;: &quot;John&quot;, &quot;last&quot;: &quot;Doe&quot;}&#39;);

// true

$result = Str::isJson(&#39;{first: &quot;John&quot;, last: &quot;Doe&quot;}&#39;);

// false
</code></pre><p><a name="method-str-is-url"></a></p><h4 id="str-isurl-collection-method" tabindex="-1"><a class="header-anchor" href="#str-isurl-collection-method" aria-hidden="true">#</a> <code>Str::isUrl()</code> {.collection-method}</h4><p>The <code>Str::isUrl</code> method determines if the given string is a valid URL:</p><pre><code>use Illuminate\\Support\\Str;

$isUrl = Str::isUrl(&#39;http://example.com&#39;);

// true

$isUrl = Str::isUrl(&#39;laravel&#39;);

// false
</code></pre><p><a name="method-str-is-ulid"></a></p><h4 id="str-isulid-collection-method" tabindex="-1"><a class="header-anchor" href="#str-isulid-collection-method" aria-hidden="true">#</a> <code>Str::isUlid()</code> {.collection-method}</h4><p>The <code>Str::isUlid</code> method determines if the given string is a valid ULID:</p><pre><code>use Illuminate\\Support\\Str;

$isUlid = Str::isUlid(&#39;01gd6r360bp37zj17nxb55yv40&#39;);

// true

$isUlid = Str::isUlid(&#39;laravel&#39;);

// false
</code></pre><p><a name="method-str-is-uuid"></a></p><h4 id="str-isuuid-collection-method" tabindex="-1"><a class="header-anchor" href="#str-isuuid-collection-method" aria-hidden="true">#</a> <code>Str::isUuid()</code> {.collection-method}</h4><p>The <code>Str::isUuid</code> method determines if the given string is a valid UUID:</p><pre><code>use Illuminate\\Support\\Str;

$isUuid = Str::isUuid(&#39;a0a2a2d2-0b87-4a18-83f2-2529882be2de&#39;);

// true

$isUuid = Str::isUuid(&#39;laravel&#39;);

// false
</code></pre><p><a name="method-kebab-case"></a></p><h4 id="str-kebab-collection-method" tabindex="-1"><a class="header-anchor" href="#str-kebab-collection-method" aria-hidden="true">#</a> <code>Str::kebab()</code> {.collection-method}</h4><p>The <code>Str::kebab</code> method converts the given string to <code>kebab-case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::kebab(&#39;fooBar&#39;);

// foo-bar
</code></pre><p><a name="method-str-lcfirst"></a></p><h4 id="str-lcfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#str-lcfirst-collection-method" aria-hidden="true">#</a> <code>Str::lcfirst()</code> {.collection-method}</h4><p>The <code>Str::lcfirst</code> method returns the given string with the first character lowercased:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::lcfirst(&#39;Foo Bar&#39;);

// foo Bar
</code></pre><p><a name="method-str-length"></a></p><h4 id="str-length-collection-method" tabindex="-1"><a class="header-anchor" href="#str-length-collection-method" aria-hidden="true">#</a> <code>Str::length()</code> {.collection-method}</h4><p>The <code>Str::length</code> method returns the length of the given string:</p><pre><code>use Illuminate\\Support\\Str;

$length = Str::length(&#39;Laravel&#39;);

// 7
</code></pre><p><a name="method-str-limit"></a></p><h4 id="str-limit-collection-method" tabindex="-1"><a class="header-anchor" href="#str-limit-collection-method" aria-hidden="true">#</a> <code>Str::limit()</code> {.collection-method}</h4><p>The <code>Str::limit</code> method truncates the given string to the specified length:</p><pre><code>use Illuminate\\Support\\Str;

$truncated = Str::limit(&#39;The quick brown fox jumps over the lazy dog&#39;, 20);

// The quick brown fox...
</code></pre><p>You may pass a third argument to the method to change the string that will be appended to the end of the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

$truncated = Str::limit(&#39;The quick brown fox jumps over the lazy dog&#39;, 20, &#39; (...)&#39;);

// The quick brown fox (...)
</code></pre><p><a name="method-str-lower"></a></p><h4 id="str-lower-collection-method" tabindex="-1"><a class="header-anchor" href="#str-lower-collection-method" aria-hidden="true">#</a> <code>Str::lower()</code> {.collection-method}</h4><p>The <code>Str::lower</code> method converts the given string to lowercase:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::lower(&#39;LARAVEL&#39;);

// laravel
</code></pre><p><a name="method-str-markdown"></a></p><h4 id="str-markdown-collection-method" tabindex="-1"><a class="header-anchor" href="#str-markdown-collection-method" aria-hidden="true">#</a> <code>Str::markdown()</code> {.collection-method}</h4>`,49),w=e("code",null,"Str::markdown",-1),y={href:"https://commonmark.thephpleague.com/",target:"_blank",rel:"noopener noreferrer"},k=n(`<pre><code>use Illuminate\\Support\\Str;

$html = Str::markdown(&#39;# Laravel&#39;);

// &lt;h1&gt;Laravel&lt;/h1&gt;

$html = Str::markdown(&#39;# Taylor &lt;b&gt;Otwell&lt;/b&gt;&#39;, [
    &#39;html_input&#39; =&gt; &#39;strip&#39;,
]);

// &lt;h1&gt;Taylor Otwell&lt;/h1&gt;
</code></pre><p><a name="method-str-mask"></a></p><h4 id="str-mask-collection-method" tabindex="-1"><a class="header-anchor" href="#str-mask-collection-method" aria-hidden="true">#</a> <code>Str::mask()</code> {.collection-method}</h4><p>The <code>Str::mask</code> method masks a portion of a string with a repeated character, and may be used to obfuscate segments of strings such as email addresses and phone numbers:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::mask(&#39;taylor@example.com&#39;, &#39;*&#39;, 3);

// tay***************
</code></pre><p>If needed, you provide a negative number as the third argument to the <code>mask</code> method, which will instruct the method to begin masking at the given distance from the end of the string:</p><pre><code>$string = Str::mask(&#39;taylor@example.com&#39;, &#39;*&#39;, -15, 3);

// tay***@example.com
</code></pre><p><a name="method-str-ordered-uuid"></a></p><h4 id="str-ordereduuid-collection-method" tabindex="-1"><a class="header-anchor" href="#str-ordereduuid-collection-method" aria-hidden="true">#</a> <code>Str::orderedUuid()</code> {.collection-method}</h4><p>The <code>Str::orderedUuid</code> method generates a &quot;timestamp first&quot; UUID that may be efficiently stored in an indexed database column. Each UUID that is generated using this method will be sorted after UUIDs previously generated using the method:</p><pre><code>use Illuminate\\Support\\Str;

return (string) Str::orderedUuid();
</code></pre><p><a name="method-str-padboth"></a></p><h4 id="str-padboth-collection-method" tabindex="-1"><a class="header-anchor" href="#str-padboth-collection-method" aria-hidden="true">#</a> <code>Str::padBoth()</code> {.collection-method}</h4><p>The <code>Str::padBoth</code> method wraps PHP&#39;s <code>str_pad</code> function, padding both sides of a string with another string until the final string reaches a desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::padBoth(&#39;James&#39;, 10, &#39;_&#39;);

// &#39;__James___&#39;

$padded = Str::padBoth(&#39;James&#39;, 10);

// &#39;  James   &#39;
</code></pre><p><a name="method-str-padleft"></a></p><h4 id="str-padleft-collection-method" tabindex="-1"><a class="header-anchor" href="#str-padleft-collection-method" aria-hidden="true">#</a> <code>Str::padLeft()</code> {.collection-method}</h4><p>The <code>Str::padLeft</code> method wraps PHP&#39;s <code>str_pad</code> function, padding the left side of a string with another string until the final string reaches a desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::padLeft(&#39;James&#39;, 10, &#39;-=&#39;);

// &#39;-=-=-James&#39;

$padded = Str::padLeft(&#39;James&#39;, 10);

// &#39;     James&#39;
</code></pre><p><a name="method-str-padright"></a></p><h4 id="str-padright-collection-method" tabindex="-1"><a class="header-anchor" href="#str-padright-collection-method" aria-hidden="true">#</a> <code>Str::padRight()</code> {.collection-method}</h4><p>The <code>Str::padRight</code> method wraps PHP&#39;s <code>str_pad</code> function, padding the right side of a string with another string until the final string reaches a desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::padRight(&#39;James&#39;, 10, &#39;-&#39;);

// &#39;James-----&#39;

$padded = Str::padRight(&#39;James&#39;, 10);

// &#39;James     &#39;
</code></pre><p><a name="method-str-password"></a></p><h4 id="str-password-collection-method" tabindex="-1"><a class="header-anchor" href="#str-password-collection-method" aria-hidden="true">#</a> <code>Str::password()</code> {.collection-method}</h4><p>The <code>Str::password</code> method may be used to generate a secure, random password of a given length. The password will consist of a combination of letters, numbers, symbols, and spaces. By default, passwords are 32 characters long:</p><pre><code>use Illuminate\\Support\\Str;

$password = Str::password();

// &#39;EbJo2vE-AS:U,$%_gkrV4n,q~1xy/-_4&#39;

$password = Str::password(12);

// &#39;qwuar&gt;#V|i]N&#39;
</code></pre><p><a name="method-str-plural"></a></p><h4 id="str-plural-collection-method" tabindex="-1"><a class="header-anchor" href="#str-plural-collection-method" aria-hidden="true">#</a> <code>Str::plural()</code> {.collection-method}</h4><p>The <code>Str::plural</code> method converts a singular word string to its plural form. This function supports <a href="./localization#pluralization-language">any of the languages support by Laravel&#39;s pluralizer</a>:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::plural(&#39;car&#39;);

// cars

$plural = Str::plural(&#39;child&#39;);

// children
</code></pre><p>You may provide an integer as a second argument to the function to retrieve the singular or plural form of the string:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::plural(&#39;child&#39;, 2);

// children

$singular = Str::plural(&#39;child&#39;, 1);

// child
</code></pre><p><a name="method-str-plural-studly"></a></p><h4 id="str-pluralstudly-collection-method" tabindex="-1"><a class="header-anchor" href="#str-pluralstudly-collection-method" aria-hidden="true">#</a> <code>Str::pluralStudly()</code> {.collection-method}</h4><p>The <code>Str::pluralStudly</code> method converts a singular word string formatted in studly caps case to its plural form. This function supports <a href="./localization#pluralization-language">any of the languages support by Laravel&#39;s pluralizer</a>:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::pluralStudly(&#39;VerifiedHuman&#39;);

// VerifiedHumans

$plural = Str::pluralStudly(&#39;UserFeedback&#39;);

// UserFeedback
</code></pre><p>You may provide an integer as a second argument to the function to retrieve the singular or plural form of the string:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::pluralStudly(&#39;VerifiedHuman&#39;, 2);

// VerifiedHumans

$singular = Str::pluralStudly(&#39;VerifiedHuman&#39;, 1);

// VerifiedHuman
</code></pre><p><a name="method-str-position"></a></p><h4 id="str-position-collection-method" tabindex="-1"><a class="header-anchor" href="#str-position-collection-method" aria-hidden="true">#</a> <code>Str::position()</code> {.collection-method}</h4><p>The <code>Str::position</code> method returns the position of the first occurrence of a substring in a string. If the substring does not exist in the given string, <code>false</code> is returned:</p><pre><code>use Illuminate\\Support\\Str;

$position = Str::position(&#39;Hello, World!&#39;, &#39;Hello&#39;);

// 0

$position = Str::position(&#39;Hello, World!&#39;, &#39;W&#39;);

// 7
</code></pre><p><a name="method-str-random"></a></p><h4 id="str-random-collection-method" tabindex="-1"><a class="header-anchor" href="#str-random-collection-method" aria-hidden="true">#</a> <code>Str::random()</code> {.collection-method}</h4><p>The <code>Str::random</code> method generates a random string of the specified length. This function uses PHP&#39;s <code>random_bytes</code> function:</p><pre><code>use Illuminate\\Support\\Str;

$random = Str::random(40);
</code></pre><p><a name="method-str-remove"></a></p><h4 id="str-remove-collection-method" tabindex="-1"><a class="header-anchor" href="#str-remove-collection-method" aria-hidden="true">#</a> <code>Str::remove()</code> {.collection-method}</h4><p>The <code>Str::remove</code> method removes the given value or array of values from the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = &#39;Peter Piper picked a peck of pickled peppers.&#39;;

$removed = Str::remove(&#39;e&#39;, $string);

// Ptr Pipr pickd a pck of pickld ppprs.
</code></pre><p>You may also pass <code>false</code> as a third argument to the <code>remove</code> method to ignore case when removing strings.</p><p><a name="method-str-repeat"></a></p><h4 id="str-repeat-collection-method" tabindex="-1"><a class="header-anchor" href="#str-repeat-collection-method" aria-hidden="true">#</a> <code>Str::repeat()</code> {.collection-method}</h4><p>The <code>Str::repeat</code> method repeats the given string:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Str</span><span class="token punctuation">;</span>

<span class="token variable">$string</span> <span class="token operator">=</span> <span class="token string single-quoted-string">&#39;a&#39;</span><span class="token punctuation">;</span>

<span class="token variable">$repeat</span> <span class="token operator">=</span> <span class="token class-name static-context">Str</span><span class="token operator">::</span><span class="token function">repeat</span><span class="token punctuation">(</span><span class="token variable">$string</span><span class="token punctuation">,</span> <span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// aaaaa</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="method-str-replace"></a></p><h4 id="str-replace-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replace-collection-method" aria-hidden="true">#</a> <code>Str::replace()</code> {.collection-method}</h4><p>The <code>Str::replace</code> method replaces a given string within the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = &#39;Laravel 8.x&#39;;

$replaced = Str::replace(&#39;8.x&#39;, &#39;9.x&#39;, $string);

// Laravel 9.x
</code></pre><p>The <code>replace</code> method also accepts a <code>caseSensitive</code> argument. By default, the <code>replace</code> method is case sensitive:</p><pre><code>Str::replace(&#39;Framework&#39;, &#39;Laravel&#39;, caseSensitive: false);
</code></pre><p><a name="method-str-replace-array"></a></p><h4 id="str-replacearray-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replacearray-collection-method" aria-hidden="true">#</a> <code>Str::replaceArray()</code> {.collection-method}</h4><p>The <code>Str::replaceArray</code> method replaces a given value in the string sequentially using an array:</p><pre><code>use Illuminate\\Support\\Str;

$string = &#39;The event will take place between ? and ?&#39;;

$replaced = Str::replaceArray(&#39;?&#39;, [&#39;8:30&#39;, &#39;9:00&#39;], $string);

// The event will take place between 8:30 and 9:00
</code></pre><p><a name="method-str-replace-first"></a></p><h4 id="str-replacefirst-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replacefirst-collection-method" aria-hidden="true">#</a> <code>Str::replaceFirst()</code> {.collection-method}</h4><p>The <code>Str::replaceFirst</code> method replaces the first occurrence of a given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceFirst(&#39;the&#39;, &#39;a&#39;, &#39;the quick brown fox jumps over the lazy dog&#39;);

// a quick brown fox jumps over the lazy dog
</code></pre><p><a name="method-str-replace-last"></a></p><h4 id="str-replacelast-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replacelast-collection-method" aria-hidden="true">#</a> <code>Str::replaceLast()</code> {.collection-method}</h4><p>The <code>Str::replaceLast</code> method replaces the last occurrence of a given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceLast(&#39;the&#39;, &#39;a&#39;, &#39;the quick brown fox jumps over the lazy dog&#39;);

// the quick brown fox jumps over a lazy dog
</code></pre><p><a name="method-str-replace-matches"></a></p><h4 id="str-replacematches-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replacematches-collection-method" aria-hidden="true">#</a> <code>Str::replaceMatches()</code> {.collection-method}</h4><p>The <code>Str::replaceMatches</code> method replaces all portions of a string matching a pattern with the given replacement string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceMatches(
    pattern: &#39;/[^A-Za-z0-9]++/&#39;,
    replace: &#39;&#39;,
    subject: &#39;(+1) 501-555-1000&#39;
)

// &#39;15015551000&#39;
</code></pre><p>The <code>replaceMatches</code> method also accepts a closure that will be invoked with each portion of the string matching the given pattern, allowing you to perform the replacement logic within the closure and return the replaced value:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceMatches(&#39;/\\d/&#39;, function (array $matches) {
    return &#39;[&#39;.$matches[0].&#39;]&#39;;
}, &#39;123&#39;);

// &#39;[1][2][3]&#39;
</code></pre><p><a name="method-str-replace-start"></a></p><h4 id="str-replacestart-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replacestart-collection-method" aria-hidden="true">#</a> <code>Str::replaceStart()</code> {.collection-method}</h4><p>The <code>Str::replaceStart</code> method replaces the first occurrence of the given value only if the value appears at the start of the string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceStart(&#39;Hello&#39;, &#39;Laravel&#39;, &#39;Hello World&#39;);

// Laravel World

$replaced = Str::replaceStart(&#39;World&#39;, &#39;Laravel&#39;, &#39;Hello World&#39;);

// Hello World
</code></pre><p><a name="method-str-replace-end"></a></p><h4 id="str-replaceend-collection-method" tabindex="-1"><a class="header-anchor" href="#str-replaceend-collection-method" aria-hidden="true">#</a> <code>Str::replaceEnd()</code> {.collection-method}</h4><p>The <code>Str::replaceEnd</code> method replaces the last occurrence of the given value only if the value appears at the end of the string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::replaceEnd(&#39;World&#39;, &#39;Laravel&#39;, &#39;Hello World&#39;);

// Hello Laravel

$replaced = Str::replaceEnd(&#39;Hello&#39;, &#39;Laravel&#39;, &#39;Hello World&#39;);

// Hello World
</code></pre><p><a name="method-str-reverse"></a></p><h4 id="str-reverse-collection-method" tabindex="-1"><a class="header-anchor" href="#str-reverse-collection-method" aria-hidden="true">#</a> <code>Str::reverse()</code> {.collection-method}</h4><p>The <code>Str::reverse</code> method reverses the given string:</p><pre><code>use Illuminate\\Support\\Str;

$reversed = Str::reverse(&#39;Hello World&#39;);

// dlroW olleH
</code></pre><p><a name="method-str-singular"></a></p><h4 id="str-singular-collection-method" tabindex="-1"><a class="header-anchor" href="#str-singular-collection-method" aria-hidden="true">#</a> <code>Str::singular()</code> {.collection-method}</h4><p>The <code>Str::singular</code> method converts a string to its singular form. This function supports <a href="./localization#pluralization-language">any of the languages support by Laravel&#39;s pluralizer</a>:</p><pre><code>use Illuminate\\Support\\Str;

$singular = Str::singular(&#39;cars&#39;);

// car

$singular = Str::singular(&#39;children&#39;);

// child
</code></pre><p><a name="method-str-slug"></a></p><h4 id="str-slug-collection-method" tabindex="-1"><a class="header-anchor" href="#str-slug-collection-method" aria-hidden="true">#</a> <code>Str::slug()</code> {.collection-method}</h4><p>The <code>Str::slug</code> method generates a URL friendly &quot;slug&quot; from the given string:</p><pre><code>use Illuminate\\Support\\Str;

$slug = Str::slug(&#39;Laravel 5 Framework&#39;, &#39;-&#39;);

// laravel-5-framework
</code></pre><p><a name="method-snake-case"></a></p><h4 id="str-snake-collection-method" tabindex="-1"><a class="header-anchor" href="#str-snake-collection-method" aria-hidden="true">#</a> <code>Str::snake()</code> {.collection-method}</h4><p>The <code>Str::snake</code> method converts the given string to <code>snake_case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::snake(&#39;fooBar&#39;);

// foo_bar

$converted = Str::snake(&#39;fooBar&#39;, &#39;-&#39;);

// foo-bar
</code></pre><p><a name="method-str-squish"></a></p><h4 id="str-squish-collection-method" tabindex="-1"><a class="header-anchor" href="#str-squish-collection-method" aria-hidden="true">#</a> <code>Str::squish()</code> {.collection-method}</h4><p>The <code>Str::squish</code> method removes all extraneous white space from a string, including extraneous white space between words:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::squish(&#39;    laravel    framework    &#39;);

// laravel framework
</code></pre><p><a name="method-str-start"></a></p><h4 id="str-start-collection-method" tabindex="-1"><a class="header-anchor" href="#str-start-collection-method" aria-hidden="true">#</a> <code>Str::start()</code> {.collection-method}</h4><p>The <code>Str::start</code> method adds a single instance of the given value to a string if it does not already start with that value:</p><pre><code>use Illuminate\\Support\\Str;

$adjusted = Str::start(&#39;this/string&#39;, &#39;/&#39;);

// /this/string

$adjusted = Str::start(&#39;/this/string&#39;, &#39;/&#39;);

// /this/string
</code></pre><p><a name="method-starts-with"></a></p><h4 id="str-startswith-collection-method" tabindex="-1"><a class="header-anchor" href="#str-startswith-collection-method" aria-hidden="true">#</a> <code>Str::startsWith()</code> {.collection-method}</h4><p>The <code>Str::startsWith</code> method determines if the given string begins with the given value:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::startsWith(&#39;This is my name&#39;, &#39;This&#39;);

// true
</code></pre><p>If an array of possible values is passed, the <code>startsWith</code> method will return <code>true</code> if the string begins with any of the given values:</p><pre><code>$result = Str::startsWith(&#39;This is my name&#39;, [&#39;This&#39;, &#39;That&#39;, &#39;There&#39;]);

// true
</code></pre><p><a name="method-studly-case"></a></p><h4 id="str-studly-collection-method" tabindex="-1"><a class="header-anchor" href="#str-studly-collection-method" aria-hidden="true">#</a> <code>Str::studly()</code> {.collection-method}</h4><p>The <code>Str::studly</code> method converts the given string to <code>StudlyCase</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::studly(&#39;foo_bar&#39;);

// FooBar
</code></pre><p><a name="method-str-substr"></a></p><h4 id="str-substr-collection-method" tabindex="-1"><a class="header-anchor" href="#str-substr-collection-method" aria-hidden="true">#</a> <code>Str::substr()</code> {.collection-method}</h4><p>The <code>Str::substr</code> method returns the portion of string specified by the start and length parameters:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::substr(&#39;The Laravel Framework&#39;, 4, 7);

// Laravel
</code></pre><p><a name="method-str-substrcount"></a></p><h4 id="str-substrcount-collection-method" tabindex="-1"><a class="header-anchor" href="#str-substrcount-collection-method" aria-hidden="true">#</a> <code>Str::substrCount()</code> {.collection-method}</h4><p>The <code>Str::substrCount</code> method returns the number of occurrences of a given value in the given string:</p><pre><code>use Illuminate\\Support\\Str;

$count = Str::substrCount(&#39;If you like ice cream, you will like snow cones.&#39;, &#39;like&#39;);

// 2
</code></pre><p><a name="method-str-substrreplace"></a></p><h4 id="str-substrreplace-collection-method" tabindex="-1"><a class="header-anchor" href="#str-substrreplace-collection-method" aria-hidden="true">#</a> <code>Str::substrReplace()</code> {.collection-method}</h4><p>The <code>Str::substrReplace</code> method replaces text within a portion of a string, starting at the position specified by the third argument and replacing the number of characters specified by the fourth argument. Passing <code>0</code> to the method&#39;s fourth argument will insert the string at the specified position without replacing any of the existing characters in the string:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::substrReplace(&#39;1300&#39;, &#39;:&#39;, 2);
// 13:

$result = Str::substrReplace(&#39;1300&#39;, &#39;:&#39;, 2, 0);
// 13:00
</code></pre><p><a name="method-str-swap"></a></p><h4 id="str-swap-collection-method" tabindex="-1"><a class="header-anchor" href="#str-swap-collection-method" aria-hidden="true">#</a> <code>Str::swap()</code> {.collection-method}</h4><p>The <code>Str::swap</code> method replaces multiple values in the given string using PHP&#39;s <code>strtr</code> function:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::swap([
    &#39;Tacos&#39; =&gt; &#39;Burritos&#39;,
    &#39;great&#39; =&gt; &#39;fantastic&#39;,
], &#39;Tacos are great!&#39;);

// Burritos are fantastic!
</code></pre><p><a name="method-take"></a></p><h4 id="str-take-collection-method" tabindex="-1"><a class="header-anchor" href="#str-take-collection-method" aria-hidden="true">#</a> <code>Str::take()</code> {.collection-method}</h4><p>The <code>Str::take</code> method returns a specified number of characters from the beginning of a string:</p><pre><code>use Illuminate\\Support\\Str;

$taken = Str::take(&#39;Build something amazing!&#39;, 5);

// Build
</code></pre><p><a name="method-title-case"></a></p><h4 id="str-title-collection-method" tabindex="-1"><a class="header-anchor" href="#str-title-collection-method" aria-hidden="true">#</a> <code>Str::title()</code> {.collection-method}</h4><p>The <code>Str::title</code> method converts the given string to <code>Title Case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::title(&#39;a nice title uses the correct case&#39;);

// A Nice Title Uses The Correct Case
</code></pre><p><a name="method-str-to-html-string"></a></p><h4 id="str-tohtmlstring-collection-method" tabindex="-1"><a class="header-anchor" href="#str-tohtmlstring-collection-method" aria-hidden="true">#</a> <code>Str::toHtmlString()</code> {.collection-method}</h4><p>The <code>Str::toHtmlString</code> method converts the string instance to an instance of <code>Illuminate\\Support\\HtmlString</code>, which may be displayed in Blade templates:</p><pre><code>use Illuminate\\Support\\Str;

$htmlString = Str::of(&#39;Nuno Maduro&#39;)-&gt;toHtmlString();
</code></pre><p><a name="method-str-ucfirst"></a></p><h4 id="str-ucfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#str-ucfirst-collection-method" aria-hidden="true">#</a> <code>Str::ucfirst()</code> {.collection-method}</h4><p>The <code>Str::ucfirst</code> method returns the given string with the first character capitalized:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::ucfirst(&#39;foo bar&#39;);

// Foo bar
</code></pre><p><a name="method-str-ucsplit"></a></p><h4 id="str-ucsplit-collection-method" tabindex="-1"><a class="header-anchor" href="#str-ucsplit-collection-method" aria-hidden="true">#</a> <code>Str::ucsplit()</code> {.collection-method}</h4><p>The <code>Str::ucsplit</code> method splits the given string into an array by uppercase characters:</p><pre><code>use Illuminate\\Support\\Str;

$segments = Str::ucsplit(&#39;FooBar&#39;);

// [0 =&gt; &#39;Foo&#39;, 1 =&gt; &#39;Bar&#39;]
</code></pre><p><a name="method-str-upper"></a></p><h4 id="str-upper-collection-method" tabindex="-1"><a class="header-anchor" href="#str-upper-collection-method" aria-hidden="true">#</a> <code>Str::upper()</code> {.collection-method}</h4><p>The <code>Str::upper</code> method converts the given string to uppercase:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::upper(&#39;laravel&#39;);

// LARAVEL
</code></pre><p><a name="method-str-ulid"></a></p><h4 id="str-ulid-collection-method" tabindex="-1"><a class="header-anchor" href="#str-ulid-collection-method" aria-hidden="true">#</a> <code>Str::ulid()</code> {.collection-method}</h4><p>The <code>Str::ulid</code> method generates a ULID, which is a compact, time-ordered unique identifier:</p><pre><code>use Illuminate\\Support\\Str;

return (string) Str::ulid();

// 01gd6r360bp37zj17nxb55yv40
</code></pre><p>If you would like to retrieve a <code>Illuminate\\Support\\Carbon</code> date instance representing the date and time that a given ULID was created, you may use the <code>createFromId</code> method provided by Laravel&#39;s Carbon integration:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Carbon</span><span class="token punctuation">;</span>
<span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Str</span><span class="token punctuation">;</span>

<span class="token variable">$date</span> <span class="token operator">=</span> <span class="token class-name static-context">Carbon</span><span class="token operator">::</span><span class="token function">createFromId</span><span class="token punctuation">(</span><span class="token punctuation">(</span><span class="token keyword type-casting">string</span><span class="token punctuation">)</span> <span class="token class-name static-context">Str</span><span class="token operator">::</span><span class="token function">ulid</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="method-str-uuid"></a></p><h4 id="str-uuid-collection-method" tabindex="-1"><a class="header-anchor" href="#str-uuid-collection-method" aria-hidden="true">#</a> <code>Str::uuid()</code> {.collection-method}</h4><p>The <code>Str::uuid</code> method generates a UUID (version 4):</p><pre><code>use Illuminate\\Support\\Str;

return (string) Str::uuid();
</code></pre><p><a name="method-str-word-count"></a></p><h4 id="str-wordcount-collection-method" tabindex="-1"><a class="header-anchor" href="#str-wordcount-collection-method" aria-hidden="true">#</a> <code>Str::wordCount()</code> {.collection-method}</h4><p>The <code>Str::wordCount</code> method returns the number of words that a string contains:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Str</span><span class="token punctuation">;</span>

<span class="token class-name static-context">Str</span><span class="token operator">::</span><span class="token function">wordCount</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;Hello, world!&#39;</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="method-str-word-wrap"></a></p><h4 id="str-wordwrap-collection-method" tabindex="-1"><a class="header-anchor" href="#str-wordwrap-collection-method" aria-hidden="true">#</a> <code>Str::wordWrap()</code> {.collection-method}</h4><p>The <code>Str::wordWrap</code> method wraps a string to a given number of characters:</p><pre><code>use Illuminate\\Support\\Str;

$text = &quot;The quick brown fox jumped over the lazy dog.&quot;

Str::wordWrap($text, characters: 20, break: &quot;&lt;br /&gt;\\n&quot;);

/*
The quick brown fox&lt;br /&gt;
jumped over the lazy&lt;br /&gt;
dog.
*/
</code></pre><p><a name="method-str-words"></a></p><h4 id="str-words-collection-method" tabindex="-1"><a class="header-anchor" href="#str-words-collection-method" aria-hidden="true">#</a> <code>Str::words()</code> {.collection-method}</h4><p>The <code>Str::words</code> method limits the number of words in a string. An additional string may be passed to this method via its third argument to specify which string should be appended to the end of the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

return Str::words(&#39;Perfectly balanced, as all things should be.&#39;, 3, &#39; &gt;&gt;&gt;&#39;);

// Perfectly balanced, as &gt;&gt;&gt;
</code></pre><p><a name="method-str-wrap"></a></p><h4 id="str-wrap-collection-method" tabindex="-1"><a class="header-anchor" href="#str-wrap-collection-method" aria-hidden="true">#</a> <code>Str::wrap()</code> {.collection-method}</h4><p>The <code>Str::wrap</code> method wraps the given string with an additional string or pair of strings:</p><pre><code>use Illuminate\\Support\\Str;

Str::wrap(&#39;Laravel&#39;, &#39;&quot;&#39;);

// &quot;Laravel&quot;

Str::wrap(&#39;is&#39;, before: &#39;This &#39;, after: &#39; Laravel!&#39;);

// This is Laravel!
</code></pre><p><a name="method-str"></a></p><h4 id="str-collection-method" tabindex="-1"><a class="header-anchor" href="#str-collection-method" aria-hidden="true">#</a> <code>str()</code> {.collection-method}</h4><p>The <code>str</code> function returns a new <code>Illuminate\\Support\\Stringable</code> instance of the given string. This function is equivalent to the <code>Str::of</code> method:</p><pre><code>$string = str(&#39;Taylor&#39;)-&gt;append(&#39; Otwell&#39;);

// &#39;Taylor Otwell&#39;
</code></pre><p>If no argument is provided to the <code>str</code> function, the function returns an instance of <code>Illuminate\\Support\\Str</code>:</p><pre><code>$snake = str()-&gt;snake(&#39;FooBar&#39;);

// &#39;foo_bar&#39;
</code></pre><p><a name="method-trans"></a></p><h4 id="trans-collection-method" tabindex="-1"><a class="header-anchor" href="#trans-collection-method" aria-hidden="true">#</a> <code>trans()</code> {.collection-method}</h4><p>The <code>trans</code> function translates the given translation key using your <a href="./localization">language files</a>:</p><pre><code>echo trans(&#39;messages.welcome&#39;);
</code></pre><p>If the specified translation key does not exist, the <code>trans</code> function will return the given key. So, using the example above, the <code>trans</code> function would return <code>messages.welcome</code> if the translation key does not exist.</p><p><a name="method-trans-choice"></a></p><h4 id="trans-choice-collection-method" tabindex="-1"><a class="header-anchor" href="#trans-choice-collection-method" aria-hidden="true">#</a> <code>trans_choice()</code> {.collection-method}</h4><p>The <code>trans_choice</code> function translates the given translation key with inflection:</p><pre><code>echo trans_choice(&#39;messages.notifications&#39;, $unreadCount);
</code></pre><p>If the specified translation key does not exist, the <code>trans_choice</code> function will return the given key. So, using the example above, the <code>trans_choice</code> function would return <code>messages.notifications</code> if the translation key does not exist.</p><p><a name="fluent-strings"></a></p><h2 id="fluent-strings-1" tabindex="-1"><a class="header-anchor" href="#fluent-strings-1" aria-hidden="true">#</a> Fluent Strings</h2><p>Fluent strings provide a more fluent, object-oriented interface for working with string values, allowing you to chain multiple string operations together using a more readable syntax compared to traditional string operations.</p><p><a name="method-fluent-str-after"></a></p><h4 id="after-collection-method" tabindex="-1"><a class="header-anchor" href="#after-collection-method" aria-hidden="true">#</a> <code>after</code> {.collection-method}</h4><p>The <code>after</code> method returns everything after the given value in a string. The entire string will be returned if the value does not exist within the string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::of(&#39;This is my name&#39;)-&gt;after(&#39;This is&#39;);

// &#39; my name&#39;
</code></pre><p><a name="method-fluent-str-after-last"></a></p><h4 id="afterlast-collection-method" tabindex="-1"><a class="header-anchor" href="#afterlast-collection-method" aria-hidden="true">#</a> <code>afterLast</code> {.collection-method}</h4><p>The <code>afterLast</code> method returns everything after the last occurrence of the given value in a string. The entire string will be returned if the value does not exist within the string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::of(&#39;App\\Http\\Controllers\\Controller&#39;)-&gt;afterLast(&#39;\\\\&#39;);

// &#39;Controller&#39;
</code></pre><p><a name="method-fluent-str-append"></a></p><h4 id="append-collection-method" tabindex="-1"><a class="header-anchor" href="#append-collection-method" aria-hidden="true">#</a> <code>append</code> {.collection-method}</h4><p>The <code>append</code> method appends the given values to the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Taylor&#39;)-&gt;append(&#39; Otwell&#39;);

// &#39;Taylor Otwell&#39;
</code></pre><p><a name="method-fluent-str-ascii"></a></p><h4 id="ascii-collection-method" tabindex="-1"><a class="header-anchor" href="#ascii-collection-method" aria-hidden="true">#</a> <code>ascii</code> {.collection-method}</h4><p>The <code>ascii</code> method will attempt to transliterate the string into an ASCII value:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;ü&#39;)-&gt;ascii();

// &#39;u&#39;
</code></pre><p><a name="method-fluent-str-basename"></a></p><h4 id="basename-collection-method" tabindex="-1"><a class="header-anchor" href="#basename-collection-method" aria-hidden="true">#</a> <code>basename</code> {.collection-method}</h4><p>The <code>basename</code> method will return the trailing name component of the given string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;/foo/bar/baz&#39;)-&gt;basename();

// &#39;baz&#39;
</code></pre><p>If needed, you may provide an &quot;extension&quot; that will be removed from the trailing component:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;/foo/bar/baz.jpg&#39;)-&gt;basename(&#39;.jpg&#39;);

// &#39;baz&#39;
</code></pre><p><a name="method-fluent-str-before"></a></p><h4 id="before-collection-method" tabindex="-1"><a class="header-anchor" href="#before-collection-method" aria-hidden="true">#</a> <code>before</code> {.collection-method}</h4><p>The <code>before</code> method returns everything before the given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::of(&#39;This is my name&#39;)-&gt;before(&#39;my name&#39;);

// &#39;This is &#39;
</code></pre><p><a name="method-fluent-str-before-last"></a></p><h4 id="beforelast-collection-method" tabindex="-1"><a class="header-anchor" href="#beforelast-collection-method" aria-hidden="true">#</a> <code>beforeLast</code> {.collection-method}</h4><p>The <code>beforeLast</code> method returns everything before the last occurrence of the given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$slice = Str::of(&#39;This is my name&#39;)-&gt;beforeLast(&#39;is&#39;);

// &#39;This &#39;
</code></pre><p><a name="method-fluent-str-between"></a></p><h4 id="between-collection-method" tabindex="-1"><a class="header-anchor" href="#between-collection-method" aria-hidden="true">#</a> <code>between</code> {.collection-method}</h4><p>The <code>between</code> method returns the portion of a string between two values:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;This is my name&#39;)-&gt;between(&#39;This&#39;, &#39;name&#39;);

// &#39; is my &#39;
</code></pre><p><a name="method-fluent-str-between-first"></a></p><h4 id="betweenfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#betweenfirst-collection-method" aria-hidden="true">#</a> <code>betweenFirst</code> {.collection-method}</h4><p>The <code>betweenFirst</code> method returns the smallest possible portion of a string between two values:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;[a] bc [d]&#39;)-&gt;betweenFirst(&#39;[&#39;, &#39;]&#39;);

// &#39;a&#39;
</code></pre><p><a name="method-fluent-str-camel"></a></p><h4 id="camel-collection-method" tabindex="-1"><a class="header-anchor" href="#camel-collection-method" aria-hidden="true">#</a> <code>camel</code> {.collection-method}</h4><p>The <code>camel</code> method converts the given string to <code>camelCase</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;foo_bar&#39;)-&gt;camel();

// fooBar
</code></pre><p><a name="method-fluent-str-class-basename"></a></p><h4 id="classbasename-collection-method" tabindex="-1"><a class="header-anchor" href="#classbasename-collection-method" aria-hidden="true">#</a> <code>classBasename</code> {.collection-method}</h4><p>The <code>classBasename</code> method returns the class name of the given class with the class&#39;s namespace removed:</p><pre><code>use Illuminate\\Support\\Str;

$class = Str::of(&#39;Foo\\Bar\\Baz&#39;)-&gt;classBasename();

// Baz
</code></pre><p><a name="method-fluent-str-contains"></a></p><h4 id="contains-collection-method" tabindex="-1"><a class="header-anchor" href="#contains-collection-method" aria-hidden="true">#</a> <code>contains</code> {.collection-method}</h4><p>The <code>contains</code> method determines if the given string contains the given value. This method is case sensitive:</p><pre><code>use Illuminate\\Support\\Str;

$contains = Str::of(&#39;This is my name&#39;)-&gt;contains(&#39;my&#39;);

// true
</code></pre><p>You may also pass an array of values to determine if the given string contains any of the values in the array:</p><pre><code>use Illuminate\\Support\\Str;

$contains = Str::of(&#39;This is my name&#39;)-&gt;contains([&#39;my&#39;, &#39;foo&#39;]);

// true
</code></pre><p><a name="method-fluent-str-contains-all"></a></p><h4 id="containsall-collection-method" tabindex="-1"><a class="header-anchor" href="#containsall-collection-method" aria-hidden="true">#</a> <code>containsAll</code> {.collection-method}</h4><p>The <code>containsAll</code> method determines if the given string contains all of the values in the given array:</p><pre><code>use Illuminate\\Support\\Str;

$containsAll = Str::of(&#39;This is my name&#39;)-&gt;containsAll([&#39;my&#39;, &#39;name&#39;]);

// true
</code></pre><p><a name="method-fluent-str-dirname"></a></p><h4 id="dirname-collection-method" tabindex="-1"><a class="header-anchor" href="#dirname-collection-method" aria-hidden="true">#</a> <code>dirname</code> {.collection-method}</h4><p>The <code>dirname</code> method returns the parent directory portion of the given string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;/foo/bar/baz&#39;)-&gt;dirname();

// &#39;/foo/bar&#39;
</code></pre><p>If necessary, you may specify how many directory levels you wish to trim from the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;/foo/bar/baz&#39;)-&gt;dirname(2);

// &#39;/foo&#39;
</code></pre><p><a name="method-fluent-str-excerpt"></a></p><h4 id="excerpt-collection-method" tabindex="-1"><a class="header-anchor" href="#excerpt-collection-method" aria-hidden="true">#</a> <code>excerpt</code> {.collection-method}</h4><p>The <code>excerpt</code> method extracts an excerpt from the string that matches the first instance of a phrase within that string:</p><pre><code>use Illuminate\\Support\\Str;

$excerpt = Str::of(&#39;This is my name&#39;)-&gt;excerpt(&#39;my&#39;, [
    &#39;radius&#39; =&gt; 3
]);

// &#39;...is my na...&#39;
</code></pre><p>The <code>radius</code> option, which defaults to <code>100</code>, allows you to define the number of characters that should appear on each side of the truncated string.</p><p>In addition, you may use the <code>omission</code> option to change the string that will be prepended and appended to the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

$excerpt = Str::of(&#39;This is my name&#39;)-&gt;excerpt(&#39;name&#39;, [
    &#39;radius&#39; =&gt; 3,
    &#39;omission&#39; =&gt; &#39;(...) &#39;
]);

// &#39;(...) my name&#39;
</code></pre><p><a name="method-fluent-str-ends-with"></a></p><h4 id="endswith-collection-method" tabindex="-1"><a class="header-anchor" href="#endswith-collection-method" aria-hidden="true">#</a> <code>endsWith</code> {.collection-method}</h4><p>The <code>endsWith</code> method determines if the given string ends with the given value:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;This is my name&#39;)-&gt;endsWith(&#39;name&#39;);

// true
</code></pre><p>You may also pass an array of values to determine if the given string ends with any of the values in the array:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;This is my name&#39;)-&gt;endsWith([&#39;name&#39;, &#39;foo&#39;]);

// true

$result = Str::of(&#39;This is my name&#39;)-&gt;endsWith([&#39;this&#39;, &#39;foo&#39;]);

// false
</code></pre><p><a name="method-fluent-str-exactly"></a></p><h4 id="exactly-collection-method" tabindex="-1"><a class="header-anchor" href="#exactly-collection-method" aria-hidden="true">#</a> <code>exactly</code> {.collection-method}</h4><p>The <code>exactly</code> method determines if the given string is an exact match with another string:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;Laravel&#39;)-&gt;exactly(&#39;Laravel&#39;);

// true
</code></pre><p><a name="method-fluent-str-explode"></a></p><h4 id="explode-collection-method" tabindex="-1"><a class="header-anchor" href="#explode-collection-method" aria-hidden="true">#</a> <code>explode</code> {.collection-method}</h4><p>The <code>explode</code> method splits the string by the given delimiter and returns a collection containing each section of the split string:</p><pre><code>use Illuminate\\Support\\Str;

$collection = Str::of(&#39;foo bar baz&#39;)-&gt;explode(&#39; &#39;);

// collect([&#39;foo&#39;, &#39;bar&#39;, &#39;baz&#39;])
</code></pre><p><a name="method-fluent-str-finish"></a></p><h4 id="finish-collection-method" tabindex="-1"><a class="header-anchor" href="#finish-collection-method" aria-hidden="true">#</a> <code>finish</code> {.collection-method}</h4><p>The <code>finish</code> method adds a single instance of the given value to a string if it does not already end with that value:</p><pre><code>use Illuminate\\Support\\Str;

$adjusted = Str::of(&#39;this/string&#39;)-&gt;finish(&#39;/&#39;);

// this/string/

$adjusted = Str::of(&#39;this/string/&#39;)-&gt;finish(&#39;/&#39;);

// this/string/
</code></pre><p><a name="method-fluent-str-headline"></a></p><h4 id="headline-collection-method" tabindex="-1"><a class="header-anchor" href="#headline-collection-method" aria-hidden="true">#</a> <code>headline</code> {.collection-method}</h4><p>The <code>headline</code> method will convert strings delimited by casing, hyphens, or underscores into a space delimited string with each word&#39;s first letter capitalized:</p><pre><code>use Illuminate\\Support\\Str;

$headline = Str::of(&#39;taylor_otwell&#39;)-&gt;headline();

// Taylor Otwell

$headline = Str::of(&#39;EmailNotificationSent&#39;)-&gt;headline();

// Email Notification Sent
</code></pre><p><a name="method-fluent-str-inline-markdown"></a></p><h4 id="inlinemarkdown-collection-method" tabindex="-1"><a class="header-anchor" href="#inlinemarkdown-collection-method" aria-hidden="true">#</a> <code>inlineMarkdown</code> {.collection-method}</h4>`,300),x=e("code",null,"inlineMarkdown",-1),T={href:"https://commonmark.thephpleague.com/",target:"_blank",rel:"noopener noreferrer"},$=e("code",null,"markdown",-1),I=n(`<pre><code>use Illuminate\\Support\\Str;

$html = Str::of(&#39;**Laravel**&#39;)-&gt;inlineMarkdown();

// &lt;strong&gt;Laravel&lt;/strong&gt;
</code></pre><p><a name="method-fluent-str-is"></a></p><h4 id="is-collection-method" tabindex="-1"><a class="header-anchor" href="#is-collection-method" aria-hidden="true">#</a> <code>is</code> {.collection-method}</h4><p>The <code>is</code> method determines if a given string matches a given pattern. Asterisks may be used as wildcard values</p><pre><code>use Illuminate\\Support\\Str;

$matches = Str::of(&#39;foobar&#39;)-&gt;is(&#39;foo*&#39;);

// true

$matches = Str::of(&#39;foobar&#39;)-&gt;is(&#39;baz*&#39;);

// false
</code></pre><p><a name="method-fluent-str-is-ascii"></a></p><h4 id="isascii-collection-method" tabindex="-1"><a class="header-anchor" href="#isascii-collection-method" aria-hidden="true">#</a> <code>isAscii</code> {.collection-method}</h4><p>The <code>isAscii</code> method determines if a given string is an ASCII string:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;Taylor&#39;)-&gt;isAscii();

// true

$result = Str::of(&#39;ü&#39;)-&gt;isAscii();

// false
</code></pre><p><a name="method-fluent-str-is-empty"></a></p><h4 id="isempty-collection-method" tabindex="-1"><a class="header-anchor" href="#isempty-collection-method" aria-hidden="true">#</a> <code>isEmpty</code> {.collection-method}</h4><p>The <code>isEmpty</code> method determines if the given string is empty:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;  &#39;)-&gt;trim()-&gt;isEmpty();

// true

$result = Str::of(&#39;Laravel&#39;)-&gt;trim()-&gt;isEmpty();

// false
</code></pre><p><a name="method-fluent-str-is-not-empty"></a></p><h4 id="isnotempty-collection-method" tabindex="-1"><a class="header-anchor" href="#isnotempty-collection-method" aria-hidden="true">#</a> <code>isNotEmpty</code> {.collection-method}</h4><p>The <code>isNotEmpty</code> method determines if the given string is not empty:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;  &#39;)-&gt;trim()-&gt;isNotEmpty();

// false

$result = Str::of(&#39;Laravel&#39;)-&gt;trim()-&gt;isNotEmpty();

// true
</code></pre><p><a name="method-fluent-str-is-json"></a></p><h4 id="isjson-collection-method" tabindex="-1"><a class="header-anchor" href="#isjson-collection-method" aria-hidden="true">#</a> <code>isJson</code> {.collection-method}</h4><p>The <code>isJson</code> method determines if a given string is valid JSON:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;[1,2,3]&#39;)-&gt;isJson();

// true

$result = Str::of(&#39;{&quot;first&quot;: &quot;John&quot;, &quot;last&quot;: &quot;Doe&quot;}&#39;)-&gt;isJson();

// true

$result = Str::of(&#39;{first: &quot;John&quot;, last: &quot;Doe&quot;}&#39;)-&gt;isJson();

// false
</code></pre><p><a name="method-fluent-str-is-ulid"></a></p><h4 id="isulid-collection-method" tabindex="-1"><a class="header-anchor" href="#isulid-collection-method" aria-hidden="true">#</a> <code>isUlid</code> {.collection-method}</h4><p>The <code>isUlid</code> method determines if a given string is a ULID:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;01gd6r360bp37zj17nxb55yv40&#39;)-&gt;isUlid();

// true

$result = Str::of(&#39;Taylor&#39;)-&gt;isUlid();

// false
</code></pre><p><a name="method-fluent-str-is-url"></a></p><h4 id="isurl-collection-method" tabindex="-1"><a class="header-anchor" href="#isurl-collection-method" aria-hidden="true">#</a> <code>isUrl</code> {.collection-method}</h4><p>The <code>isUrl</code> method determines if a given string is a URL:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;http://example.com&#39;)-&gt;isUrl();

// true

$result = Str::of(&#39;Taylor&#39;)-&gt;isUrl();

// false
</code></pre><p><a name="method-fluent-str-is-uuid"></a></p><h4 id="isuuid-collection-method" tabindex="-1"><a class="header-anchor" href="#isuuid-collection-method" aria-hidden="true">#</a> <code>isUuid</code> {.collection-method}</h4><p>The <code>isUuid</code> method determines if a given string is a UUID:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;5ace9ab9-e9cf-4ec6-a19d-5881212a452c&#39;)-&gt;isUuid();

// true

$result = Str::of(&#39;Taylor&#39;)-&gt;isUuid();

// false
</code></pre><p><a name="method-fluent-str-kebab"></a></p><h4 id="kebab-collection-method" tabindex="-1"><a class="header-anchor" href="#kebab-collection-method" aria-hidden="true">#</a> <code>kebab</code> {.collection-method}</h4><p>The <code>kebab</code> method converts the given string to <code>kebab-case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;fooBar&#39;)-&gt;kebab();

// foo-bar
</code></pre><p><a name="method-fluent-str-lcfirst"></a></p><h4 id="lcfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#lcfirst-collection-method" aria-hidden="true">#</a> <code>lcfirst</code> {.collection-method}</h4><p>The <code>lcfirst</code> method returns the given string with the first character lowercased:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Foo Bar&#39;)-&gt;lcfirst();

// foo Bar
</code></pre><p><a name="method-fluent-str-length"></a></p><h4 id="length-collection-method" tabindex="-1"><a class="header-anchor" href="#length-collection-method" aria-hidden="true">#</a> <code>length</code> {.collection-method}</h4><p>The <code>length</code> method returns the length of the given string:</p><pre><code>use Illuminate\\Support\\Str;

$length = Str::of(&#39;Laravel&#39;)-&gt;length();

// 7
</code></pre><p><a name="method-fluent-str-limit"></a></p><h4 id="limit-collection-method" tabindex="-1"><a class="header-anchor" href="#limit-collection-method" aria-hidden="true">#</a> <code>limit</code> {.collection-method}</h4><p>The <code>limit</code> method truncates the given string to the specified length:</p><pre><code>use Illuminate\\Support\\Str;

$truncated = Str::of(&#39;The quick brown fox jumps over the lazy dog&#39;)-&gt;limit(20);

// The quick brown fox...
</code></pre><p>You may also pass a second argument to change the string that will be appended to the end of the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

$truncated = Str::of(&#39;The quick brown fox jumps over the lazy dog&#39;)-&gt;limit(20, &#39; (...)&#39;);

// The quick brown fox (...)
</code></pre><p><a name="method-fluent-str-lower"></a></p><h4 id="lower-collection-method" tabindex="-1"><a class="header-anchor" href="#lower-collection-method" aria-hidden="true">#</a> <code>lower</code> {.collection-method}</h4><p>The <code>lower</code> method converts the given string to lowercase:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;LARAVEL&#39;)-&gt;lower();

// &#39;laravel&#39;
</code></pre><p><a name="method-fluent-str-ltrim"></a></p><h4 id="ltrim-collection-method" tabindex="-1"><a class="header-anchor" href="#ltrim-collection-method" aria-hidden="true">#</a> <code>ltrim</code> {.collection-method}</h4><p>The <code>ltrim</code> method trims the left side of the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;  Laravel  &#39;)-&gt;ltrim();

// &#39;Laravel  &#39;

$string = Str::of(&#39;/Laravel/&#39;)-&gt;ltrim(&#39;/&#39;);

// &#39;Laravel/&#39;
</code></pre><p><a name="method-fluent-str-markdown"></a></p><h4 id="markdown-collection-method" tabindex="-1"><a class="header-anchor" href="#markdown-collection-method" aria-hidden="true">#</a> <code>markdown</code> {.collection-method}</h4><p>The <code>markdown</code> method converts GitHub flavored Markdown into HTML:</p><pre><code>use Illuminate\\Support\\Str;

$html = Str::of(&#39;# Laravel&#39;)-&gt;markdown();

// &lt;h1&gt;Laravel&lt;/h1&gt;

$html = Str::of(&#39;# Taylor &lt;b&gt;Otwell&lt;/b&gt;&#39;)-&gt;markdown([
    &#39;html_input&#39; =&gt; &#39;strip&#39;,
]);

// &lt;h1&gt;Taylor Otwell&lt;/h1&gt;
</code></pre><p><a name="method-fluent-str-mask"></a></p><h4 id="mask-collection-method" tabindex="-1"><a class="header-anchor" href="#mask-collection-method" aria-hidden="true">#</a> <code>mask</code> {.collection-method}</h4><p>The <code>mask</code> method masks a portion of a string with a repeated character, and may be used to obfuscate segments of strings such as email addresses and phone numbers:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;taylor@example.com&#39;)-&gt;mask(&#39;*&#39;, 3);

// tay***************
</code></pre><p>If needed, you may provide negative numbers as the third or fourth argument to the <code>mask</code> method, which will instruct the method to begin masking at the given distance from the end of the string:</p><pre><code>$string = Str::of(&#39;taylor@example.com&#39;)-&gt;mask(&#39;*&#39;, -15, 3);

// tay***@example.com

$string = Str::of(&#39;taylor@example.com&#39;)-&gt;mask(&#39;*&#39;, 4, -4);

// tayl**********.com
</code></pre><p><a name="method-fluent-str-match"></a></p><h4 id="match-collection-method" tabindex="-1"><a class="header-anchor" href="#match-collection-method" aria-hidden="true">#</a> <code>match</code> {.collection-method}</h4><p>The <code>match</code> method will return the portion of a string that matches a given regular expression pattern:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;foo bar&#39;)-&gt;match(&#39;/bar/&#39;);

// &#39;bar&#39;

$result = Str::of(&#39;foo bar&#39;)-&gt;match(&#39;/foo (.*)/&#39;);

// &#39;bar&#39;
</code></pre><p><a name="method-fluent-str-match-all"></a></p><h4 id="matchall-collection-method" tabindex="-1"><a class="header-anchor" href="#matchall-collection-method" aria-hidden="true">#</a> <code>matchAll</code> {.collection-method}</h4><p>The <code>matchAll</code> method will return a collection containing the portions of a string that match a given regular expression pattern:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;bar foo bar&#39;)-&gt;matchAll(&#39;/bar/&#39;);

// collect([&#39;bar&#39;, &#39;bar&#39;])
</code></pre><p>If you specify a matching group within the expression, Laravel will return a collection of that group&#39;s matches:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;bar fun bar fly&#39;)-&gt;matchAll(&#39;/f(\\w*)/&#39;);

// collect([&#39;un&#39;, &#39;ly&#39;]);
</code></pre><p>If no matches are found, an empty collection will be returned.</p><p><a name="method-fluent-str-is-match"></a></p><h4 id="ismatch-collection-method" tabindex="-1"><a class="header-anchor" href="#ismatch-collection-method" aria-hidden="true">#</a> <code>isMatch</code> {.collection-method}</h4><p>The <code>isMatch</code> method will return <code>true</code> if the string matches a given regular expression:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;foo bar&#39;)-&gt;isMatch(&#39;/foo (.*)/&#39;);

// true

$result = Str::of(&#39;laravel&#39;)-&gt;isMatch(&#39;/foo (.*)/&#39;);

// false
</code></pre><p><a name="method-fluent-str-new-line"></a></p><h4 id="newline-collection-method" tabindex="-1"><a class="header-anchor" href="#newline-collection-method" aria-hidden="true">#</a> <code>newLine</code> {.collection-method}</h4><p>The <code>newLine</code> method appends an &quot;end of line&quot; character to a string:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::of(&#39;Laravel&#39;)-&gt;newLine()-&gt;append(&#39;Framework&#39;);

// &#39;Laravel
//  Framework&#39;
</code></pre><p><a name="method-fluent-str-padboth"></a></p><h4 id="padboth-collection-method" tabindex="-1"><a class="header-anchor" href="#padboth-collection-method" aria-hidden="true">#</a> <code>padBoth</code> {.collection-method}</h4><p>The <code>padBoth</code> method wraps PHP&#39;s <code>str_pad</code> function, padding both sides of a string with another string until the final string reaches the desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::of(&#39;James&#39;)-&gt;padBoth(10, &#39;_&#39;);

// &#39;__James___&#39;

$padded = Str::of(&#39;James&#39;)-&gt;padBoth(10);

// &#39;  James   &#39;
</code></pre><p><a name="method-fluent-str-padleft"></a></p><h4 id="padleft-collection-method" tabindex="-1"><a class="header-anchor" href="#padleft-collection-method" aria-hidden="true">#</a> <code>padLeft</code> {.collection-method}</h4><p>The <code>padLeft</code> method wraps PHP&#39;s <code>str_pad</code> function, padding the left side of a string with another string until the final string reaches the desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::of(&#39;James&#39;)-&gt;padLeft(10, &#39;-=&#39;);

// &#39;-=-=-James&#39;

$padded = Str::of(&#39;James&#39;)-&gt;padLeft(10);

// &#39;     James&#39;
</code></pre><p><a name="method-fluent-str-padright"></a></p><h4 id="padright-collection-method" tabindex="-1"><a class="header-anchor" href="#padright-collection-method" aria-hidden="true">#</a> <code>padRight</code> {.collection-method}</h4><p>The <code>padRight</code> method wraps PHP&#39;s <code>str_pad</code> function, padding the right side of a string with another string until the final string reaches the desired length:</p><pre><code>use Illuminate\\Support\\Str;

$padded = Str::of(&#39;James&#39;)-&gt;padRight(10, &#39;-&#39;);

// &#39;James-----&#39;

$padded = Str::of(&#39;James&#39;)-&gt;padRight(10);

// &#39;James     &#39;
</code></pre><p><a name="method-fluent-str-pipe"></a></p><h4 id="pipe-collection-method" tabindex="-1"><a class="header-anchor" href="#pipe-collection-method" aria-hidden="true">#</a> <code>pipe</code> {.collection-method}</h4><p>The <code>pipe</code> method allows you to transform the string by passing its current value to the given callable:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$hash = Str::of(&#39;Laravel&#39;)-&gt;pipe(&#39;md5&#39;)-&gt;prepend(&#39;Checksum: &#39;);

// &#39;Checksum: a5c95b86291ea299fcbe64458ed12702&#39;

$closure = Str::of(&#39;foo&#39;)-&gt;pipe(function (Stringable $str) {
    return &#39;bar&#39;;
});

// &#39;bar&#39;
</code></pre><p><a name="method-fluent-str-plural"></a></p><h4 id="plural-collection-method" tabindex="-1"><a class="header-anchor" href="#plural-collection-method" aria-hidden="true">#</a> <code>plural</code> {.collection-method}</h4><p>The <code>plural</code> method converts a singular word string to its plural form. This function supports <a href="./localization#pluralization-language">any of the languages support by Laravel&#39;s pluralizer</a>:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::of(&#39;car&#39;)-&gt;plural();

// cars

$plural = Str::of(&#39;child&#39;)-&gt;plural();

// children
</code></pre><p>You may provide an integer as a second argument to the function to retrieve the singular or plural form of the string:</p><pre><code>use Illuminate\\Support\\Str;

$plural = Str::of(&#39;child&#39;)-&gt;plural(2);

// children

$plural = Str::of(&#39;child&#39;)-&gt;plural(1);

// child
</code></pre><p><a name="method-fluent-str-position"></a></p><h4 id="position-collection-method" tabindex="-1"><a class="header-anchor" href="#position-collection-method" aria-hidden="true">#</a> <code>position</code> {.collection-method}</h4><p>The <code>position</code> method returns the position of the first occurrence of a substring in a string. If the substring does not exist within the string, <code>false</code> is returned:</p><pre><code>use Illuminate\\Support\\Str;

$position = Str::of(&#39;Hello, World!&#39;)-&gt;position(&#39;Hello&#39;);

// 0

$position = Str::of(&#39;Hello, World!&#39;)-&gt;position(&#39;W&#39;);

// 7
</code></pre><p><a name="method-fluent-str-prepend"></a></p><h4 id="prepend-collection-method" tabindex="-1"><a class="header-anchor" href="#prepend-collection-method" aria-hidden="true">#</a> <code>prepend</code> {.collection-method}</h4><p>The <code>prepend</code> method prepends the given values onto the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Framework&#39;)-&gt;prepend(&#39;Laravel &#39;);

// Laravel Framework
</code></pre><p><a name="method-fluent-str-remove"></a></p><h4 id="remove-collection-method" tabindex="-1"><a class="header-anchor" href="#remove-collection-method" aria-hidden="true">#</a> <code>remove</code> {.collection-method}</h4><p>The <code>remove</code> method removes the given value or array of values from the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Arkansas is quite beautiful!&#39;)-&gt;remove(&#39;quite&#39;);

// Arkansas is beautiful!
</code></pre><p>You may also pass <code>false</code> as a second parameter to ignore case when removing strings.</p><p><a name="method-fluent-str-repeat"></a></p><h4 id="repeat-collection-method" tabindex="-1"><a class="header-anchor" href="#repeat-collection-method" aria-hidden="true">#</a> <code>repeat</code> {.collection-method}</h4><p>The <code>repeat</code> method repeats the given string:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Str</span><span class="token punctuation">;</span>

<span class="token variable">$repeated</span> <span class="token operator">=</span> <span class="token class-name static-context">Str</span><span class="token operator">::</span><span class="token function">of</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;a&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">repeat</span><span class="token punctuation">(</span><span class="token number">5</span><span class="token punctuation">)</span><span class="token punctuation">;</span>

<span class="token comment">// aaaaa</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="method-fluent-str-replace"></a></p><h4 id="replace-collection-method" tabindex="-1"><a class="header-anchor" href="#replace-collection-method" aria-hidden="true">#</a> <code>replace</code> {.collection-method}</h4><p>The <code>replace</code> method replaces a given string within the string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;Laravel 6.x&#39;)-&gt;replace(&#39;6.x&#39;, &#39;7.x&#39;);

// Laravel 7.x
</code></pre><p>The <code>replace</code> method also accepts a <code>caseSensitive</code> argument. By default, the <code>replace</code> method is case sensitive:</p><pre><code>$replaced = Str::of(&#39;macOS 13.x&#39;)-&gt;replace(
    &#39;macOS&#39;, &#39;iOS&#39;, caseSensitive: false
);
</code></pre><p><a name="method-fluent-str-replace-array"></a></p><h4 id="replacearray-collection-method" tabindex="-1"><a class="header-anchor" href="#replacearray-collection-method" aria-hidden="true">#</a> <code>replaceArray</code> {.collection-method}</h4><p>The <code>replaceArray</code> method replaces a given value in the string sequentially using an array:</p><pre><code>use Illuminate\\Support\\Str;

$string = &#39;The event will take place between ? and ?&#39;;

$replaced = Str::of($string)-&gt;replaceArray(&#39;?&#39;, [&#39;8:30&#39;, &#39;9:00&#39;]);

// The event will take place between 8:30 and 9:00
</code></pre><p><a name="method-fluent-str-replace-first"></a></p><h4 id="replacefirst-collection-method" tabindex="-1"><a class="header-anchor" href="#replacefirst-collection-method" aria-hidden="true">#</a> <code>replaceFirst</code> {.collection-method}</h4><p>The <code>replaceFirst</code> method replaces the first occurrence of a given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;the quick brown fox jumps over the lazy dog&#39;)-&gt;replaceFirst(&#39;the&#39;, &#39;a&#39;);

// a quick brown fox jumps over the lazy dog
</code></pre><p><a name="method-fluent-str-replace-last"></a></p><h4 id="replacelast-collection-method" tabindex="-1"><a class="header-anchor" href="#replacelast-collection-method" aria-hidden="true">#</a> <code>replaceLast</code> {.collection-method}</h4><p>The <code>replaceLast</code> method replaces the last occurrence of a given value in a string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;the quick brown fox jumps over the lazy dog&#39;)-&gt;replaceLast(&#39;the&#39;, &#39;a&#39;);

// the quick brown fox jumps over a lazy dog
</code></pre><p><a name="method-fluent-str-replace-matches"></a></p><h4 id="replacematches-collection-method" tabindex="-1"><a class="header-anchor" href="#replacematches-collection-method" aria-hidden="true">#</a> <code>replaceMatches</code> {.collection-method}</h4><p>The <code>replaceMatches</code> method replaces all portions of a string matching a pattern with the given replacement string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;(+1) 501-555-1000&#39;)-&gt;replaceMatches(&#39;/[^A-Za-z0-9]++/&#39;, &#39;&#39;)

// &#39;15015551000&#39;
</code></pre><p>The <code>replaceMatches</code> method also accepts a closure that will be invoked with each portion of the string matching the given pattern, allowing you to perform the replacement logic within the closure and return the replaced value:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;123&#39;)-&gt;replaceMatches(&#39;/\\d/&#39;, function (array $matches) {
    return &#39;[&#39;.$matches[0].&#39;]&#39;;
});

// &#39;[1][2][3]&#39;
</code></pre><p><a name="method-fluent-str-replace-start"></a></p><h4 id="replacestart-collection-method" tabindex="-1"><a class="header-anchor" href="#replacestart-collection-method" aria-hidden="true">#</a> <code>replaceStart</code> {.collection-method}</h4><p>The <code>replaceStart</code> method replaces the first occurrence of the given value only if the value appears at the start of the string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;Hello World&#39;)-&gt;replaceStart(&#39;Hello&#39;, &#39;Laravel&#39;);

// Laravel World

$replaced = Str::of(&#39;Hello World&#39;)-&gt;replaceStart(&#39;World&#39;, &#39;Laravel&#39;);

// Hello World
</code></pre><p><a name="method-fluent-str-replace-end"></a></p><h4 id="replaceend-collection-method" tabindex="-1"><a class="header-anchor" href="#replaceend-collection-method" aria-hidden="true">#</a> <code>replaceEnd</code> {.collection-method}</h4><p>The <code>replaceEnd</code> method replaces the last occurrence of the given value only if the value appears at the end of the string:</p><pre><code>use Illuminate\\Support\\Str;

$replaced = Str::of(&#39;Hello World&#39;)-&gt;replaceEnd(&#39;World&#39;, &#39;Laravel&#39;);

// Hello Laravel

$replaced = Str::of(&#39;Hello World&#39;)-&gt;replaceEnd(&#39;Hello&#39;, &#39;Laravel&#39;);

// Hello World
</code></pre><p><a name="method-fluent-str-rtrim"></a></p><h4 id="rtrim-collection-method" tabindex="-1"><a class="header-anchor" href="#rtrim-collection-method" aria-hidden="true">#</a> <code>rtrim</code> {.collection-method}</h4><p>The <code>rtrim</code> method trims the right side of the given string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;  Laravel  &#39;)-&gt;rtrim();

// &#39;  Laravel&#39;

$string = Str::of(&#39;/Laravel/&#39;)-&gt;rtrim(&#39;/&#39;);

// &#39;/Laravel&#39;
</code></pre><p><a name="method-fluent-str-scan"></a></p><h4 id="scan-collection-method" tabindex="-1"><a class="header-anchor" href="#scan-collection-method" aria-hidden="true">#</a> <code>scan</code> {.collection-method}</h4>`,165),L=e("code",null,"scan",-1),_={href:"https://www.php.net/manual/en/function.sscanf.php",target:"_blank",rel:"noopener noreferrer"},q=e("code",null,"sscanf",-1),U=n(`<pre><code>use Illuminate\\Support\\Str;

$collection = Str::of(&#39;filename.jpg&#39;)-&gt;scan(&#39;%[^.].%s&#39;);

// collect([&#39;filename&#39;, &#39;jpg&#39;])
</code></pre><p><a name="method-fluent-str-singular"></a></p><h4 id="singular-collection-method" tabindex="-1"><a class="header-anchor" href="#singular-collection-method" aria-hidden="true">#</a> <code>singular</code> {.collection-method}</h4><p>The <code>singular</code> method converts a string to its singular form. This function supports <a href="./localization#pluralization-language">any of the languages support by Laravel&#39;s pluralizer</a>:</p><pre><code>use Illuminate\\Support\\Str;

$singular = Str::of(&#39;cars&#39;)-&gt;singular();

// car

$singular = Str::of(&#39;children&#39;)-&gt;singular();

// child
</code></pre><p><a name="method-fluent-str-slug"></a></p><h4 id="slug-collection-method" tabindex="-1"><a class="header-anchor" href="#slug-collection-method" aria-hidden="true">#</a> <code>slug</code> {.collection-method}</h4><p>The <code>slug</code> method generates a URL friendly &quot;slug&quot; from the given string:</p><pre><code>use Illuminate\\Support\\Str;

$slug = Str::of(&#39;Laravel Framework&#39;)-&gt;slug(&#39;-&#39;);

// laravel-framework
</code></pre><p><a name="method-fluent-str-snake"></a></p><h4 id="snake-collection-method" tabindex="-1"><a class="header-anchor" href="#snake-collection-method" aria-hidden="true">#</a> <code>snake</code> {.collection-method}</h4><p>The <code>snake</code> method converts the given string to <code>snake_case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;fooBar&#39;)-&gt;snake();

// foo_bar
</code></pre><p><a name="method-fluent-str-split"></a></p><h4 id="split-collection-method" tabindex="-1"><a class="header-anchor" href="#split-collection-method" aria-hidden="true">#</a> <code>split</code> {.collection-method}</h4><p>The <code>split</code> method splits a string into a collection using a regular expression:</p><pre><code>use Illuminate\\Support\\Str;

$segments = Str::of(&#39;one, two, three&#39;)-&gt;split(&#39;/[\\s,]+/&#39;);

// collect([&quot;one&quot;, &quot;two&quot;, &quot;three&quot;])
</code></pre><p><a name="method-fluent-str-squish"></a></p><h4 id="squish-collection-method" tabindex="-1"><a class="header-anchor" href="#squish-collection-method" aria-hidden="true">#</a> <code>squish</code> {.collection-method}</h4><p>The <code>squish</code> method removes all extraneous white space from a string, including extraneous white space between words:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;    laravel    framework    &#39;)-&gt;squish();

// laravel framework
</code></pre><p><a name="method-fluent-str-start"></a></p><h4 id="start-collection-method" tabindex="-1"><a class="header-anchor" href="#start-collection-method" aria-hidden="true">#</a> <code>start</code> {.collection-method}</h4><p>The <code>start</code> method adds a single instance of the given value to a string if it does not already start with that value:</p><pre><code>use Illuminate\\Support\\Str;

$adjusted = Str::of(&#39;this/string&#39;)-&gt;start(&#39;/&#39;);

// /this/string

$adjusted = Str::of(&#39;/this/string&#39;)-&gt;start(&#39;/&#39;);

// /this/string
</code></pre><p><a name="method-fluent-str-starts-with"></a></p><h4 id="startswith-collection-method" tabindex="-1"><a class="header-anchor" href="#startswith-collection-method" aria-hidden="true">#</a> <code>startsWith</code> {.collection-method}</h4><p>The <code>startsWith</code> method determines if the given string begins with the given value:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;This is my name&#39;)-&gt;startsWith(&#39;This&#39;);

// true
</code></pre><p><a name="method-fluent-str-studly"></a></p><h4 id="studly-collection-method" tabindex="-1"><a class="header-anchor" href="#studly-collection-method" aria-hidden="true">#</a> <code>studly</code> {.collection-method}</h4><p>The <code>studly</code> method converts the given string to <code>StudlyCase</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;foo_bar&#39;)-&gt;studly();

// FooBar
</code></pre><p><a name="method-fluent-str-substr"></a></p><h4 id="substr-collection-method" tabindex="-1"><a class="header-anchor" href="#substr-collection-method" aria-hidden="true">#</a> <code>substr</code> {.collection-method}</h4><p>The <code>substr</code> method returns the portion of the string specified by the given start and length parameters:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Laravel Framework&#39;)-&gt;substr(8);

// Framework

$string = Str::of(&#39;Laravel Framework&#39;)-&gt;substr(8, 5);

// Frame
</code></pre><p><a name="method-fluent-str-substrreplace"></a></p><h4 id="substrreplace-collection-method" tabindex="-1"><a class="header-anchor" href="#substrreplace-collection-method" aria-hidden="true">#</a> <code>substrReplace</code> {.collection-method}</h4><p>The <code>substrReplace</code> method replaces text within a portion of a string, starting at the position specified by the second argument and replacing the number of characters specified by the third argument. Passing <code>0</code> to the method&#39;s third argument will insert the string at the specified position without replacing any of the existing characters in the string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;1300&#39;)-&gt;substrReplace(&#39;:&#39;, 2);

// 13:

$string = Str::of(&#39;The Framework&#39;)-&gt;substrReplace(&#39; Laravel&#39;, 3, 0);

// The Laravel Framework
</code></pre><p><a name="method-fluent-str-swap"></a></p><h4 id="swap-collection-method" tabindex="-1"><a class="header-anchor" href="#swap-collection-method" aria-hidden="true">#</a> <code>swap</code> {.collection-method}</h4><p>The <code>swap</code> method replaces multiple values in the string using PHP&#39;s <code>strtr</code> function:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Tacos are great!&#39;)
    -&gt;swap([
        &#39;Tacos&#39; =&gt; &#39;Burritos&#39;,
        &#39;great&#39; =&gt; &#39;fantastic&#39;,
    ]);

// Burritos are fantastic!
</code></pre><p><a name="method-fluent-str-take"></a></p><h4 id="take-collection-method" tabindex="-1"><a class="header-anchor" href="#take-collection-method" aria-hidden="true">#</a> <code>take</code> {.collection-method}</h4><p>The <code>take</code> method returns a specified number of characters from the beginning of the string:</p><pre><code>use Illuminate\\Support\\Str;

$taken = Str::of(&#39;Build something amazing!&#39;)-&gt;take(5);

// Build
</code></pre><p><a name="method-fluent-str-tap"></a></p><h4 id="tap-collection-method" tabindex="-1"><a class="header-anchor" href="#tap-collection-method" aria-hidden="true">#</a> <code>tap</code> {.collection-method}</h4><p>The <code>tap</code> method passes the string to the given closure, allowing you to examine and interact with the string while not affecting the string itself. The original string is returned by the <code>tap</code> method regardless of what is returned by the closure:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;Laravel&#39;)
    -&gt;append(&#39; Framework&#39;)
    -&gt;tap(function (Stringable $string) {
        dump(&#39;String after append: &#39;.$string);
    })
    -&gt;upper();

// LARAVEL FRAMEWORK
</code></pre><p><a name="method-fluent-str-test"></a></p><h4 id="test-collection-method" tabindex="-1"><a class="header-anchor" href="#test-collection-method" aria-hidden="true">#</a> <code>test</code> {.collection-method}</h4><p>The <code>test</code> method determines if a string matches the given regular expression pattern:</p><pre><code>use Illuminate\\Support\\Str;

$result = Str::of(&#39;Laravel Framework&#39;)-&gt;test(&#39;/Laravel/&#39;);

// true
</code></pre><p><a name="method-fluent-str-title"></a></p><h4 id="title-collection-method" tabindex="-1"><a class="header-anchor" href="#title-collection-method" aria-hidden="true">#</a> <code>title</code> {.collection-method}</h4><p>The <code>title</code> method converts the given string to <code>Title Case</code>:</p><pre><code>use Illuminate\\Support\\Str;

$converted = Str::of(&#39;a nice title uses the correct case&#39;)-&gt;title();

// A Nice Title Uses The Correct Case
</code></pre><p><a name="method-fluent-str-trim"></a></p><h4 id="trim-collection-method" tabindex="-1"><a class="header-anchor" href="#trim-collection-method" aria-hidden="true">#</a> <code>trim</code> {.collection-method}</h4><p>The <code>trim</code> method trims the given string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;  Laravel  &#39;)-&gt;trim();

// &#39;Laravel&#39;

$string = Str::of(&#39;/Laravel/&#39;)-&gt;trim(&#39;/&#39;);

// &#39;Laravel&#39;
</code></pre><p><a name="method-fluent-str-ucfirst"></a></p><h4 id="ucfirst-collection-method" tabindex="-1"><a class="header-anchor" href="#ucfirst-collection-method" aria-hidden="true">#</a> <code>ucfirst</code> {.collection-method}</h4><p>The <code>ucfirst</code> method returns the given string with the first character capitalized:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;foo bar&#39;)-&gt;ucfirst();

// Foo bar
</code></pre><p><a name="method-fluent-str-ucsplit"></a></p><h4 id="ucsplit-collection-method" tabindex="-1"><a class="header-anchor" href="#ucsplit-collection-method" aria-hidden="true">#</a> <code>ucsplit</code> {.collection-method}</h4><p>The <code>ucsplit</code> method splits the given string into a collection by uppercase characters:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Foo Bar&#39;)-&gt;ucsplit();

// collect([&#39;Foo&#39;, &#39;Bar&#39;])
</code></pre><p><a name="method-fluent-str-upper"></a></p><h4 id="upper-collection-method" tabindex="-1"><a class="header-anchor" href="#upper-collection-method" aria-hidden="true">#</a> <code>upper</code> {.collection-method}</h4><p>The <code>upper</code> method converts the given string to uppercase:</p><pre><code>use Illuminate\\Support\\Str;

$adjusted = Str::of(&#39;laravel&#39;)-&gt;upper();

// LARAVEL
</code></pre><p><a name="method-fluent-str-when"></a></p><h4 id="when-collection-method" tabindex="-1"><a class="header-anchor" href="#when-collection-method" aria-hidden="true">#</a> <code>when</code> {.collection-method}</h4><p>The <code>when</code> method invokes the given closure if a given condition is <code>true</code>. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;Taylor&#39;)
                -&gt;when(true, function (Stringable $string) {
                    return $string-&gt;append(&#39; Otwell&#39;);
                });

// &#39;Taylor Otwell&#39;
</code></pre><p>If necessary, you may pass another closure as the third parameter to the <code>when</code> method. This closure will execute if the condition parameter evaluates to <code>false</code>.</p><p><a name="method-fluent-str-when-contains"></a></p><h4 id="whencontains-collection-method" tabindex="-1"><a class="header-anchor" href="#whencontains-collection-method" aria-hidden="true">#</a> <code>whenContains</code> {.collection-method}</h4><p>The <code>whenContains</code> method invokes the given closure if the string contains the given value. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;tony stark&#39;)
            -&gt;whenContains(&#39;tony&#39;, function (Stringable $string) {
                return $string-&gt;title();
            });

// &#39;Tony Stark&#39;
</code></pre><p>If necessary, you may pass another closure as the third parameter to the <code>when</code> method. This closure will execute if the string does not contain the given value.</p><p>You may also pass an array of values to determine if the given string contains any of the values in the array:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;tony stark&#39;)
            -&gt;whenContains([&#39;tony&#39;, &#39;hulk&#39;], function (Stringable $string) {
                return $string-&gt;title();
            });

// Tony Stark
</code></pre><p><a name="method-fluent-str-when-contains-all"></a></p><h4 id="whencontainsall-collection-method" tabindex="-1"><a class="header-anchor" href="#whencontainsall-collection-method" aria-hidden="true">#</a> <code>whenContainsAll</code> {.collection-method}</h4><p>The <code>whenContainsAll</code> method invokes the given closure if the string contains all of the given sub-strings. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;tony stark&#39;)
                -&gt;whenContainsAll([&#39;tony&#39;, &#39;stark&#39;], function (Stringable $string) {
                    return $string-&gt;title();
                });

// &#39;Tony Stark&#39;
</code></pre><p>If necessary, you may pass another closure as the third parameter to the <code>when</code> method. This closure will execute if the condition parameter evaluates to <code>false</code>.</p><p><a name="method-fluent-str-when-empty"></a></p><h4 id="whenempty-collection-method" tabindex="-1"><a class="header-anchor" href="#whenempty-collection-method" aria-hidden="true">#</a> <code>whenEmpty</code> {.collection-method}</h4><p>The <code>whenEmpty</code> method invokes the given closure if the string is empty. If the closure returns a value, that value will also be returned by the <code>whenEmpty</code> method. If the closure does not return a value, the fluent string instance will be returned:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;  &#39;)-&gt;whenEmpty(function (Stringable $string) {
    return $string-&gt;trim()-&gt;prepend(&#39;Laravel&#39;);
});

// &#39;Laravel&#39;
</code></pre><p><a name="method-fluent-str-when-not-empty"></a></p><h4 id="whennotempty-collection-method" tabindex="-1"><a class="header-anchor" href="#whennotempty-collection-method" aria-hidden="true">#</a> <code>whenNotEmpty</code> {.collection-method}</h4><p>The <code>whenNotEmpty</code> method invokes the given closure if the string is not empty. If the closure returns a value, that value will also be returned by the <code>whenNotEmpty</code> method. If the closure does not return a value, the fluent string instance will be returned:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;Framework&#39;)-&gt;whenNotEmpty(function (Stringable $string) {
    return $string-&gt;prepend(&#39;Laravel &#39;);
});

// &#39;Laravel Framework&#39;
</code></pre><p><a name="method-fluent-str-when-starts-with"></a></p><h4 id="whenstartswith-collection-method" tabindex="-1"><a class="header-anchor" href="#whenstartswith-collection-method" aria-hidden="true">#</a> <code>whenStartsWith</code> {.collection-method}</h4><p>The <code>whenStartsWith</code> method invokes the given closure if the string starts with the given sub-string. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;disney world&#39;)-&gt;whenStartsWith(&#39;disney&#39;, function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Disney World&#39;
</code></pre><p><a name="method-fluent-str-when-ends-with"></a></p><h4 id="whenendswith-collection-method" tabindex="-1"><a class="header-anchor" href="#whenendswith-collection-method" aria-hidden="true">#</a> <code>whenEndsWith</code> {.collection-method}</h4><p>The <code>whenEndsWith</code> method invokes the given closure if the string ends with the given sub-string. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;disney world&#39;)-&gt;whenEndsWith(&#39;world&#39;, function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Disney World&#39;
</code></pre><p><a name="method-fluent-str-when-exactly"></a></p><h4 id="whenexactly-collection-method" tabindex="-1"><a class="header-anchor" href="#whenexactly-collection-method" aria-hidden="true">#</a> <code>whenExactly</code> {.collection-method}</h4><p>The <code>whenExactly</code> method invokes the given closure if the string exactly matches the given string. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;laravel&#39;)-&gt;whenExactly(&#39;laravel&#39;, function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Laravel&#39;
</code></pre><p><a name="method-fluent-str-when-not-exactly"></a></p><h4 id="whennotexactly-collection-method" tabindex="-1"><a class="header-anchor" href="#whennotexactly-collection-method" aria-hidden="true">#</a> <code>whenNotExactly</code> {.collection-method}</h4><p>The <code>whenNotExactly</code> method invokes the given closure if the string does not exactly match the given string. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;framework&#39;)-&gt;whenNotExactly(&#39;laravel&#39;, function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Framework&#39;
</code></pre><p><a name="method-fluent-str-when-is"></a></p><h4 id="whenis-collection-method" tabindex="-1"><a class="header-anchor" href="#whenis-collection-method" aria-hidden="true">#</a> <code>whenIs</code> {.collection-method}</h4><p>The <code>whenIs</code> method invokes the given closure if the string matches a given pattern. Asterisks may be used as wildcard values. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;foo/bar&#39;)-&gt;whenIs(&#39;foo/*&#39;, function (Stringable $string) {
    return $string-&gt;append(&#39;/baz&#39;);
});

// &#39;foo/bar/baz&#39;
</code></pre><p><a name="method-fluent-str-when-is-ascii"></a></p><h4 id="whenisascii-collection-method" tabindex="-1"><a class="header-anchor" href="#whenisascii-collection-method" aria-hidden="true">#</a> <code>whenIsAscii</code> {.collection-method}</h4><p>The <code>whenIsAscii</code> method invokes the given closure if the string is 7 bit ASCII. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;laravel&#39;)-&gt;whenIsAscii(function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Laravel&#39;
</code></pre><p><a name="method-fluent-str-when-is-ulid"></a></p><h4 id="whenisulid-collection-method" tabindex="-1"><a class="header-anchor" href="#whenisulid-collection-method" aria-hidden="true">#</a> <code>whenIsUlid</code> {.collection-method}</h4><p>The <code>whenIsUlid</code> method invokes the given closure if the string is a valid ULID. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;01gd6r360bp37zj17nxb55yv40&#39;)-&gt;whenIsUlid(function (Stringable $string) {
    return $string-&gt;substr(0, 8);
});

// &#39;01gd6r36&#39;
</code></pre><p><a name="method-fluent-str-when-is-uuid"></a></p><h4 id="whenisuuid-collection-method" tabindex="-1"><a class="header-anchor" href="#whenisuuid-collection-method" aria-hidden="true">#</a> <code>whenIsUuid</code> {.collection-method}</h4><p>The <code>whenIsUuid</code> method invokes the given closure if the string is a valid UUID. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;a0a2a2d2-0b87-4a18-83f2-2529882be2de&#39;)-&gt;whenIsUuid(function (Stringable $string) {
    return $string-&gt;substr(0, 8);
});

// &#39;a0a2a2d2&#39;
</code></pre><p><a name="method-fluent-str-when-test"></a></p><h4 id="whentest-collection-method" tabindex="-1"><a class="header-anchor" href="#whentest-collection-method" aria-hidden="true">#</a> <code>whenTest</code> {.collection-method}</h4><p>The <code>whenTest</code> method invokes the given closure if the string matches the given regular expression. The closure will receive the fluent string instance:</p><pre><code>use Illuminate\\Support\\Str;
use Illuminate\\Support\\Stringable;

$string = Str::of(&#39;laravel framework&#39;)-&gt;whenTest(&#39;/laravel/&#39;, function (Stringable $string) {
    return $string-&gt;title();
});

// &#39;Laravel Framework&#39;
</code></pre><p><a name="method-fluent-str-word-count"></a></p><h4 id="wordcount-collection-method" tabindex="-1"><a class="header-anchor" href="#wordcount-collection-method" aria-hidden="true">#</a> <code>wordCount</code> {.collection-method}</h4><p>The <code>wordCount</code> method returns the number of words that a string contains:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Support<span class="token punctuation">\\</span>Str</span><span class="token punctuation">;</span>

<span class="token class-name static-context">Str</span><span class="token operator">::</span><span class="token function">of</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;Hello, world!&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">wordCount</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span> <span class="token comment">// 2</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="method-fluent-str-words"></a></p><h4 id="words-collection-method" tabindex="-1"><a class="header-anchor" href="#words-collection-method" aria-hidden="true">#</a> <code>words</code> {.collection-method}</h4><p>The <code>words</code> method limits the number of words in a string. If necessary, you may specify an additional string that will be appended to the truncated string:</p><pre><code>use Illuminate\\Support\\Str;

$string = Str::of(&#39;Perfectly balanced, as all things should be.&#39;)-&gt;words(3, &#39; &gt;&gt;&gt;&#39;);

// Perfectly balanced, as &gt;&gt;&gt;
</code></pre>`,146);function A(H,W){const o=i("ExternalLinkIcon");return d(),s("div",null,[l,h,p,u,m,f,e("p",null,[t("The "),g,t(" method converts GitHub flavored Markdown into inline HTML using "),e("a",S,[t("CommonMark"),a(o)]),t(". However, unlike the "),b,t(" method, it does not wrap all generated HTML in a block-level element:")]),v,e("p",null,[t("The "),w,t(" method converts GitHub flavored Markdown into HTML using "),e("a",y,[t("CommonMark"),a(o)]),t(":")]),k,e("p",null,[t("The "),x,t(" method converts GitHub flavored Markdown into inline HTML using "),e("a",T,[t("CommonMark"),a(o)]),t(". However, unlike the "),$,t(" method, it does not wrap all generated HTML in a block-level element:")]),I,e("p",null,[t("The "),L,t(" method parses input from a string into a collection according to a format supported by the "),e("a",_,[q,t(" PHP function"),a(o)]),t(":")]),U])}const F=r(c,[["render",A],["__file","strings.html.vue"]]);export{F as default};
