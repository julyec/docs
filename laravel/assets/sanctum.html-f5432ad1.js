import{_ as o}from"./plugin-vue_export-helper-c27b6911.js";import{r as s,o as i,c as r,b as e,d as a,e as c,a as t}from"./app-06635a3b.js";const u={},p=t('<h1 id="laravel-sanctum" tabindex="-1"><a class="header-anchor" href="#laravel-sanctum" aria-hidden="true">#</a> Laravel Sanctum</h1><ul><li><a href="#introduction">Introduction</a><ul><li><a href="#how-it-works">How It Works</a></li></ul></li><li><a href="#installation">Installation</a></li><li><a href="#configuration">Configuration</a><ul><li><a href="#overriding-default-models">Overriding Default Models</a></li></ul></li><li><a href="#api-token-authentication">API Token Authentication</a><ul><li><a href="#issuing-api-tokens">Issuing API Tokens</a></li><li><a href="#token-abilities">Token Abilities</a></li><li><a href="#protecting-routes">Protecting Routes</a></li><li><a href="#revoking-tokens">Revoking Tokens</a></li><li><a href="#token-expiration">Token Expiration</a></li></ul></li><li><a href="#spa-authentication">SPA Authentication</a><ul><li><a href="#spa-configuration">Configuration</a></li><li><a href="#spa-authenticating">Authenticating</a></li><li><a href="#protecting-spa-routes">Protecting Routes</a></li><li><a href="#authorizing-private-broadcast-channels">Authorizing Private Broadcast Channels</a></li></ul></li><li><a href="#mobile-application-authentication">Mobile Application Authentication</a><ul><li><a href="#issuing-mobile-api-tokens">Issuing API Tokens</a></li><li><a href="#protecting-mobile-api-routes">Protecting Routes</a></li><li><a href="#revoking-mobile-api-tokens">Revoking Tokens</a></li></ul></li><li><a href="#testing">Testing</a></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2>',4),l={href:"https://github.com/laravel/sanctum",target:"_blank",rel:"noopener noreferrer"},d=t(`<p><a name="how-it-works"></a></p><h3 id="how-it-works" tabindex="-1"><a class="header-anchor" href="#how-it-works" aria-hidden="true">#</a> How It Works</h3><p>Laravel Sanctum exists to solve two separate problems. Let&#39;s discuss each before digging deeper into the library.</p><p><a name="how-it-works-api-tokens"></a></p><h4 id="api-tokens" tabindex="-1"><a class="header-anchor" href="#api-tokens" aria-hidden="true">#</a> API Tokens</h4><p>First, Sanctum is a simple package you may use to issue API tokens to your users without the complication of OAuth. This feature is inspired by GitHub and other applications which issue &quot;personal access tokens&quot;. For example, imagine the &quot;account settings&quot; of your application has a screen where a user may generate an API token for their account. You may use Sanctum to generate and manage those tokens. These tokens typically have a very long expiration time (years), but may be manually revoked by the user at anytime.</p><p>Laravel Sanctum offers this feature by storing user API tokens in a single database table and authenticating incoming HTTP requests via the <code>Authorization</code> header which should contain a valid API token.</p><p><a name="how-it-works-spa-authentication"></a></p><h4 id="spa-authentication" tabindex="-1"><a class="header-anchor" href="#spa-authentication" aria-hidden="true">#</a> SPA Authentication</h4><p>Second, Sanctum exists to offer a simple way to authenticate single page applications (SPAs) that need to communicate with a Laravel powered API. These SPAs might exist in the same repository as your Laravel application or might be an entirely separate repository, such as a SPA created using Vue CLI or a Next.js application.</p><p>For this feature, Sanctum does not use tokens of any kind. Instead, Sanctum uses Laravel&#39;s built-in cookie based session authentication services. Typically, Sanctum utilizes Laravel&#39;s <code>web</code> authentication guard to accomplish this. This provides the benefits of CSRF protection, session authentication, as well as protects against leakage of the authentication credentials via XSS.</p><p>Sanctum will only attempt to authenticate using cookies when the incoming request originates from your own SPA frontend. When Sanctum examines an incoming HTTP request, it will first check for an authentication cookie and, if none is present, Sanctum will then examine the <code>Authorization</code> header for a valid API token.</p><blockquote><p><strong>Note</strong><br> It is perfectly fine to use Sanctum only for API token authentication or only for SPA authentication. Just because you use Sanctum does not mean you are required to use both features it offers.</p></blockquote><p><a name="installation"></a></p><h2 id="installation" tabindex="-1"><a class="header-anchor" href="#installation" aria-hidden="true">#</a> Installation</h2><blockquote><p><strong>Note</strong><br> The most recent versions of Laravel already include Laravel Sanctum. However, if your application&#39;s <code>composer.json</code> file does not include <code>laravel/sanctum</code>, you may follow the installation instructions below.</p></blockquote><p>You may install Laravel Sanctum via the Composer package manager:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code><span class="token function">composer</span> require laravel/sanctum
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Next, you should publish the Sanctum configuration and migration files using the <code>vendor:publish</code> Artisan command. The <code>sanctum</code> configuration file will be placed in your application&#39;s <code>config</code> directory:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan vendor:publish <span class="token parameter variable">--provider</span><span class="token operator">=</span><span class="token string">&quot;Laravel\\Sanctum\\SanctumServiceProvider&quot;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Finally, you should run your database migrations. Sanctum will create one database table in which to store API tokens:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Next, if you plan to utilize Sanctum to authenticate a SPA, you should add Sanctum&#39;s middleware to your <code>api</code> middleware group within your application&#39;s <code>app/Http/Kernel.php</code> file:</p><pre><code>&#39;api&#39; =&gt; [
    \\Laravel\\Sanctum\\Http\\Middleware\\EnsureFrontendRequestsAreStateful::class,
    \\Illuminate\\Routing\\Middleware\\ThrottleRequests::class.&#39;:api&#39;,
    \\Illuminate\\Routing\\Middleware\\SubstituteBindings::class,
],
</code></pre><p><a name="migration-customization"></a></p><h4 id="migration-customization" tabindex="-1"><a class="header-anchor" href="#migration-customization" aria-hidden="true">#</a> Migration Customization</h4><p>If you are not going to use Sanctum&#39;s default migrations, you should call the <code>Sanctum::ignoreMigrations</code> method in the <code>register</code> method of your <code>App\\Providers\\AppServiceProvider</code> class. You may export the default migrations by executing the following command: <code>php artisan vendor:publish --tag=sanctum-migrations</code></p><p><a name="configuration"></a></p><h2 id="configuration" tabindex="-1"><a class="header-anchor" href="#configuration" aria-hidden="true">#</a> Configuration</h2><p><a name="overriding-default-models"></a></p><h3 id="overriding-default-models" tabindex="-1"><a class="header-anchor" href="#overriding-default-models" aria-hidden="true">#</a> Overriding Default Models</h3><p>Although not typically required, you are free to extend the <code>PersonalAccessToken</code> model used internally by Sanctum:</p><pre><code>use Laravel\\Sanctum\\PersonalAccessToken as SanctumPersonalAccessToken;

class PersonalAccessToken extends SanctumPersonalAccessToken
{
    // ...
}
</code></pre><p>Then, you may instruct Sanctum to use your custom model via the <code>usePersonalAccessTokenModel</code> method provided by Sanctum. Typically, you should call this method in the <code>boot</code> method of one of your application&#39;s service providers:</p><pre><code>use App\\Models\\Sanctum\\PersonalAccessToken;
use Laravel\\Sanctum\\Sanctum;

/**
 * Bootstrap any application services.
 */
public function boot(): void
{
    Sanctum::usePersonalAccessTokenModel(PersonalAccessToken::class);
}
</code></pre><p><a name="api-token-authentication"></a></p><h2 id="api-token-authentication" tabindex="-1"><a class="header-anchor" href="#api-token-authentication" aria-hidden="true">#</a> API Token Authentication</h2><blockquote><p><strong>Note</strong><br> You should not use API tokens to authenticate your own first-party SPA. Instead, use Sanctum&#39;s built-in <a href="#spa-authentication">SPA authentication features</a>.</p></blockquote><p><a name="issuing-api-tokens"></a></p><h3 id="issuing-api-tokens" tabindex="-1"><a class="header-anchor" href="#issuing-api-tokens" aria-hidden="true">#</a> Issuing API Tokens</h3><p>Sanctum allows you to issue API tokens / personal access tokens that may be used to authenticate API requests to your application. When making requests using API tokens, the token should be included in the <code>Authorization</code> header as a <code>Bearer</code> token.</p><p>To begin issuing tokens for users, your User model should use the <code>Laravel\\Sanctum\\HasApiTokens</code> trait:</p><pre><code>use Laravel\\Sanctum\\HasApiTokens;

class User extends Authenticatable
{
    use HasApiTokens, HasFactory, Notifiable;
}
</code></pre><p>To issue a token, you may use the <code>createToken</code> method. The <code>createToken</code> method returns a <code>Laravel\\Sanctum\\NewAccessToken</code> instance. API tokens are hashed using SHA-256 hashing before being stored in your database, but you may access the plain-text value of the token using the <code>plainTextToken</code> property of the <code>NewAccessToken</code> instance. You should display this value to the user immediately after the token has been created:</p><pre><code>use Illuminate\\Http\\Request;

Route::post(&#39;/tokens/create&#39;, function (Request $request) {
    $token = $request-&gt;user()-&gt;createToken($request-&gt;token_name);

    return [&#39;token&#39; =&gt; $token-&gt;plainTextToken];
});
</code></pre><p>You may access all of the user&#39;s tokens using the <code>tokens</code> Eloquent relationship provided by the <code>HasApiTokens</code> trait:</p><pre><code>foreach ($user-&gt;tokens as $token) {
    // ...
}
</code></pre><p><a name="token-abilities"></a></p><h3 id="token-abilities" tabindex="-1"><a class="header-anchor" href="#token-abilities" aria-hidden="true">#</a> Token Abilities</h3><p>Sanctum allows you to assign &quot;abilities&quot; to tokens. Abilities serve a similar purpose as OAuth&#39;s &quot;scopes&quot;. You may pass an array of string abilities as the second argument to the <code>createToken</code> method:</p><pre><code>return $user-&gt;createToken(&#39;token-name&#39;, [&#39;server:update&#39;])-&gt;plainTextToken;
</code></pre><p>When handling an incoming request authenticated by Sanctum, you may determine if the token has a given ability using the <code>tokenCan</code> method:</p><pre><code>if ($user-&gt;tokenCan(&#39;server:update&#39;)) {
    // ...
}
</code></pre><p><a name="token-ability-middleware"></a></p><h4 id="token-ability-middleware" tabindex="-1"><a class="header-anchor" href="#token-ability-middleware" aria-hidden="true">#</a> Token Ability Middleware</h4><p>Sanctum also includes two middleware that may be used to verify that an incoming request is authenticated with a token that has been granted a given ability. To get started, add the following middleware to the <code>$middlewareAliases</code> property of your application&#39;s <code>app/Http/Kernel.php</code> file:</p><pre><code>&#39;abilities&#39; =&gt; \\Laravel\\Sanctum\\Http\\Middleware\\CheckAbilities::class,
&#39;ability&#39; =&gt; \\Laravel\\Sanctum\\Http\\Middleware\\CheckForAnyAbility::class,
</code></pre><p>The <code>abilities</code> middleware may be assigned to a route to verify that the incoming request&#39;s token has all of the listed abilities:</p><pre><code>Route::get(&#39;/orders&#39;, function () {
    // Token has both &quot;check-status&quot; and &quot;place-orders&quot; abilities...
})-&gt;middleware([&#39;auth:sanctum&#39;, &#39;abilities:check-status,place-orders&#39;]);
</code></pre><p>The <code>ability</code> middleware may be assigned to a route to verify that the incoming request&#39;s token has <em>at least one</em> of the listed abilities:</p><pre><code>Route::get(&#39;/orders&#39;, function () {
    // Token has the &quot;check-status&quot; or &quot;place-orders&quot; ability...
})-&gt;middleware([&#39;auth:sanctum&#39;, &#39;ability:check-status,place-orders&#39;]);
</code></pre><p><a name="first-party-ui-initiated-requests"></a></p><h4 id="first-party-ui-initiated-requests" tabindex="-1"><a class="header-anchor" href="#first-party-ui-initiated-requests" aria-hidden="true">#</a> First-Party UI Initiated Requests</h4><p>For convenience, the <code>tokenCan</code> method will always return <code>true</code> if the incoming authenticated request was from your first-party SPA and you are using Sanctum&#39;s built-in <a href="#spa-authentication">SPA authentication</a>.</p><p>However, this does not necessarily mean that your application has to allow the user to perform the action. Typically, your application&#39;s <a href="./authorization#creating-policies">authorization policies</a> will determine if the token has been granted the permission to perform the abilities as well as check that the user instance itself should be allowed to perform the action.</p><p>For example, if we imagine an application that manages servers, this might mean checking that token is authorized to update servers <strong>and</strong> that the server belongs to the user:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">return</span> <span class="token variable">$request</span><span class="token operator">-&gt;</span><span class="token function">user</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token property">id</span> <span class="token operator">===</span> <span class="token variable">$server</span><span class="token operator">-&gt;</span><span class="token property">user_id</span> <span class="token operator">&amp;&amp;</span>
       <span class="token variable">$request</span><span class="token operator">-&gt;</span><span class="token function">user</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">tokenCan</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;server:update&#39;</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>At first, allowing the <code>tokenCan</code> method to be called and always return <code>true</code> for first-party UI initiated requests may seem strange; however, it is convenient to be able to always assume an API token is available and can be inspected via the <code>tokenCan</code> method. By taking this approach, you may always call the <code>tokenCan</code> method within your application&#39;s authorizations policies without worrying about whether the request was triggered from your application&#39;s UI or was initiated by one of your API&#39;s third-party consumers.</p><p><a name="protecting-routes"></a></p><h3 id="protecting-routes" tabindex="-1"><a class="header-anchor" href="#protecting-routes" aria-hidden="true">#</a> Protecting Routes</h3><p>To protect routes so that all incoming requests must be authenticated, you should attach the <code>sanctum</code> authentication guard to your protected routes within your <code>routes/web.php</code> and <code>routes/api.php</code> route files. This guard will ensure that incoming requests are authenticated as either stateful, cookie authenticated requests or contain a valid API token header if the request is from a third party.</p><p>You may be wondering why we suggest that you authenticate the routes within your application&#39;s <code>routes/web.php</code> file using the <code>sanctum</code> guard. Remember, Sanctum will first attempt to authenticate incoming requests using Laravel&#39;s typical session authentication cookie. If that cookie is not present then Sanctum will attempt to authenticate the request using a token in the request&#39;s <code>Authorization</code> header. In addition, authenticating all requests using Sanctum ensures that we may always call the <code>tokenCan</code> method on the currently authenticated user instance:</p><pre><code>use Illuminate\\Http\\Request;

Route::middleware(&#39;auth:sanctum&#39;)-&gt;get(&#39;/user&#39;, function (Request $request) {
    return $request-&gt;user();
});
</code></pre><p><a name="revoking-tokens"></a></p><h3 id="revoking-tokens" tabindex="-1"><a class="header-anchor" href="#revoking-tokens" aria-hidden="true">#</a> Revoking Tokens</h3><p>You may &quot;revoke&quot; tokens by deleting them from your database using the <code>tokens</code> relationship that is provided by the <code>Laravel\\Sanctum\\HasApiTokens</code> trait:</p><pre><code>// Revoke all tokens...
$user-&gt;tokens()-&gt;delete();

// Revoke the token that was used to authenticate the current request...
$request-&gt;user()-&gt;currentAccessToken()-&gt;delete();

// Revoke a specific token...
$user-&gt;tokens()-&gt;where(&#39;id&#39;, $tokenId)-&gt;delete();
</code></pre><p><a name="token-expiration"></a></p><h3 id="token-expiration" tabindex="-1"><a class="header-anchor" href="#token-expiration" aria-hidden="true">#</a> Token Expiration</h3><p>By default, Sanctum tokens never expire and may only be invalidated by <a href="#revoking-tokens">revoking the token</a>. However, if you would like to configure an expiration time for your application&#39;s API tokens, you may do so via the <code>expiration</code> configuration option defined in your application&#39;s <code>sanctum</code> configuration file. This configuration option defines the number of minutes until an issued token will be considered expired:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token string single-quoted-string">&#39;expiration&#39;</span> <span class="token operator">=&gt;</span> <span class="token number">525600</span><span class="token punctuation">,</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you have configured a token expiration time for your application, you may also wish to <a href="./scheduling">schedule a task</a> to prune your application&#39;s expired tokens. Thankfully, Sanctum includes a <code>sanctum:prune-expired</code> Artisan command that you may use to accomplish this. For example, you may configure a scheduled tasks to delete all expired token database records that have been expired for at least 24 hours:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token variable">$schedule</span><span class="token operator">-&gt;</span><span class="token function">command</span><span class="token punctuation">(</span><span class="token string single-quoted-string">&#39;sanctum:prune-expired --hours=24&#39;</span><span class="token punctuation">)</span><span class="token operator">-&gt;</span><span class="token function">daily</span><span class="token punctuation">(</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="spa-authentication"></a></p><h2 id="spa-authentication-1" tabindex="-1"><a class="header-anchor" href="#spa-authentication-1" aria-hidden="true">#</a> SPA Authentication</h2><p>Sanctum also exists to provide a simple method of authenticating single page applications (SPAs) that need to communicate with a Laravel powered API. These SPAs might exist in the same repository as your Laravel application or might be an entirely separate repository.</p><p>For this feature, Sanctum does not use tokens of any kind. Instead, Sanctum uses Laravel&#39;s built-in cookie based session authentication services. This approach to authentication provides the benefits of CSRF protection, session authentication, as well as protects against leakage of the authentication credentials via XSS.</p><blockquote><p><strong>Warning</strong><br> In order to authenticate, your SPA and API must share the same top-level domain. However, they may be placed on different subdomains. Additionally, you should ensure that you send the <code>Accept: application/json</code> header and either the <code>Referer</code> or <code>Origin</code> header with your request.</p></blockquote><p><a name="spa-configuration"></a></p><h3 id="configuration-1" tabindex="-1"><a class="header-anchor" href="#configuration-1" aria-hidden="true">#</a> Configuration</h3><p><a name="configuring-your-first-party-domains"></a></p><h4 id="configuring-your-first-party-domains" tabindex="-1"><a class="header-anchor" href="#configuring-your-first-party-domains" aria-hidden="true">#</a> Configuring Your First-Party Domains</h4><p>First, you should configure which domains your SPA will be making requests from. You may configure these domains using the <code>stateful</code> configuration option in your <code>sanctum</code> configuration file. This configuration setting determines which domains will maintain &quot;stateful&quot; authentication using Laravel session cookies when making requests to your API.</p><blockquote><p><strong>Warning</strong><br> If you are accessing your application via a URL that includes a port (<code>127.0.0.1:8000</code>), you should ensure that you include the port number with the domain.</p></blockquote><p><a name="sanctum-middleware"></a></p><h4 id="sanctum-middleware" tabindex="-1"><a class="header-anchor" href="#sanctum-middleware" aria-hidden="true">#</a> Sanctum Middleware</h4><p>Next, you should add Sanctum&#39;s middleware to your <code>api</code> middleware group within your <code>app/Http/Kernel.php</code> file. This middleware is responsible for ensuring that incoming requests from your SPA can authenticate using Laravel&#39;s session cookies, while still allowing requests from third parties or mobile applications to authenticate using API tokens:</p><pre><code>&#39;api&#39; =&gt; [
    \\Laravel\\Sanctum\\Http\\Middleware\\EnsureFrontendRequestsAreStateful::class,
    \\Illuminate\\Routing\\Middleware\\ThrottleRequests::class.&#39;:api&#39;,
    \\Illuminate\\Routing\\Middleware\\SubstituteBindings::class,
],
</code></pre><p><a name="cors-and-cookies"></a></p><h4 id="cors-cookies" tabindex="-1"><a class="header-anchor" href="#cors-cookies" aria-hidden="true">#</a> CORS &amp; Cookies</h4><p>If you are having trouble authenticating with your application from a SPA that executes on a separate subdomain, you have likely misconfigured your CORS (Cross-Origin Resource Sharing) or session cookie settings.</p><p>You should ensure that your application&#39;s CORS configuration is returning the <code>Access-Control-Allow-Credentials</code> header with a value of <code>True</code>. This may be accomplished by setting the <code>supports_credentials</code> option within your application&#39;s <code>config/cors.php</code> configuration file to <code>true</code>.</p><p>In addition, you should enable the <code>withCredentials</code> option on your application&#39;s global <code>axios</code> instance. Typically, this should be performed in your <code>resources/js/bootstrap.js</code> file. If you are not using Axios to make HTTP requests from your frontend, you should perform the equivalent configuration on your own HTTP client:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>axios<span class="token punctuation">.</span>defaults<span class="token punctuation">.</span>withCredentials <span class="token operator">=</span> <span class="token boolean">true</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Finally, you should ensure your application&#39;s session cookie domain configuration supports any subdomain of your root domain. You may accomplish this by prefixing the domain with a leading <code>.</code> within your application&#39;s <code>config/session.php</code> configuration file:</p><pre><code>&#39;domain&#39; =&gt; &#39;.domain.com&#39;,
</code></pre><p><a name="spa-authenticating"></a></p><h3 id="authenticating" tabindex="-1"><a class="header-anchor" href="#authenticating" aria-hidden="true">#</a> Authenticating</h3><p><a name="csrf-protection"></a></p><h4 id="csrf-protection" tabindex="-1"><a class="header-anchor" href="#csrf-protection" aria-hidden="true">#</a> CSRF Protection</h4><p>To authenticate your SPA, your SPA&#39;s &quot;login&quot; page should first make a request to the <code>/sanctum/csrf-cookie</code> endpoint to initialize CSRF protection for the application:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>axios<span class="token punctuation">.</span><span class="token function">get</span><span class="token punctuation">(</span><span class="token string">&#39;/sanctum/csrf-cookie&#39;</span><span class="token punctuation">)</span><span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token parameter">response</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
    <span class="token comment">// Login...</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>During this request, Laravel will set an <code>XSRF-TOKEN</code> cookie containing the current CSRF token. This token should then be passed in an <code>X-XSRF-TOKEN</code> header on subsequent requests, which some HTTP client libraries like Axios and the Angular HttpClient will do automatically for you. If your JavaScript HTTP library does not set the value for you, you will need to manually set the <code>X-XSRF-TOKEN</code> header to match the value of the <code>XSRF-TOKEN</code> cookie that is set by this route.</p><p><a name="logging-in"></a></p><h4 id="logging-in" tabindex="-1"><a class="header-anchor" href="#logging-in" aria-hidden="true">#</a> Logging In</h4><p>Once CSRF protection has been initialized, you should make a <code>POST</code> request to your Laravel application&#39;s <code>/login</code> route. This <code>/login</code> route may be <a href="./authentication#authenticating-users">implemented manually</a> or using a headless authentication package like <a href="./fortify">Laravel Fortify</a>.</p><p>If the login request is successful, you will be authenticated and subsequent requests to your application&#39;s routes will automatically be authenticated via the session cookie that the Laravel application issued to your client. In addition, since your application already made a request to the <code>/sanctum/csrf-cookie</code> route, subsequent requests should automatically receive CSRF protection as long as your JavaScript HTTP client sends the value of the <code>XSRF-TOKEN</code> cookie in the <code>X-XSRF-TOKEN</code> header.</p><p>Of course, if your user&#39;s session expires due to lack of activity, subsequent requests to the Laravel application may receive 401 or 419 HTTP error response. In this case, you should redirect the user to your SPA&#39;s login page.</p><blockquote><p><strong>Warning</strong><br> You are free to write your own <code>/login</code> endpoint; however, you should ensure that it authenticates the user using the standard, <a href="./authentication#authenticating-users">session based authentication services that Laravel provides</a>. Typically, this means using the <code>web</code> authentication guard.</p></blockquote><p><a name="protecting-spa-routes"></a></p><h3 id="protecting-routes-1" tabindex="-1"><a class="header-anchor" href="#protecting-routes-1" aria-hidden="true">#</a> Protecting Routes</h3><p>To protect routes so that all incoming requests must be authenticated, you should attach the <code>sanctum</code> authentication guard to your API routes within your <code>routes/api.php</code> file. This guard will ensure that incoming requests are authenticated as either a stateful authenticated requests from your SPA or contain a valid API token header if the request is from a third party:</p><pre><code>use Illuminate\\Http\\Request;

Route::middleware(&#39;auth:sanctum&#39;)-&gt;get(&#39;/user&#39;, function (Request $request) {
    return $request-&gt;user();
});
</code></pre><p><a name="authorizing-private-broadcast-channels"></a></p><h3 id="authorizing-private-broadcast-channels" tabindex="-1"><a class="header-anchor" href="#authorizing-private-broadcast-channels" aria-hidden="true">#</a> Authorizing Private Broadcast Channels</h3><p>If your SPA needs to authenticate with <a href="./broadcasting#authorizing-channels">private / presence broadcast channels</a>, you should place the <code>Broadcast::routes</code> method call within your <code>routes/api.php</code> file:</p><pre><code>Broadcast::routes([&#39;middleware&#39; =&gt; [&#39;auth:sanctum&#39;]]);
</code></pre><p>Next, in order for Pusher&#39;s authorization requests to succeed, you will need to provide a custom Pusher <code>authorizer</code> when initializing <a href="./broadcasting#client-side-installation">Laravel Echo</a>. This allows your application to configure Pusher to use the <code>axios</code> instance that is <a href="#cors-and-cookies">properly configured for cross-domain requests</a>:</p><div class="language-javascript line-numbers-mode" data-ext="js"><pre class="language-javascript"><code>window<span class="token punctuation">.</span>Echo <span class="token operator">=</span> <span class="token keyword">new</span> <span class="token class-name">Echo</span><span class="token punctuation">(</span><span class="token punctuation">{</span>
    <span class="token literal-property property">broadcaster</span><span class="token operator">:</span> <span class="token string">&quot;pusher&quot;</span><span class="token punctuation">,</span>
    <span class="token literal-property property">cluster</span><span class="token operator">:</span> <span class="token keyword">import</span><span class="token punctuation">.</span>meta<span class="token punctuation">.</span>env<span class="token punctuation">.</span><span class="token constant">VITE_PUSHER_APP_CLUSTER</span><span class="token punctuation">,</span>
    <span class="token literal-property property">encrypted</span><span class="token operator">:</span> <span class="token boolean">true</span><span class="token punctuation">,</span>
    <span class="token literal-property property">key</span><span class="token operator">:</span> <span class="token keyword">import</span><span class="token punctuation">.</span>meta<span class="token punctuation">.</span>env<span class="token punctuation">.</span><span class="token constant">VITE_PUSHER_APP_KEY</span><span class="token punctuation">,</span>
    <span class="token function-variable function">authorizer</span><span class="token operator">:</span> <span class="token punctuation">(</span><span class="token parameter">channel<span class="token punctuation">,</span> options</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
        <span class="token keyword">return</span> <span class="token punctuation">{</span>
            <span class="token function-variable function">authorize</span><span class="token operator">:</span> <span class="token punctuation">(</span><span class="token parameter">socketId<span class="token punctuation">,</span> callback</span><span class="token punctuation">)</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
                axios<span class="token punctuation">.</span><span class="token function">post</span><span class="token punctuation">(</span><span class="token string">&#39;/api/broadcasting/auth&#39;</span><span class="token punctuation">,</span> <span class="token punctuation">{</span>
                    <span class="token literal-property property">socket_id</span><span class="token operator">:</span> socketId<span class="token punctuation">,</span>
                    <span class="token literal-property property">channel_name</span><span class="token operator">:</span> channel<span class="token punctuation">.</span>name
                <span class="token punctuation">}</span><span class="token punctuation">)</span>
                <span class="token punctuation">.</span><span class="token function">then</span><span class="token punctuation">(</span><span class="token parameter">response</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
                    <span class="token function">callback</span><span class="token punctuation">(</span><span class="token boolean">false</span><span class="token punctuation">,</span> response<span class="token punctuation">.</span>data<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">}</span><span class="token punctuation">)</span>
                <span class="token punctuation">.</span><span class="token function">catch</span><span class="token punctuation">(</span><span class="token parameter">error</span> <span class="token operator">=&gt;</span> <span class="token punctuation">{</span>
                    <span class="token function">callback</span><span class="token punctuation">(</span><span class="token boolean">true</span><span class="token punctuation">,</span> error<span class="token punctuation">)</span><span class="token punctuation">;</span>
                <span class="token punctuation">}</span><span class="token punctuation">)</span><span class="token punctuation">;</span>
            <span class="token punctuation">}</span>
        <span class="token punctuation">}</span><span class="token punctuation">;</span>
    <span class="token punctuation">}</span><span class="token punctuation">,</span>
<span class="token punctuation">}</span><span class="token punctuation">)</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p><a name="mobile-application-authentication"></a></p><h2 id="mobile-application-authentication" tabindex="-1"><a class="header-anchor" href="#mobile-application-authentication" aria-hidden="true">#</a> Mobile Application Authentication</h2><p>You may also use Sanctum tokens to authenticate your mobile application&#39;s requests to your API. The process for authenticating mobile application requests is similar to authenticating third-party API requests; however, there are small differences in how you will issue the API tokens.</p><p><a name="issuing-mobile-api-tokens"></a></p><h3 id="issuing-api-tokens-1" tabindex="-1"><a class="header-anchor" href="#issuing-api-tokens-1" aria-hidden="true">#</a> Issuing API Tokens</h3><p>To get started, create a route that accepts the user&#39;s email / username, password, and device name, then exchanges those credentials for a new Sanctum token. The &quot;device name&quot; given to this endpoint is for informational purposes and may be any value you wish. In general, the device name value should be a name the user would recognize, such as &quot;Nuno&#39;s iPhone 12&quot;.</p><p>Typically, you will make a request to the token endpoint from your mobile application&#39;s &quot;login&quot; screen. The endpoint will return the plain-text API token which may then be stored on the mobile device and used to make additional API requests:</p><pre><code>use App\\Models\\User;
use Illuminate\\Http\\Request;
use Illuminate\\Support\\Facades\\Hash;
use Illuminate\\Validation\\ValidationException;

Route::post(&#39;/sanctum/token&#39;, function (Request $request) {
    $request-&gt;validate([
        &#39;email&#39; =&gt; &#39;required|email&#39;,
        &#39;password&#39; =&gt; &#39;required&#39;,
        &#39;device_name&#39; =&gt; &#39;required&#39;,
    ]);

    $user = User::where(&#39;email&#39;, $request-&gt;email)-&gt;first();

    if (! $user || ! Hash::check($request-&gt;password, $user-&gt;password)) {
        throw ValidationException::withMessages([
            &#39;email&#39; =&gt; [&#39;The provided credentials are incorrect.&#39;],
        ]);
    }

    return $user-&gt;createToken($request-&gt;device_name)-&gt;plainTextToken;
});
</code></pre><p>When the mobile application uses the token to make an API request to your application, it should pass the token in the <code>Authorization</code> header as a <code>Bearer</code> token.</p><blockquote><p><strong>Note</strong><br> When issuing tokens for a mobile application, you are also free to specify <a href="#token-abilities">token abilities</a>.</p></blockquote><p><a name="protecting-mobile-api-routes"></a></p><h3 id="protecting-routes-2" tabindex="-1"><a class="header-anchor" href="#protecting-routes-2" aria-hidden="true">#</a> Protecting Routes</h3><p>As previously documented, you may protect routes so that all incoming requests must be authenticated by attaching the <code>sanctum</code> authentication guard to the routes:</p><pre><code>Route::middleware(&#39;auth:sanctum&#39;)-&gt;get(&#39;/user&#39;, function (Request $request) {
    return $request-&gt;user();
});
</code></pre><p><a name="revoking-mobile-api-tokens"></a></p><h3 id="revoking-tokens-1" tabindex="-1"><a class="header-anchor" href="#revoking-tokens-1" aria-hidden="true">#</a> Revoking Tokens</h3><p>To allow users to revoke API tokens issued to mobile devices, you may list them by name, along with a &quot;Revoke&quot; button, within an &quot;account settings&quot; portion of your web application&#39;s UI. When the user clicks the &quot;Revoke&quot; button, you can delete the token from the database. Remember, you can access a user&#39;s API tokens via the <code>tokens</code> relationship provided by the <code>Laravel\\Sanctum\\HasApiTokens</code> trait:</p><pre><code>// Revoke all tokens...
$user-&gt;tokens()-&gt;delete();

// Revoke a specific token...
$user-&gt;tokens()-&gt;where(&#39;id&#39;, $tokenId)-&gt;delete();
</code></pre><p><a name="testing"></a></p><h2 id="testing" tabindex="-1"><a class="header-anchor" href="#testing" aria-hidden="true">#</a> Testing</h2><p>While testing, the <code>Sanctum::actingAs</code> method may be used to authenticate a user and specify which abilities should be granted to their token:</p><pre><code>use App\\Models\\User;
use Laravel\\Sanctum\\Sanctum;

public function test_task_list_can_be_retrieved(): void
{
    Sanctum::actingAs(
        User::factory()-&gt;create(),
        [&#39;view-tasks&#39;]
    );

    $response = $this-&gt;get(&#39;/api/task&#39;);

    $response-&gt;assertOk();
}
</code></pre><p>If you would like to grant all abilities to the token, you should include <code>*</code> in the ability list provided to the <code>actingAs</code> method:</p><pre><code>Sanctum::actingAs(
    User::factory()-&gt;create(),
    [&#39;*&#39;]
);
</code></pre>`,153);function h(m,k){const n=s("ExternalLinkIcon");return i(),r("div",null,[p,e("p",null,[e("a",l,[a("Laravel Sanctum"),c(n)]),a(" provides a featherweight authentication system for SPAs (single page applications), mobile applications, and simple, token based APIs. Sanctum allows each user of your application to generate multiple API tokens for their account. These tokens may be granted abilities / scopes which specify which actions the tokens are allowed to perform.")]),d])}const y=o(u,[["render",h],["__file","sanctum.html.vue"]]);export{y as default};
