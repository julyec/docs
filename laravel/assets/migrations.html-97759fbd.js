import{_ as n}from"./plugin-vue_export-helper-c27b6911.js";import{r as d,o as i,c,b as e,d as t,e as r,a}from"./app-06635a3b.js";const l={},s=a(`<h1 id="database-migrations" tabindex="-1"><a class="header-anchor" href="#database-migrations" aria-hidden="true">#</a> Database: Migrations</h1><ul><li><a href="#introduction">Introduction</a></li><li><a href="#generating-migrations">Generating Migrations</a><ul><li><a href="#squashing-migrations">Squashing Migrations</a></li></ul></li><li><a href="#migration-structure">Migration Structure</a></li><li><a href="#running-migrations">Running Migrations</a><ul><li><a href="#rolling-back-migrations">Rolling Back Migrations</a></li></ul></li><li><a href="#tables">Tables</a><ul><li><a href="#creating-tables">Creating Tables</a></li><li><a href="#updating-tables">Updating Tables</a></li><li><a href="#renaming-and-dropping-tables">Renaming / Dropping Tables</a></li></ul></li><li><a href="#columns">Columns</a><ul><li><a href="#creating-columns">Creating Columns</a></li><li><a href="#available-column-types">Available Column Types</a></li><li><a href="#column-modifiers">Column Modifiers</a></li><li><a href="#modifying-columns">Modifying Columns</a></li><li><a href="#renaming-columns">Renaming Columns</a></li><li><a href="#dropping-columns">Dropping Columns</a></li></ul></li><li><a href="#indexes">Indexes</a><ul><li><a href="#creating-indexes">Creating Indexes</a></li><li><a href="#renaming-indexes">Renaming Indexes</a></li><li><a href="#dropping-indexes">Dropping Indexes</a></li><li><a href="#foreign-key-constraints">Foreign Key Constraints</a></li></ul></li><li><a href="#events">Events</a></li></ul><p><a name="introduction"></a></p><h2 id="introduction" tabindex="-1"><a class="header-anchor" href="#introduction" aria-hidden="true">#</a> Introduction</h2><p>Migrations are like version control for your database, allowing your team to define and share the application&#39;s database schema definition. If you have ever had to tell a teammate to manually add a column to their local database schema after pulling in your changes from source control, you&#39;ve faced the problem that database migrations solve.</p><p>The Laravel <code>Schema</code> <a href="./facades">facade</a> provides database agnostic support for creating and manipulating tables across all of Laravel&#39;s supported database systems. Typically, migrations will use this facade to create and modify database tables and columns.</p><p><a name="generating-migrations"></a></p><h2 id="generating-migrations" tabindex="-1"><a class="header-anchor" href="#generating-migrations" aria-hidden="true">#</a> Generating Migrations</h2><p>You may use the <code>make:migration</code> <a href="./artisan">Artisan command</a> to generate a database migration. The new migration will be placed in your <code>database/migrations</code> directory. Each migration filename contains a timestamp that allows Laravel to determine the order of the migrations:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan make:migration create_flights_table
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>Laravel will use the name of the migration to attempt to guess the name of the table and whether or not the migration will be creating a new table. If Laravel is able to determine the table name from the migration name, Laravel will pre-fill the generated migration file with the specified table. Otherwise, you may simply specify the table in the migration file manually.</p><p>If you would like to specify a custom path for the generated migration, you may use the <code>--path</code> option when executing the <code>make:migration</code> command. The given path should be relative to your application&#39;s base path.</p><blockquote><p><strong>Note</strong><br> Migration stubs may be customized using <a href="./artisan#stub-customization">stub publishing</a>.</p></blockquote><p><a name="squashing-migrations"></a></p><h3 id="squashing-migrations" tabindex="-1"><a class="header-anchor" href="#squashing-migrations" aria-hidden="true">#</a> Squashing Migrations</h3><p>As you build your application, you may accumulate more and more migrations over time. This can lead to your <code>database/migrations</code> directory becoming bloated with potentially hundreds of migrations. If you would like, you may &quot;squash&quot; your migrations into a single SQL file. To get started, execute the <code>schema:dump</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan schema:dump

<span class="token comment"># Dump the current database schema and prune all existing migrations...</span>
php artisan schema:dump <span class="token parameter variable">--prune</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>When you execute this command, Laravel will write a &quot;schema&quot; file to your application&#39;s <code>database/schema</code> directory. The schema file&#39;s name will correspond to the database connection. Now, when you attempt to migrate your database and no other migrations have been executed, Laravel will first execute the SQL statements in the schema file of the database connection you are using. After executing the schema file&#39;s SQL statements, Laravel will execute any remaining migrations that were not part of the schema dump.</p><p>If your application&#39;s tests use a different database connection than the one you typically use during local development, you should ensure you have dumped a schema file using that database connection so that your tests are able to build your database. You may wish to do this after dumping the database connection you typically use during local development:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan schema:dump
php artisan schema:dump <span class="token parameter variable">--database</span><span class="token operator">=</span>testing <span class="token parameter variable">--prune</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div></div></div><p>You should commit your database schema file to source control so that other new developers on your team may quickly create your application&#39;s initial database structure.</p><blockquote><p><strong>Warning</strong><br> Migration squashing is only available for the MySQL, PostgreSQL, and SQLite databases and utilizes the database&#39;s command-line client. Schema dumps may not be restored to in-memory SQLite databases.</p></blockquote><p><a name="migration-structure"></a></p><h2 id="migration-structure" tabindex="-1"><a class="header-anchor" href="#migration-structure" aria-hidden="true">#</a> Migration Structure</h2><p>A migration class contains two methods: <code>up</code> and <code>down</code>. The <code>up</code> method is used to add new tables, columns, or indexes to your database, while the <code>down</code> method should reverse the operations performed by the <code>up</code> method.</p><p>Within both of these methods, you may use the Laravel schema builder to expressively create and modify tables. To learn about all of the methods available on the <code>Schema</code> builder, <a href="#creating-tables">check out its documentation</a>. For example, the following migration creates a <code>flights</code> table:</p><pre><code>&lt;?php

use Illuminate\\Database\\Migrations\\Migration;
use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create(&#39;flights&#39;, function (Blueprint $table) {
            $table-&gt;id();
            $table-&gt;string(&#39;name&#39;);
            $table-&gt;string(&#39;airline&#39;);
            $table-&gt;timestamps();
        });
    }

    /**
     * Reverse the migrations.
     */
    public function down(): void
    {
        Schema::drop(&#39;flights&#39;);
    }
};
</code></pre><p><a name="setting-the-migration-connection"></a></p><h4 id="setting-the-migration-connection" tabindex="-1"><a class="header-anchor" href="#setting-the-migration-connection" aria-hidden="true">#</a> Setting The Migration Connection</h4><p>If your migration will be interacting with a database connection other than your application&#39;s default database connection, you should set the <code>$connection</code> property of your migration:</p><pre><code>/**
 * The database connection that should be used by the migration.
 *
 * @var string
 */
protected $connection = &#39;pgsql&#39;;

/**
 * Run the migrations.
 */
public function up(): void
{
    // ...
}
</code></pre><p><a name="running-migrations"></a></p><h2 id="running-migrations" tabindex="-1"><a class="header-anchor" href="#running-migrations" aria-hidden="true">#</a> Running Migrations</h2><p>To run all of your outstanding migrations, execute the <code>migrate</code> Artisan command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you would like to see which migrations have run thus far, you may use the <code>migrate:status</code> Artisan command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:status
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you would like to see the SQL statements that will be executed by the migrations without actually running them, you may provide the <code>--pretend</code> flag to the <code>migrate</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--pretend</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><h4 id="isolating-migration-execution" tabindex="-1"><a class="header-anchor" href="#isolating-migration-execution" aria-hidden="true">#</a> Isolating Migration Execution</h4><p>If you are deploying your application across multiple servers and running migrations as part of your deployment process, you likely do not want two servers attempting to migrate the database at the same time. To avoid this, you may use the <code>isolated</code> option when invoking the <code>migrate</code> command.</p><p>When the <code>isolated</code> option is provided, Laravel will acquire an atomic lock using your application&#39;s cache driver before attempting to run your migrations. All other attempts to run the <code>migrate</code> command while that lock is held will not execute; however, the command will still exit with a successful exit status code:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--isolated</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><blockquote><p><strong>Warning</strong> To utilize this feature, your application must be using the <code>memcached</code>, <code>redis</code>, <code>dynamodb</code>, <code>database</code>, <code>file</code>, or <code>array</code> cache driver as your application&#39;s default cache driver. In addition, all servers must be communicating with the same central cache server.</p></blockquote><p><a name="forcing-migrations-to-run-in-production"></a></p><h4 id="forcing-migrations-to-run-in-production" tabindex="-1"><a class="header-anchor" href="#forcing-migrations-to-run-in-production" aria-hidden="true">#</a> Forcing Migrations To Run In Production</h4><p>Some migration operations are destructive, which means they may cause you to lose data. In order to protect you from running these commands against your production database, you will be prompted for confirmation before the commands are executed. To force the commands to run without a prompt, use the <code>--force</code> flag:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate <span class="token parameter variable">--force</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="rolling-back-migrations"></a></p><h3 id="rolling-back-migrations" tabindex="-1"><a class="header-anchor" href="#rolling-back-migrations" aria-hidden="true">#</a> Rolling Back Migrations</h3><p>To roll back the latest migration operation, you may use the <code>rollback</code> Artisan command. This command rolls back the last &quot;batch&quot; of migrations, which may include multiple migration files:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You may roll back a limited number of migrations by providing the <code>step</code> option to the <code>rollback</code> command. For example, the following command will roll back the last five migrations:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback <span class="token parameter variable">--step</span><span class="token operator">=</span><span class="token number">5</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>You may roll back a specific &quot;batch&quot; of migrations by providing the <code>batch</code> option to the <code>rollback</code> command, where the <code>batch</code> option corresponds to a batch value within your application&#39;s <code>migrations</code> database table. For example, the following command will roll back all migrations in batch three:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback <span class="token parameter variable">--batch</span><span class="token operator">=</span><span class="token number">3</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>If you would like to see the SQL statements that will be executed by the migrations without actually running them, you may provide the <code>--pretend</code> flag to the <code>migrate:rollback</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:rollback <span class="token parameter variable">--pretend</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p>The <code>migrate:reset</code> command will roll back all of your application&#39;s migrations:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:reset
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="roll-back-migrate-using-a-single-command"></a></p><h4 id="roll-back-migrate-using-a-single-command" tabindex="-1"><a class="header-anchor" href="#roll-back-migrate-using-a-single-command" aria-hidden="true">#</a> Roll Back &amp; Migrate Using A Single Command</h4><p>The <code>migrate:refresh</code> command will roll back all of your migrations and then execute the <code>migrate</code> command. This command effectively re-creates your entire database:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:refresh

<span class="token comment"># Refresh the database and run all database seeds...</span>
php artisan migrate:refresh <span class="token parameter variable">--seed</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><p>You may roll back and re-migrate a limited number of migrations by providing the <code>step</code> option to the <code>refresh</code> command. For example, the following command will roll back and re-migrate the last five migrations:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:refresh <span class="token parameter variable">--step</span><span class="token operator">=</span><span class="token number">5</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div></div></div><p><a name="drop-all-tables-migrate"></a></p><h4 id="drop-all-tables-migrate" tabindex="-1"><a class="header-anchor" href="#drop-all-tables-migrate" aria-hidden="true">#</a> Drop All Tables &amp; Migrate</h4><p>The <code>migrate:fresh</code> command will drop all tables from the database and then execute the <code>migrate</code> command:</p><div class="language-bash line-numbers-mode" data-ext="sh"><pre class="language-bash"><code>php artisan migrate:fresh

php artisan migrate:fresh <span class="token parameter variable">--seed</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>Warning</strong><br> The <code>migrate:fresh</code> command will drop all database tables regardless of their prefix. This command should be used with caution when developing on a database that is shared with other applications.</p></blockquote><p><a name="tables"></a></p><h2 id="tables" tabindex="-1"><a class="header-anchor" href="#tables" aria-hidden="true">#</a> Tables</h2><p><a name="creating-tables"></a></p><h3 id="creating-tables" tabindex="-1"><a class="header-anchor" href="#creating-tables" aria-hidden="true">#</a> Creating Tables</h3><p>To create a new database table, use the <code>create</code> method on the <code>Schema</code> facade. The <code>create</code> method accepts two arguments: the first is the name of the table, while the second is a closure which receives a <code>Blueprint</code> object that may be used to define the new table:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;id();
    $table-&gt;string(&#39;name&#39;);
    $table-&gt;string(&#39;email&#39;);
    $table-&gt;timestamps();
});
</code></pre><p>When creating the table, you may use any of the schema builder&#39;s <a href="#creating-columns">column methods</a> to define the table&#39;s columns.</p><p><a name="checking-for-table-column-existence"></a></p><h4 id="checking-for-table-column-existence" tabindex="-1"><a class="header-anchor" href="#checking-for-table-column-existence" aria-hidden="true">#</a> Checking For Table / Column Existence</h4><p>You may check for the existence of a table or column using the <code>hasTable</code> and <code>hasColumn</code> methods:</p><pre><code>if (Schema::hasTable(&#39;users&#39;)) {
    // The &quot;users&quot; table exists...
}

if (Schema::hasColumn(&#39;users&#39;, &#39;email&#39;)) {
    // The &quot;users&quot; table exists and has an &quot;email&quot; column...
}
</code></pre><p><a name="database-connection-table-options"></a></p><h4 id="database-connection-table-options" tabindex="-1"><a class="header-anchor" href="#database-connection-table-options" aria-hidden="true">#</a> Database Connection &amp; Table Options</h4><p>If you want to perform a schema operation on a database connection that is not your application&#39;s default connection, use the <code>connection</code> method:</p><pre><code>Schema::connection(&#39;sqlite&#39;)-&gt;create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;id();
});
</code></pre><p>In addition, a few other properties and methods may be used to define other aspects of the table&#39;s creation. The <code>engine</code> property may be used to specify the table&#39;s storage engine when using MySQL:</p><pre><code>Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;engine = &#39;InnoDB&#39;;

    // ...
});
</code></pre><p>The <code>charset</code> and <code>collation</code> properties may be used to specify the character set and collation for the created table when using MySQL:</p><pre><code>Schema::create(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;charset = &#39;utf8mb4&#39;;
    $table-&gt;collation = &#39;utf8mb4_unicode_ci&#39;;

    // ...
});
</code></pre><p>The <code>temporary</code> method may be used to indicate that the table should be &quot;temporary&quot;. Temporary tables are only visible to the current connection&#39;s database session and are dropped automatically when the connection is closed:</p><pre><code>Schema::create(&#39;calculations&#39;, function (Blueprint $table) {
    $table-&gt;temporary();

    // ...
});
</code></pre><p>If you would like to add a &quot;comment&quot; to a database table, you may invoke the <code>comment</code> method on the table instance. Table comments are currently only supported by MySQL and Postgres:</p><pre><code>Schema::create(&#39;calculations&#39;, function (Blueprint $table) {
    $table-&gt;comment(&#39;Business calculations&#39;);

    // ...
});
</code></pre><p><a name="updating-tables"></a></p><h3 id="updating-tables" tabindex="-1"><a class="header-anchor" href="#updating-tables" aria-hidden="true">#</a> Updating Tables</h3><p>The <code>table</code> method on the <code>Schema</code> facade may be used to update existing tables. Like the <code>create</code> method, the <code>table</code> method accepts two arguments: the name of the table and a closure that receives a <code>Blueprint</code> instance you may use to add columns or indexes to the table:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;);
});
</code></pre><p><a name="renaming-and-dropping-tables"></a></p><h3 id="renaming-dropping-tables" tabindex="-1"><a class="header-anchor" href="#renaming-dropping-tables" aria-hidden="true">#</a> Renaming / Dropping Tables</h3><p>To rename an existing database table, use the <code>rename</code> method:</p><pre><code>use Illuminate\\Support\\Facades\\Schema;

Schema::rename($from, $to);
</code></pre><p>To drop an existing table, you may use the <code>drop</code> or <code>dropIfExists</code> methods:</p><pre><code>Schema::drop(&#39;users&#39;);

Schema::dropIfExists(&#39;users&#39;);
</code></pre><p><a name="renaming-tables-with-foreign-keys"></a></p><h4 id="renaming-tables-with-foreign-keys" tabindex="-1"><a class="header-anchor" href="#renaming-tables-with-foreign-keys" aria-hidden="true">#</a> Renaming Tables With Foreign Keys</h4><p>Before renaming a table, you should verify that any foreign key constraints on the table have an explicit name in your migration files instead of letting Laravel assign a convention based name. Otherwise, the foreign key constraint name will refer to the old table name.</p><p><a name="columns"></a></p><h2 id="columns" tabindex="-1"><a class="header-anchor" href="#columns" aria-hidden="true">#</a> Columns</h2><p><a name="creating-columns"></a></p><h3 id="creating-columns" tabindex="-1"><a class="header-anchor" href="#creating-columns" aria-hidden="true">#</a> Creating Columns</h3><p>The <code>table</code> method on the <code>Schema</code> facade may be used to update existing tables. Like the <code>create</code> method, the <code>table</code> method accepts two arguments: the name of the table and a closure that receives an <code>Illuminate\\Database\\Schema\\Blueprint</code> instance you may use to add columns to the table:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;);
});
</code></pre><p><a name="available-column-types"></a></p><h3 id="available-column-types" tabindex="-1"><a class="header-anchor" href="#available-column-types" aria-hidden="true">#</a> Available Column Types</h3><p>The schema builder blueprint offers a variety of methods that correspond to the different types of columns you can add to your database tables. Each of the available methods are listed in the table below:</p>`,116),h=e("div",{class:"collection-method-list",markdown:"1"},[e("p",null,[e("a",{href:"#column-method-bigIncrements"},"bigIncrements"),e("a",{href:"#column-method-bigInteger"},"bigInteger"),e("a",{href:"#column-method-binary"},"binary"),e("a",{href:"#column-method-boolean"},"boolean"),e("a",{href:"#column-method-char"},"char"),e("a",{href:"#column-method-dateTimeTz"},"dateTimeTz"),e("a",{href:"#column-method-dateTime"},"dateTime"),e("a",{href:"#column-method-date"},"date"),e("a",{href:"#column-method-decimal"},"decimal"),e("a",{href:"#column-method-double"},"double"),e("a",{href:"#column-method-enum"},"enum"),e("a",{href:"#column-method-float"},"float"),e("a",{href:"#column-method-foreignId"},"foreignId"),e("a",{href:"#column-method-foreignIdFor"},"foreignIdFor"),e("a",{href:"#column-method-foreignUlid"},"foreignUlid"),e("a",{href:"#column-method-foreignUuid"},"foreignUuid"),e("a",{href:"#column-method-geometryCollection"},"geometryCollection"),e("a",{href:"#column-method-geometry"},"geometry"),e("a",{href:"#column-method-id"},"id"),e("a",{href:"#column-method-increments"},"increments"),e("a",{href:"#column-method-integer"},"integer"),e("a",{href:"#column-method-ipAddress"},"ipAddress"),e("a",{href:"#column-method-json"},"json"),e("a",{href:"#column-method-jsonb"},"jsonb"),e("a",{href:"#column-method-lineString"},"lineString"),e("a",{href:"#column-method-longText"},"longText"),e("a",{href:"#column-method-macAddress"},"macAddress"),e("a",{href:"#column-method-mediumIncrements"},"mediumIncrements"),e("a",{href:"#column-method-mediumInteger"},"mediumInteger"),e("a",{href:"#column-method-mediumText"},"mediumText"),e("a",{href:"#column-method-morphs"},"morphs"),e("a",{href:"#column-method-multiLineString"},"multiLineString"),e("a",{href:"#column-method-multiPoint"},"multiPoint"),e("a",{href:"#column-method-multiPolygon"},"multiPolygon"),e("a",{href:"#column-method-nullableMorphs"},"nullableMorphs"),e("a",{href:"#column-method-nullableTimestamps"},"nullableTimestamps"),e("a",{href:"#column-method-nullableUlidMorphs"},"nullableUlidMorphs"),e("a",{href:"#column-method-nullableUuidMorphs"},"nullableUuidMorphs"),e("a",{href:"#column-method-point"},"point"),e("a",{href:"#column-method-polygon"},"polygon"),e("a",{href:"#column-method-rememberToken"},"rememberToken"),e("a",{href:"#column-method-set"},"set"),e("a",{href:"#column-method-smallIncrements"},"smallIncrements"),e("a",{href:"#column-method-smallInteger"},"smallInteger"),e("a",{href:"#column-method-softDeletesTz"},"softDeletesTz"),e("a",{href:"#column-method-softDeletes"},"softDeletes"),e("a",{href:"#column-method-string"},"string"),e("a",{href:"#column-method-text"},"text"),e("a",{href:"#column-method-timeTz"},"timeTz"),e("a",{href:"#column-method-time"},"time"),e("a",{href:"#column-method-timestampTz"},"timestampTz"),e("a",{href:"#column-method-timestamp"},"timestamp"),e("a",{href:"#column-method-timestampsTz"},"timestampsTz"),e("a",{href:"#column-method-timestamps"},"timestamps"),e("a",{href:"#column-method-tinyIncrements"},"tinyIncrements"),e("a",{href:"#column-method-tinyInteger"},"tinyInteger"),e("a",{href:"#column-method-tinyText"},"tinyText"),e("a",{href:"#column-method-unsignedBigInteger"},"unsignedBigInteger"),e("a",{href:"#column-method-unsignedDecimal"},"unsignedDecimal"),e("a",{href:"#column-method-unsignedInteger"},"unsignedInteger"),e("a",{href:"#column-method-unsignedMediumInteger"},"unsignedMediumInteger"),e("a",{href:"#column-method-unsignedSmallInteger"},"unsignedSmallInteger"),e("a",{href:"#column-method-unsignedTinyInteger"},"unsignedTinyInteger"),e("a",{href:"#column-method-ulidMorphs"},"ulidMorphs"),e("a",{href:"#column-method-uuidMorphs"},"uuidMorphs"),e("a",{href:"#column-method-ulid"},"ulid"),e("a",{href:"#column-method-uuid"},"uuid"),e("a",{href:"#column-method-year"},"year")])],-1),m=a(`<p><a name="column-method-bigIncrements"></a></p><h4 id="bigincrements-collection-method-first-collection-method" tabindex="-1"><a class="header-anchor" href="#bigincrements-collection-method-first-collection-method" aria-hidden="true">#</a> <code>bigIncrements()</code> {.collection-method .first-collection-method}</h4><p>The <code>bigIncrements</code> method creates an auto-incrementing <code>UNSIGNED BIGINT</code> (primary key) equivalent column:</p><pre><code>$table-&gt;bigIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-bigInteger"></a></p><h4 id="biginteger-collection-method" tabindex="-1"><a class="header-anchor" href="#biginteger-collection-method" aria-hidden="true">#</a> <code>bigInteger()</code> {.collection-method}</h4><p>The <code>bigInteger</code> method creates a <code>BIGINT</code> equivalent column:</p><pre><code>$table-&gt;bigInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-binary"></a></p><h4 id="binary-collection-method" tabindex="-1"><a class="header-anchor" href="#binary-collection-method" aria-hidden="true">#</a> <code>binary()</code> {.collection-method}</h4><p>The <code>binary</code> method creates a <code>BLOB</code> equivalent column:</p><pre><code>$table-&gt;binary(&#39;photo&#39;);
</code></pre><p><a name="column-method-boolean"></a></p><h4 id="boolean-collection-method" tabindex="-1"><a class="header-anchor" href="#boolean-collection-method" aria-hidden="true">#</a> <code>boolean()</code> {.collection-method}</h4><p>The <code>boolean</code> method creates a <code>BOOLEAN</code> equivalent column:</p><pre><code>$table-&gt;boolean(&#39;confirmed&#39;);
</code></pre><p><a name="column-method-char"></a></p><h4 id="char-collection-method" tabindex="-1"><a class="header-anchor" href="#char-collection-method" aria-hidden="true">#</a> <code>char()</code> {.collection-method}</h4><p>The <code>char</code> method creates a <code>CHAR</code> equivalent column with of a given length:</p><pre><code>$table-&gt;char(&#39;name&#39;, 100);
</code></pre><p><a name="column-method-dateTimeTz"></a></p><h4 id="datetimetz-collection-method" tabindex="-1"><a class="header-anchor" href="#datetimetz-collection-method" aria-hidden="true">#</a> <code>dateTimeTz()</code> {.collection-method}</h4><p>The <code>dateTimeTz</code> method creates a <code>DATETIME</code> (with timezone) equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;dateTimeTz(&#39;created_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-dateTime"></a></p><h4 id="datetime-collection-method" tabindex="-1"><a class="header-anchor" href="#datetime-collection-method" aria-hidden="true">#</a> <code>dateTime()</code> {.collection-method}</h4><p>The <code>dateTime</code> method creates a <code>DATETIME</code> equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;dateTime(&#39;created_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-date"></a></p><h4 id="date-collection-method" tabindex="-1"><a class="header-anchor" href="#date-collection-method" aria-hidden="true">#</a> <code>date()</code> {.collection-method}</h4><p>The <code>date</code> method creates a <code>DATE</code> equivalent column:</p><pre><code>$table-&gt;date(&#39;created_at&#39;);
</code></pre><p><a name="column-method-decimal"></a></p><h4 id="decimal-collection-method" tabindex="-1"><a class="header-anchor" href="#decimal-collection-method" aria-hidden="true">#</a> <code>decimal()</code> {.collection-method}</h4><p>The <code>decimal</code> method creates a <code>DECIMAL</code> equivalent column with the given precision (total digits) and scale (decimal digits):</p><pre><code>$table-&gt;decimal(&#39;amount&#39;, $precision = 8, $scale = 2);
</code></pre><p><a name="column-method-double"></a></p><h4 id="double-collection-method" tabindex="-1"><a class="header-anchor" href="#double-collection-method" aria-hidden="true">#</a> <code>double()</code> {.collection-method}</h4><p>The <code>double</code> method creates a <code>DOUBLE</code> equivalent column with the given precision (total digits) and scale (decimal digits):</p><pre><code>$table-&gt;double(&#39;amount&#39;, 8, 2);
</code></pre><p><a name="column-method-enum"></a></p><h4 id="enum-collection-method" tabindex="-1"><a class="header-anchor" href="#enum-collection-method" aria-hidden="true">#</a> <code>enum()</code> {.collection-method}</h4><p>The <code>enum</code> method creates a <code>ENUM</code> equivalent column with the given valid values:</p><pre><code>$table-&gt;enum(&#39;difficulty&#39;, [&#39;easy&#39;, &#39;hard&#39;]);
</code></pre><p><a name="column-method-float"></a></p><h4 id="float-collection-method" tabindex="-1"><a class="header-anchor" href="#float-collection-method" aria-hidden="true">#</a> <code>float()</code> {.collection-method}</h4><p>The <code>float</code> method creates a <code>FLOAT</code> equivalent column with the given precision (total digits) and scale (decimal digits):</p><pre><code>$table-&gt;float(&#39;amount&#39;, 8, 2);
</code></pre><p><a name="column-method-foreignId"></a></p><h4 id="foreignid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignid-collection-method" aria-hidden="true">#</a> <code>foreignId()</code> {.collection-method}</h4><p>The <code>foreignId</code> method creates an <code>UNSIGNED BIGINT</code> equivalent column:</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;);
</code></pre><p><a name="column-method-foreignIdFor"></a></p><h4 id="foreignidfor-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignidfor-collection-method" aria-hidden="true">#</a> <code>foreignIdFor()</code> {.collection-method}</h4><p>The <code>foreignIdFor</code> method adds a <code>{column}_id</code> equivalent column for a given model class. The column type will be <code>UNSIGNED BIGINT</code>, <code>CHAR(36)</code>, or <code>CHAR(26)</code> depending on the model key type:</p><pre><code>$table-&gt;foreignIdFor(User::class);
</code></pre><p><a name="column-method-foreignUlid"></a></p><h4 id="foreignulid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignulid-collection-method" aria-hidden="true">#</a> <code>foreignUlid()</code> {.collection-method}</h4><p>The <code>foreignUlid</code> method creates a <code>ULID</code> equivalent column:</p><pre><code>$table-&gt;foreignUlid(&#39;user_id&#39;);
</code></pre><p><a name="column-method-foreignUuid"></a></p><h4 id="foreignuuid-collection-method" tabindex="-1"><a class="header-anchor" href="#foreignuuid-collection-method" aria-hidden="true">#</a> <code>foreignUuid()</code> {.collection-method}</h4><p>The <code>foreignUuid</code> method creates a <code>UUID</code> equivalent column:</p><pre><code>$table-&gt;foreignUuid(&#39;user_id&#39;);
</code></pre><p><a name="column-method-geometryCollection"></a></p><h4 id="geometrycollection-collection-method" tabindex="-1"><a class="header-anchor" href="#geometrycollection-collection-method" aria-hidden="true">#</a> <code>geometryCollection()</code> {.collection-method}</h4><p>The <code>geometryCollection</code> method creates a <code>GEOMETRYCOLLECTION</code> equivalent column:</p><pre><code>$table-&gt;geometryCollection(&#39;positions&#39;);
</code></pre><p><a name="column-method-geometry"></a></p><h4 id="geometry-collection-method" tabindex="-1"><a class="header-anchor" href="#geometry-collection-method" aria-hidden="true">#</a> <code>geometry()</code> {.collection-method}</h4><p>The <code>geometry</code> method creates a <code>GEOMETRY</code> equivalent column:</p><pre><code>$table-&gt;geometry(&#39;positions&#39;);
</code></pre><p><a name="column-method-id"></a></p><h4 id="id-collection-method" tabindex="-1"><a class="header-anchor" href="#id-collection-method" aria-hidden="true">#</a> <code>id()</code> {.collection-method}</h4><p>The <code>id</code> method is an alias of the <code>bigIncrements</code> method. By default, the method will create an <code>id</code> column; however, you may pass a column name if you would like to assign a different name to the column:</p><pre><code>$table-&gt;id();
</code></pre><p><a name="column-method-increments"></a></p><h4 id="increments-collection-method" tabindex="-1"><a class="header-anchor" href="#increments-collection-method" aria-hidden="true">#</a> <code>increments()</code> {.collection-method}</h4><p>The <code>increments</code> method creates an auto-incrementing <code>UNSIGNED INTEGER</code> equivalent column as a primary key:</p><pre><code>$table-&gt;increments(&#39;id&#39;);
</code></pre><p><a name="column-method-integer"></a></p><h4 id="integer-collection-method" tabindex="-1"><a class="header-anchor" href="#integer-collection-method" aria-hidden="true">#</a> <code>integer()</code> {.collection-method}</h4><p>The <code>integer</code> method creates an <code>INTEGER</code> equivalent column:</p><pre><code>$table-&gt;integer(&#39;votes&#39;);
</code></pre><p><a name="column-method-ipAddress"></a></p><h4 id="ipaddress-collection-method" tabindex="-1"><a class="header-anchor" href="#ipaddress-collection-method" aria-hidden="true">#</a> <code>ipAddress()</code> {.collection-method}</h4><p>The <code>ipAddress</code> method creates a <code>VARCHAR</code> equivalent column:</p><pre><code>$table-&gt;ipAddress(&#39;visitor&#39;);
</code></pre><p>When using Postgres, an <code>INET</code> column will be created.</p><p><a name="column-method-json"></a></p><h4 id="json-collection-method" tabindex="-1"><a class="header-anchor" href="#json-collection-method" aria-hidden="true">#</a> <code>json()</code> {.collection-method}</h4><p>The <code>json</code> method creates a <code>JSON</code> equivalent column:</p><pre><code>$table-&gt;json(&#39;options&#39;);
</code></pre><p><a name="column-method-jsonb"></a></p><h4 id="jsonb-collection-method" tabindex="-1"><a class="header-anchor" href="#jsonb-collection-method" aria-hidden="true">#</a> <code>jsonb()</code> {.collection-method}</h4><p>The <code>jsonb</code> method creates a <code>JSONB</code> equivalent column:</p><pre><code>$table-&gt;jsonb(&#39;options&#39;);
</code></pre><p><a name="column-method-lineString"></a></p><h4 id="linestring-collection-method" tabindex="-1"><a class="header-anchor" href="#linestring-collection-method" aria-hidden="true">#</a> <code>lineString()</code> {.collection-method}</h4><p>The <code>lineString</code> method creates a <code>LINESTRING</code> equivalent column:</p><pre><code>$table-&gt;lineString(&#39;positions&#39;);
</code></pre><p><a name="column-method-longText"></a></p><h4 id="longtext-collection-method" tabindex="-1"><a class="header-anchor" href="#longtext-collection-method" aria-hidden="true">#</a> <code>longText()</code> {.collection-method}</h4><p>The <code>longText</code> method creates a <code>LONGTEXT</code> equivalent column:</p><pre><code>$table-&gt;longText(&#39;description&#39;);
</code></pre><p><a name="column-method-macAddress"></a></p><h4 id="macaddress-collection-method" tabindex="-1"><a class="header-anchor" href="#macaddress-collection-method" aria-hidden="true">#</a> <code>macAddress()</code> {.collection-method}</h4><p>The <code>macAddress</code> method creates a column that is intended to hold a MAC address. Some database systems, such as PostgreSQL, have a dedicated column type for this type of data. Other database systems will use a string equivalent column:</p><pre><code>$table-&gt;macAddress(&#39;device&#39;);
</code></pre><p><a name="column-method-mediumIncrements"></a></p><h4 id="mediumincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#mediumincrements-collection-method" aria-hidden="true">#</a> <code>mediumIncrements()</code> {.collection-method}</h4><p>The <code>mediumIncrements</code> method creates an auto-incrementing <code>UNSIGNED MEDIUMINT</code> equivalent column as a primary key:</p><pre><code>$table-&gt;mediumIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-mediumInteger"></a></p><h4 id="mediuminteger-collection-method" tabindex="-1"><a class="header-anchor" href="#mediuminteger-collection-method" aria-hidden="true">#</a> <code>mediumInteger()</code> {.collection-method}</h4><p>The <code>mediumInteger</code> method creates a <code>MEDIUMINT</code> equivalent column:</p><pre><code>$table-&gt;mediumInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-mediumText"></a></p><h4 id="mediumtext-collection-method" tabindex="-1"><a class="header-anchor" href="#mediumtext-collection-method" aria-hidden="true">#</a> <code>mediumText()</code> {.collection-method}</h4><p>The <code>mediumText</code> method creates a <code>MEDIUMTEXT</code> equivalent column:</p><pre><code>$table-&gt;mediumText(&#39;description&#39;);
</code></pre><p><a name="column-method-morphs"></a></p><h4 id="morphs-collection-method" tabindex="-1"><a class="header-anchor" href="#morphs-collection-method" aria-hidden="true">#</a> <code>morphs()</code> {.collection-method}</h4><p>The <code>morphs</code> method is a convenience method that adds a <code>{column}_id</code> equivalent column and a <code>{column}_type</code> <code>VARCHAR</code> equivalent column. The column type for the <code>{column}_id</code> will be <code>UNSIGNED BIGINT</code>, <code>CHAR(36)</code>, or <code>CHAR(26)</code> depending on the model key type.</p><p>This method is intended to be used when defining the columns necessary for a polymorphic <a href="./eloquent-relationships">Eloquent relationship</a>. In the following example, <code>taggable_id</code> and <code>taggable_type</code> columns would be created:</p><pre><code>$table-&gt;morphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-multiLineString"></a></p><h4 id="multilinestring-collection-method" tabindex="-1"><a class="header-anchor" href="#multilinestring-collection-method" aria-hidden="true">#</a> <code>multiLineString()</code> {.collection-method}</h4><p>The <code>multiLineString</code> method creates a <code>MULTILINESTRING</code> equivalent column:</p><pre><code>$table-&gt;multiLineString(&#39;positions&#39;);
</code></pre><p><a name="column-method-multiPoint"></a></p><h4 id="multipoint-collection-method" tabindex="-1"><a class="header-anchor" href="#multipoint-collection-method" aria-hidden="true">#</a> <code>multiPoint()</code> {.collection-method}</h4><p>The <code>multiPoint</code> method creates a <code>MULTIPOINT</code> equivalent column:</p><pre><code>$table-&gt;multiPoint(&#39;positions&#39;);
</code></pre><p><a name="column-method-multiPolygon"></a></p><h4 id="multipolygon-collection-method" tabindex="-1"><a class="header-anchor" href="#multipolygon-collection-method" aria-hidden="true">#</a> <code>multiPolygon()</code> {.collection-method}</h4><p>The <code>multiPolygon</code> method creates a <code>MULTIPOLYGON</code> equivalent column:</p><pre><code>$table-&gt;multiPolygon(&#39;positions&#39;);
</code></pre><p><a name="column-method-nullableTimestamps"></a></p><h4 id="nullabletimestamps-collection-method" tabindex="-1"><a class="header-anchor" href="#nullabletimestamps-collection-method" aria-hidden="true">#</a> <code>nullableTimestamps()</code> {.collection-method}</h4><p>The <code>nullableTimestamps</code> method is an alias of the <a href="#column-method-timestamps">timestamps</a> method:</p><pre><code>$table-&gt;nullableTimestamps(0);
</code></pre><p><a name="column-method-nullableMorphs"></a></p><h4 id="nullablemorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullablemorphs-collection-method" aria-hidden="true">#</a> <code>nullableMorphs()</code> {.collection-method}</h4><p>The method is similar to the <a href="#column-method-morphs">morphs</a> method; however, the columns that are created will be &quot;nullable&quot;:</p><pre><code>$table-&gt;nullableMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-nullableUlidMorphs"></a></p><h4 id="nullableulidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullableulidmorphs-collection-method" aria-hidden="true">#</a> <code>nullableUlidMorphs()</code> {.collection-method}</h4><p>The method is similar to the <a href="#column-method-ulidMorphs">ulidMorphs</a> method; however, the columns that are created will be &quot;nullable&quot;:</p><pre><code>$table-&gt;nullableUlidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-nullableUuidMorphs"></a></p><h4 id="nullableuuidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#nullableuuidmorphs-collection-method" aria-hidden="true">#</a> <code>nullableUuidMorphs()</code> {.collection-method}</h4><p>The method is similar to the <a href="#column-method-uuidMorphs">uuidMorphs</a> method; however, the columns that are created will be &quot;nullable&quot;:</p><pre><code>$table-&gt;nullableUuidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-point"></a></p><h4 id="point-collection-method" tabindex="-1"><a class="header-anchor" href="#point-collection-method" aria-hidden="true">#</a> <code>point()</code> {.collection-method}</h4><p>The <code>point</code> method creates a <code>POINT</code> equivalent column:</p><pre><code>$table-&gt;point(&#39;position&#39;);
</code></pre><p><a name="column-method-polygon"></a></p><h4 id="polygon-collection-method" tabindex="-1"><a class="header-anchor" href="#polygon-collection-method" aria-hidden="true">#</a> <code>polygon()</code> {.collection-method}</h4><p>The <code>polygon</code> method creates a <code>POLYGON</code> equivalent column:</p><pre><code>$table-&gt;polygon(&#39;position&#39;);
</code></pre><p><a name="column-method-rememberToken"></a></p><h4 id="remembertoken-collection-method" tabindex="-1"><a class="header-anchor" href="#remembertoken-collection-method" aria-hidden="true">#</a> <code>rememberToken()</code> {.collection-method}</h4><p>The <code>rememberToken</code> method creates a nullable, <code>VARCHAR(100)</code> equivalent column that is intended to store the current &quot;remember me&quot; <a href="./authentication#remembering-users">authentication token</a>:</p><pre><code>$table-&gt;rememberToken();
</code></pre><p><a name="column-method-set"></a></p><h4 id="set-collection-method" tabindex="-1"><a class="header-anchor" href="#set-collection-method" aria-hidden="true">#</a> <code>set()</code> {.collection-method}</h4><p>The <code>set</code> method creates a <code>SET</code> equivalent column with the given list of valid values:</p><pre><code>$table-&gt;set(&#39;flavors&#39;, [&#39;strawberry&#39;, &#39;vanilla&#39;]);
</code></pre><p><a name="column-method-smallIncrements"></a></p><h4 id="smallincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#smallincrements-collection-method" aria-hidden="true">#</a> <code>smallIncrements()</code> {.collection-method}</h4><p>The <code>smallIncrements</code> method creates an auto-incrementing <code>UNSIGNED SMALLINT</code> equivalent column as a primary key:</p><pre><code>$table-&gt;smallIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-smallInteger"></a></p><h4 id="smallinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#smallinteger-collection-method" aria-hidden="true">#</a> <code>smallInteger()</code> {.collection-method}</h4><p>The <code>smallInteger</code> method creates a <code>SMALLINT</code> equivalent column:</p><pre><code>$table-&gt;smallInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-softDeletesTz"></a></p><h4 id="softdeletestz-collection-method" tabindex="-1"><a class="header-anchor" href="#softdeletestz-collection-method" aria-hidden="true">#</a> <code>softDeletesTz()</code> {.collection-method}</h4><p>The <code>softDeletesTz</code> method adds a nullable <code>deleted_at</code> <code>TIMESTAMP</code> (with timezone) equivalent column with an optional precision (total digits). This column is intended to store the <code>deleted_at</code> timestamp needed for Eloquent&#39;s &quot;soft delete&quot; functionality:</p><pre><code>$table-&gt;softDeletesTz($column = &#39;deleted_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-softDeletes"></a></p><h4 id="softdeletes-collection-method" tabindex="-1"><a class="header-anchor" href="#softdeletes-collection-method" aria-hidden="true">#</a> <code>softDeletes()</code> {.collection-method}</h4><p>The <code>softDeletes</code> method adds a nullable <code>deleted_at</code> <code>TIMESTAMP</code> equivalent column with an optional precision (total digits). This column is intended to store the <code>deleted_at</code> timestamp needed for Eloquent&#39;s &quot;soft delete&quot; functionality:</p><pre><code>$table-&gt;softDeletes($column = &#39;deleted_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-string"></a></p><h4 id="string-collection-method" tabindex="-1"><a class="header-anchor" href="#string-collection-method" aria-hidden="true">#</a> <code>string()</code> {.collection-method}</h4><p>The <code>string</code> method creates a <code>VARCHAR</code> equivalent column of the given length:</p><pre><code>$table-&gt;string(&#39;name&#39;, 100);
</code></pre><p><a name="column-method-text"></a></p><h4 id="text-collection-method" tabindex="-1"><a class="header-anchor" href="#text-collection-method" aria-hidden="true">#</a> <code>text()</code> {.collection-method}</h4><p>The <code>text</code> method creates a <code>TEXT</code> equivalent column:</p><pre><code>$table-&gt;text(&#39;description&#39;);
</code></pre><p><a name="column-method-timeTz"></a></p><h4 id="timetz-collection-method" tabindex="-1"><a class="header-anchor" href="#timetz-collection-method" aria-hidden="true">#</a> <code>timeTz()</code> {.collection-method}</h4><p>The <code>timeTz</code> method creates a <code>TIME</code> (with timezone) equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;timeTz(&#39;sunrise&#39;, $precision = 0);
</code></pre><p><a name="column-method-time"></a></p><h4 id="time-collection-method" tabindex="-1"><a class="header-anchor" href="#time-collection-method" aria-hidden="true">#</a> <code>time()</code> {.collection-method}</h4><p>The <code>time</code> method creates a <code>TIME</code> equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;time(&#39;sunrise&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestampTz"></a></p><h4 id="timestamptz-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamptz-collection-method" aria-hidden="true">#</a> <code>timestampTz()</code> {.collection-method}</h4><p>The <code>timestampTz</code> method creates a <code>TIMESTAMP</code> (with timezone) equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;timestampTz(&#39;added_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestamp"></a></p><h4 id="timestamp-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamp-collection-method" aria-hidden="true">#</a> <code>timestamp()</code> {.collection-method}</h4><p>The <code>timestamp</code> method creates a <code>TIMESTAMP</code> equivalent column with an optional precision (total digits):</p><pre><code>$table-&gt;timestamp(&#39;added_at&#39;, $precision = 0);
</code></pre><p><a name="column-method-timestampsTz"></a></p><h4 id="timestampstz-collection-method" tabindex="-1"><a class="header-anchor" href="#timestampstz-collection-method" aria-hidden="true">#</a> <code>timestampsTz()</code> {.collection-method}</h4><p>The <code>timestampsTz</code> method creates <code>created_at</code> and <code>updated_at</code> <code>TIMESTAMP</code> (with timezone) equivalent columns with an optional precision (total digits):</p><pre><code>$table-&gt;timestampsTz($precision = 0);
</code></pre><p><a name="column-method-timestamps"></a></p><h4 id="timestamps-collection-method" tabindex="-1"><a class="header-anchor" href="#timestamps-collection-method" aria-hidden="true">#</a> <code>timestamps()</code> {.collection-method}</h4><p>The <code>timestamps</code> method creates <code>created_at</code> and <code>updated_at</code> <code>TIMESTAMP</code> equivalent columns with an optional precision (total digits):</p><pre><code>$table-&gt;timestamps($precision = 0);
</code></pre><p><a name="column-method-tinyIncrements"></a></p><h4 id="tinyincrements-collection-method" tabindex="-1"><a class="header-anchor" href="#tinyincrements-collection-method" aria-hidden="true">#</a> <code>tinyIncrements()</code> {.collection-method}</h4><p>The <code>tinyIncrements</code> method creates an auto-incrementing <code>UNSIGNED TINYINT</code> equivalent column as a primary key:</p><pre><code>$table-&gt;tinyIncrements(&#39;id&#39;);
</code></pre><p><a name="column-method-tinyInteger"></a></p><h4 id="tinyinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#tinyinteger-collection-method" aria-hidden="true">#</a> <code>tinyInteger()</code> {.collection-method}</h4><p>The <code>tinyInteger</code> method creates a <code>TINYINT</code> equivalent column:</p><pre><code>$table-&gt;tinyInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-tinyText"></a></p><h4 id="tinytext-collection-method" tabindex="-1"><a class="header-anchor" href="#tinytext-collection-method" aria-hidden="true">#</a> <code>tinyText()</code> {.collection-method}</h4><p>The <code>tinyText</code> method creates a <code>TINYTEXT</code> equivalent column:</p><pre><code>$table-&gt;tinyText(&#39;notes&#39;);
</code></pre><p><a name="column-method-unsignedBigInteger"></a></p><h4 id="unsignedbiginteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedbiginteger-collection-method" aria-hidden="true">#</a> <code>unsignedBigInteger()</code> {.collection-method}</h4><p>The <code>unsignedBigInteger</code> method creates an <code>UNSIGNED BIGINT</code> equivalent column:</p><pre><code>$table-&gt;unsignedBigInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedDecimal"></a></p><h4 id="unsigneddecimal-collection-method" tabindex="-1"><a class="header-anchor" href="#unsigneddecimal-collection-method" aria-hidden="true">#</a> <code>unsignedDecimal()</code> {.collection-method}</h4><p>The <code>unsignedDecimal</code> method creates an <code>UNSIGNED DECIMAL</code> equivalent column with an optional precision (total digits) and scale (decimal digits):</p><pre><code>$table-&gt;unsignedDecimal(&#39;amount&#39;, $precision = 8, $scale = 2);
</code></pre><p><a name="column-method-unsignedInteger"></a></p><h4 id="unsignedinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedinteger-collection-method" aria-hidden="true">#</a> <code>unsignedInteger()</code> {.collection-method}</h4><p>The <code>unsignedInteger</code> method creates an <code>UNSIGNED INTEGER</code> equivalent column:</p><pre><code>$table-&gt;unsignedInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedMediumInteger"></a></p><h4 id="unsignedmediuminteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedmediuminteger-collection-method" aria-hidden="true">#</a> <code>unsignedMediumInteger()</code> {.collection-method}</h4><p>The <code>unsignedMediumInteger</code> method creates an <code>UNSIGNED MEDIUMINT</code> equivalent column:</p><pre><code>$table-&gt;unsignedMediumInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedSmallInteger"></a></p><h4 id="unsignedsmallinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedsmallinteger-collection-method" aria-hidden="true">#</a> <code>unsignedSmallInteger()</code> {.collection-method}</h4><p>The <code>unsignedSmallInteger</code> method creates an <code>UNSIGNED SMALLINT</code> equivalent column:</p><pre><code>$table-&gt;unsignedSmallInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-unsignedTinyInteger"></a></p><h4 id="unsignedtinyinteger-collection-method" tabindex="-1"><a class="header-anchor" href="#unsignedtinyinteger-collection-method" aria-hidden="true">#</a> <code>unsignedTinyInteger()</code> {.collection-method}</h4><p>The <code>unsignedTinyInteger</code> method creates an <code>UNSIGNED TINYINT</code> equivalent column:</p><pre><code>$table-&gt;unsignedTinyInteger(&#39;votes&#39;);
</code></pre><p><a name="column-method-ulidMorphs"></a></p><h4 id="ulidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#ulidmorphs-collection-method" aria-hidden="true">#</a> <code>ulidMorphs()</code> {.collection-method}</h4><p>The <code>ulidMorphs</code> method is a convenience method that adds a <code>{column}_id</code> <code>CHAR(26)</code> equivalent column and a <code>{column}_type</code> <code>VARCHAR</code> equivalent column.</p><p>This method is intended to be used when defining the columns necessary for a polymorphic <a href="./eloquent-relationships">Eloquent relationship</a> that use ULID identifiers. In the following example, <code>taggable_id</code> and <code>taggable_type</code> columns would be created:</p><pre><code>$table-&gt;ulidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-uuidMorphs"></a></p><h4 id="uuidmorphs-collection-method" tabindex="-1"><a class="header-anchor" href="#uuidmorphs-collection-method" aria-hidden="true">#</a> <code>uuidMorphs()</code> {.collection-method}</h4><p>The <code>uuidMorphs</code> method is a convenience method that adds a <code>{column}_id</code> <code>CHAR(36)</code> equivalent column and a <code>{column}_type</code> <code>VARCHAR</code> equivalent column.</p><p>This method is intended to be used when defining the columns necessary for a polymorphic <a href="./eloquent-relationships">Eloquent relationship</a> that use UUID identifiers. In the following example, <code>taggable_id</code> and <code>taggable_type</code> columns would be created:</p><pre><code>$table-&gt;uuidMorphs(&#39;taggable&#39;);
</code></pre><p><a name="column-method-ulid"></a></p><h4 id="ulid-collection-method" tabindex="-1"><a class="header-anchor" href="#ulid-collection-method" aria-hidden="true">#</a> <code>ulid()</code> {.collection-method}</h4><p>The <code>ulid</code> method creates a <code>ULID</code> equivalent column:</p><pre><code>$table-&gt;ulid(&#39;id&#39;);
</code></pre><p><a name="column-method-uuid"></a></p><h4 id="uuid-collection-method" tabindex="-1"><a class="header-anchor" href="#uuid-collection-method" aria-hidden="true">#</a> <code>uuid()</code> {.collection-method}</h4><p>The <code>uuid</code> method creates a <code>UUID</code> equivalent column:</p><pre><code>$table-&gt;uuid(&#39;id&#39;);
</code></pre><p><a name="column-method-year"></a></p><h4 id="year-collection-method" tabindex="-1"><a class="header-anchor" href="#year-collection-method" aria-hidden="true">#</a> <code>year()</code> {.collection-method}</h4><p>The <code>year</code> method creates a <code>YEAR</code> equivalent column:</p><pre><code>$table-&gt;year(&#39;birth_year&#39;);
</code></pre><p><a name="column-modifiers"></a></p><h3 id="column-modifiers" tabindex="-1"><a class="header-anchor" href="#column-modifiers" aria-hidden="true">#</a> Column Modifiers</h3><p>In addition to the column types listed above, there are several column &quot;modifiers&quot; you may use when adding a column to a database table. For example, to make the column &quot;nullable&quot;, you may use the <code>nullable</code> method:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;email&#39;)-&gt;nullable();
});
</code></pre><p>The following table contains all of the available column modifiers. This list does not include <a href="#creating-indexes">index modifiers</a>:</p><table><thead><tr><th>Modifier</th><th>Description</th></tr></thead><tbody><tr><td><code>-&gt;after(&#39;column&#39;)</code></td><td>Place the column &quot;after&quot; another column (MySQL).</td></tr><tr><td><code>-&gt;autoIncrement()</code></td><td>Set INTEGER columns as auto-incrementing (primary key).</td></tr><tr><td><code>-&gt;charset(&#39;utf8mb4&#39;)</code></td><td>Specify a character set for the column (MySQL).</td></tr><tr><td><code>-&gt;collation(&#39;utf8mb4_unicode_ci&#39;)</code></td><td>Specify a collation for the column (MySQL/PostgreSQL/SQL Server).</td></tr><tr><td><code>-&gt;comment(&#39;my comment&#39;)</code></td><td>Add a comment to a column (MySQL/PostgreSQL).</td></tr><tr><td><code>-&gt;default($value)</code></td><td>Specify a &quot;default&quot; value for the column.</td></tr><tr><td><code>-&gt;first()</code></td><td>Place the column &quot;first&quot; in the table (MySQL).</td></tr><tr><td><code>-&gt;from($integer)</code></td><td>Set the starting value of an auto-incrementing field (MySQL / PostgreSQL).</td></tr><tr><td><code>-&gt;invisible()</code></td><td>Make the column &quot;invisible&quot; to <code>SELECT *</code> queries (MySQL).</td></tr><tr><td><code>-&gt;nullable($value = true)</code></td><td>Allow NULL values to be inserted into the column.</td></tr><tr><td><code>-&gt;storedAs($expression)</code></td><td>Create a stored generated column (MySQL / PostgreSQL).</td></tr><tr><td><code>-&gt;unsigned()</code></td><td>Set INTEGER columns as UNSIGNED (MySQL).</td></tr><tr><td><code>-&gt;useCurrent()</code></td><td>Set TIMESTAMP columns to use CURRENT_TIMESTAMP as default value.</td></tr><tr><td><code>-&gt;useCurrentOnUpdate()</code></td><td>Set TIMESTAMP columns to use CURRENT_TIMESTAMP when a record is updated (MySQL).</td></tr><tr><td><code>-&gt;virtualAs($expression)</code></td><td>Create a virtual generated column (MySQL / PostgreSQL / SQLite).</td></tr><tr><td><code>-&gt;generatedAs($expression)</code></td><td>Create an identity column with specified sequence options (PostgreSQL).</td></tr><tr><td><code>-&gt;always()</code></td><td>Defines the precedence of sequence values over input for an identity column (PostgreSQL).</td></tr><tr><td><code>-&gt;isGeometry()</code></td><td>Set spatial column type to <code>geometry</code> - the default type is <code>geography</code> (PostgreSQL).</td></tr></tbody></table><p><a name="default-expressions"></a></p><h4 id="default-expressions" tabindex="-1"><a class="header-anchor" href="#default-expressions" aria-hidden="true">#</a> Default Expressions</h4><p>The <code>default</code> modifier accepts a value or an <code>Illuminate\\Database\\Query\\Expression</code> instance. Using an <code>Expression</code> instance will prevent Laravel from wrapping the value in quotes and allow you to use database specific functions. One situation where this is particularly useful is when you need to assign default values to JSON columns:</p><pre><code>&lt;?php

use Illuminate\\Support\\Facades\\Schema;
use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Database\\Query\\Expression;
use Illuminate\\Database\\Migrations\\Migration;

return new class extends Migration
{
    /**
     * Run the migrations.
     */
    public function up(): void
    {
        Schema::create(&#39;flights&#39;, function (Blueprint $table) {
            $table-&gt;id();
            $table-&gt;json(&#39;movies&#39;)-&gt;default(new Expression(&#39;(JSON_ARRAY())&#39;));
            $table-&gt;timestamps();
        });
    }
};
</code></pre><blockquote><p><strong>Warning</strong><br> Support for default expressions depends on your database driver, database version, and the field type. Please refer to your database&#39;s documentation.</p></blockquote><p><a name="column-order"></a></p><h4 id="column-order" tabindex="-1"><a class="header-anchor" href="#column-order" aria-hidden="true">#</a> Column Order</h4><p>When using the MySQL database, the <code>after</code> method may be used to add columns after an existing column in the schema:</p><pre><code>$table-&gt;after(&#39;password&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;address_line1&#39;);
    $table-&gt;string(&#39;address_line2&#39;);
    $table-&gt;string(&#39;city&#39;);
});
</code></pre><p><a name="modifying-columns"></a></p><h3 id="modifying-columns" tabindex="-1"><a class="header-anchor" href="#modifying-columns" aria-hidden="true">#</a> Modifying Columns</h3><p>The <code>change</code> method allows you to modify the type and attributes of existing columns. For example, you may wish to increase the size of a <code>string</code> column. To see the <code>change</code> method in action, let&#39;s increase the size of the <code>name</code> column from 25 to 50. To accomplish this, we simply define the new state of the column and then call the <code>change</code> method:</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;name&#39;, 50)-&gt;change();
});
</code></pre><p>When modifying a column, you must explicitly include all of the modifiers you want to keep on the column definition - any missing attribute will be dropped. For example, to retain the <code>unsigned</code>, <code>default</code>, and <code>comment</code> attributes, you must call each modifier explicitly when changing the column:</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;integer(&#39;votes&#39;)-&gt;unsigned()-&gt;default(1)-&gt;comment(&#39;my comment&#39;)-&gt;change();
});
</code></pre><p><a name="modifying-columns-on-sqlite"></a></p><h4 id="modifying-columns-on-sqlite" tabindex="-1"><a class="header-anchor" href="#modifying-columns-on-sqlite" aria-hidden="true">#</a> Modifying Columns On SQLite</h4><p>If your application is utilizing an SQLite database, you must install the <code>doctrine/dbal</code> package using the Composer package manager before modifying a column. The Doctrine DBAL library is used to determine the current state of the column and to create the SQL queries needed to make the requested changes to your column:</p><pre><code>composer require doctrine/dbal
</code></pre><p>If you plan to modify columns created using the <code>timestamp</code> method, you must also add the following configuration to your application&#39;s <code>config/database.php</code> configuration file:</p><div class="language-php line-numbers-mode" data-ext="php"><pre class="language-php"><code><span class="token keyword">use</span> <span class="token package">Illuminate<span class="token punctuation">\\</span>Database<span class="token punctuation">\\</span>DBAL<span class="token punctuation">\\</span>TimestampType</span><span class="token punctuation">;</span>

<span class="token string single-quoted-string">&#39;dbal&#39;</span> <span class="token operator">=&gt;</span> <span class="token punctuation">[</span>
    <span class="token string single-quoted-string">&#39;types&#39;</span> <span class="token operator">=&gt;</span> <span class="token punctuation">[</span>
        <span class="token string single-quoted-string">&#39;timestamp&#39;</span> <span class="token operator">=&gt;</span> <span class="token class-name static-context">TimestampType</span><span class="token operator">::</span><span class="token keyword">class</span><span class="token punctuation">,</span>
    <span class="token punctuation">]</span><span class="token punctuation">,</span>
<span class="token punctuation">]</span><span class="token punctuation">,</span>
</code></pre><div class="line-numbers" aria-hidden="true"><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div><div class="line-number"></div></div></div><blockquote><p><strong>Warning</strong><br> When using the <code>doctrine/dbal</code> package, the following column types can be modified: <code>bigInteger</code>, <code>binary</code>, <code>boolean</code>, <code>char</code>, <code>date</code>, <code>dateTime</code>, <code>dateTimeTz</code>, <code>decimal</code>, <code>double</code>, <code>integer</code>, <code>json</code>, <code>longText</code>, <code>mediumText</code>, <code>smallInteger</code>, <code>string</code>, <code>text</code>, <code>time</code>, <code>tinyText</code>, <code>unsignedBigInteger</code>, <code>unsignedInteger</code>, <code>unsignedSmallInteger</code>, <code>ulid</code>, and <code>uuid</code>.</p></blockquote><p><a name="renaming-columns"></a></p><h3 id="renaming-columns" tabindex="-1"><a class="header-anchor" href="#renaming-columns" aria-hidden="true">#</a> Renaming Columns</h3><p>To rename a column, you may use the <code>renameColumn</code> method provided by the schema builder:</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;renameColumn(&#39;from&#39;, &#39;to&#39;);
});
</code></pre><p><a name="renaming-columns-on-legacy-databases"></a></p><h4 id="renaming-columns-on-legacy-databases" tabindex="-1"><a class="header-anchor" href="#renaming-columns-on-legacy-databases" aria-hidden="true">#</a> Renaming Columns On Legacy Databases</h4><p>If you are running a database installation older than one of the following releases, you should ensure that you have installed the <code>doctrine/dbal</code> library via the Composer package manager before renaming a column:</p>`,311),u=e("div",{class:"content-list",markdown:"1"},[e("ul",null,[e("li",null,[t("MySQL < "),e("code",null,"8.0.3")]),e("li",null,[t("MariaDB < "),e("code",null,"10.5.2")]),e("li",null,[t("SQLite < "),e("code",null,"3.25.0")])])],-1),p=a(`<p><a name="dropping-columns"></a></p><h3 id="dropping-columns" tabindex="-1"><a class="header-anchor" href="#dropping-columns" aria-hidden="true">#</a> Dropping Columns</h3><p>To drop a column, you may use the <code>dropColumn</code> method on the schema builder:</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;dropColumn(&#39;votes&#39;);
});
</code></pre><p>You may drop multiple columns from a table by passing an array of column names to the <code>dropColumn</code> method:</p><pre><code>Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;dropColumn([&#39;votes&#39;, &#39;avatar&#39;, &#39;location&#39;]);
});
</code></pre><p><a name="dropping-columns-on-legacy-databases"></a></p><h4 id="dropping-columns-on-legacy-databases" tabindex="-1"><a class="header-anchor" href="#dropping-columns-on-legacy-databases" aria-hidden="true">#</a> Dropping Columns On Legacy Databases</h4><p>If you are running a version of SQLite prior to <code>3.35.0</code>, you must install the <code>doctrine/dbal</code> package via the Composer package manager before the <code>dropColumn</code> method may be used. Dropping or modifying multiple columns within a single migration while using this package is not supported.</p><p><a name="available-command-aliases"></a></p><h4 id="available-command-aliases" tabindex="-1"><a class="header-anchor" href="#available-command-aliases" aria-hidden="true">#</a> Available Command Aliases</h4><p>Laravel provides several convenient methods related to dropping common types of columns. Each of these methods is described in the table below:</p><table><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><code>$table-&gt;dropMorphs(&#39;morphable&#39;);</code></td><td>Drop the <code>morphable_id</code> and <code>morphable_type</code> columns.</td></tr><tr><td><code>$table-&gt;dropRememberToken();</code></td><td>Drop the <code>remember_token</code> column.</td></tr><tr><td><code>$table-&gt;dropSoftDeletes();</code></td><td>Drop the <code>deleted_at</code> column.</td></tr><tr><td><code>$table-&gt;dropSoftDeletesTz();</code></td><td>Alias of <code>dropSoftDeletes()</code> method.</td></tr><tr><td><code>$table-&gt;dropTimestamps();</code></td><td>Drop the <code>created_at</code> and <code>updated_at</code> columns.</td></tr><tr><td><code>$table-&gt;dropTimestampsTz();</code></td><td>Alias of <code>dropTimestamps()</code> method.</td></tr></tbody></table><p><a name="indexes"></a></p><h2 id="indexes" tabindex="-1"><a class="header-anchor" href="#indexes" aria-hidden="true">#</a> Indexes</h2><p><a name="creating-indexes"></a></p><h3 id="creating-indexes" tabindex="-1"><a class="header-anchor" href="#creating-indexes" aria-hidden="true">#</a> Creating Indexes</h3><p>The Laravel schema builder supports several types of indexes. The following example creates a new <code>email</code> column and specifies that its values should be unique. To create the index, we can chain the <code>unique</code> method onto the column definition:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;users&#39;, function (Blueprint $table) {
    $table-&gt;string(&#39;email&#39;)-&gt;unique();
});
</code></pre><p>Alternatively, you may create the index after defining the column. To do so, you should call the <code>unique</code> method on the schema builder blueprint. This method accepts the name of the column that should receive a unique index:</p><pre><code>$table-&gt;unique(&#39;email&#39;);
</code></pre><p>You may even pass an array of columns to an index method to create a compound (or composite) index:</p><pre><code>$table-&gt;index([&#39;account_id&#39;, &#39;created_at&#39;]);
</code></pre><p>When creating an index, Laravel will automatically generate an index name based on the table, column names, and the index type, but you may pass a second argument to the method to specify the index name yourself:</p><pre><code>$table-&gt;unique(&#39;email&#39;, &#39;unique_email&#39;);
</code></pre><p><a name="available-index-types"></a></p><h4 id="available-index-types" tabindex="-1"><a class="header-anchor" href="#available-index-types" aria-hidden="true">#</a> Available Index Types</h4><p>Laravel&#39;s schema builder blueprint class provides methods for creating each type of index supported by Laravel. Each index method accepts an optional second argument to specify the name of the index. If omitted, the name will be derived from the names of the table and column(s) used for the index, as well as the index type. Each of the available index methods is described in the table below:</p><table><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><code>$table-&gt;primary(&#39;id&#39;);</code></td><td>Adds a primary key.</td></tr><tr><td><code>$table-&gt;primary([&#39;id&#39;, &#39;parent_id&#39;]);</code></td><td>Adds composite keys.</td></tr><tr><td><code>$table-&gt;unique(&#39;email&#39;);</code></td><td>Adds a unique index.</td></tr><tr><td><code>$table-&gt;index(&#39;state&#39;);</code></td><td>Adds an index.</td></tr><tr><td><code>$table-&gt;fullText(&#39;body&#39;);</code></td><td>Adds a full text index (MySQL/PostgreSQL).</td></tr><tr><td><code>$table-&gt;fullText(&#39;body&#39;)-&gt;language(&#39;english&#39;);</code></td><td>Adds a full text index of the specified language (PostgreSQL).</td></tr><tr><td><code>$table-&gt;spatialIndex(&#39;location&#39;);</code></td><td>Adds a spatial index (except SQLite).</td></tr></tbody></table><p><a name="index-lengths-mysql-mariadb"></a></p><h4 id="index-lengths-mysql-mariadb" tabindex="-1"><a class="header-anchor" href="#index-lengths-mysql-mariadb" aria-hidden="true">#</a> Index Lengths &amp; MySQL / MariaDB</h4><p>By default, Laravel uses the <code>utf8mb4</code> character set. If you are running a version of MySQL older than the 5.7.7 release or MariaDB older than the 10.2.2 release, you may need to manually configure the default string length generated by migrations in order for MySQL to create indexes for them. You may configure the default string length by calling the <code>Schema::defaultStringLength</code> method within the <code>boot</code> method of your <code>App\\Providers\\AppServiceProvider</code> class:</p><pre><code>use Illuminate\\Support\\Facades\\Schema;

/**
 * Bootstrap any application services.
 */
public function boot(): void
{
    Schema::defaultStringLength(191);
}
</code></pre><p>Alternatively, you may enable the <code>innodb_large_prefix</code> option for your database. Refer to your database&#39;s documentation for instructions on how to properly enable this option.</p><p><a name="renaming-indexes"></a></p><h3 id="renaming-indexes" tabindex="-1"><a class="header-anchor" href="#renaming-indexes" aria-hidden="true">#</a> Renaming Indexes</h3><p>To rename an index, you may use the <code>renameIndex</code> method provided by the schema builder blueprint. This method accepts the current index name as its first argument and the desired name as its second argument:</p><pre><code>$table-&gt;renameIndex(&#39;from&#39;, &#39;to&#39;)
</code></pre><blockquote><p><strong>Warning</strong><br> If your application is utilizing an SQLite database, you must install the <code>doctrine/dbal</code> package via the Composer package manager before the <code>renameIndex</code> method may be used.</p></blockquote><p><a name="dropping-indexes"></a></p><h3 id="dropping-indexes" tabindex="-1"><a class="header-anchor" href="#dropping-indexes" aria-hidden="true">#</a> Dropping Indexes</h3><p>To drop an index, you must specify the index&#39;s name. By default, Laravel automatically assigns an index name based on the table name, the name of the indexed column, and the index type. Here are some examples:</p><table><thead><tr><th>Command</th><th>Description</th></tr></thead><tbody><tr><td><code>$table-&gt;dropPrimary(&#39;users_id_primary&#39;);</code></td><td>Drop a primary key from the &quot;users&quot; table.</td></tr><tr><td><code>$table-&gt;dropUnique(&#39;users_email_unique&#39;);</code></td><td>Drop a unique index from the &quot;users&quot; table.</td></tr><tr><td><code>$table-&gt;dropIndex(&#39;geo_state_index&#39;);</code></td><td>Drop a basic index from the &quot;geo&quot; table.</td></tr><tr><td><code>$table-&gt;dropFullText(&#39;posts_body_fulltext&#39;);</code></td><td>Drop a full text index from the &quot;posts&quot; table.</td></tr><tr><td><code>$table-&gt;dropSpatialIndex(&#39;geo_location_spatialindex&#39;);</code></td><td>Drop a spatial index from the &quot;geo&quot; table (except SQLite).</td></tr></tbody></table><p>If you pass an array of columns into a method that drops indexes, the conventional index name will be generated based on the table name, columns, and index type:</p><pre><code>Schema::table(&#39;geo&#39;, function (Blueprint $table) {
    $table-&gt;dropIndex([&#39;state&#39;]); // Drops index &#39;geo_state_index&#39;
});
</code></pre><p><a name="foreign-key-constraints"></a></p><h3 id="foreign-key-constraints" tabindex="-1"><a class="header-anchor" href="#foreign-key-constraints" aria-hidden="true">#</a> Foreign Key Constraints</h3><p>Laravel also provides support for creating foreign key constraints, which are used to force referential integrity at the database level. For example, let&#39;s define a <code>user_id</code> column on the <code>posts</code> table that references the <code>id</code> column on a <code>users</code> table:</p><pre><code>use Illuminate\\Database\\Schema\\Blueprint;
use Illuminate\\Support\\Facades\\Schema;

Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;unsignedBigInteger(&#39;user_id&#39;);

    $table-&gt;foreign(&#39;user_id&#39;)-&gt;references(&#39;id&#39;)-&gt;on(&#39;users&#39;);
});
</code></pre><p>Since this syntax is rather verbose, Laravel provides additional, terser methods that use conventions to provide a better developer experience. When using the <code>foreignId</code> method to create your column, the example above can be rewritten like so:</p><pre><code>Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;foreignId(&#39;user_id&#39;)-&gt;constrained();
});
</code></pre><p>The <code>foreignId</code> method creates an <code>UNSIGNED BIGINT</code> equivalent column, while the <code>constrained</code> method will use conventions to determine the table and column being referenced. If your table name does not match Laravel&#39;s conventions, you may manually provide it to the <code>constrained</code> method. In addition, the name that should be assigned to the generated index may be specified as well:</p><pre><code>Schema::table(&#39;posts&#39;, function (Blueprint $table) {
    $table-&gt;foreignId(&#39;user_id&#39;)-&gt;constrained(
        table: &#39;users&#39;, indexName: &#39;posts_user_id&#39;
    );
});
</code></pre><p>You may also specify the desired action for the &quot;on delete&quot; and &quot;on update&quot; properties of the constraint:</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;)
      -&gt;constrained()
      -&gt;onUpdate(&#39;cascade&#39;)
      -&gt;onDelete(&#39;cascade&#39;);
</code></pre><p>An alternative, expressive syntax is also provided for these actions:</p><table><thead><tr><th>Method</th><th>Description</th></tr></thead><tbody><tr><td><code>$table-&gt;cascadeOnUpdate();</code></td><td>Updates should cascade.</td></tr><tr><td><code>$table-&gt;restrictOnUpdate();</code></td><td>Updates should be restricted.</td></tr><tr><td><code>$table-&gt;cascadeOnDelete();</code></td><td>Deletes should cascade.</td></tr><tr><td><code>$table-&gt;restrictOnDelete();</code></td><td>Deletes should be restricted.</td></tr><tr><td><code>$table-&gt;nullOnDelete();</code></td><td>Deletes should set the foreign key value to null.</td></tr></tbody></table><p>Any additional <a href="#column-modifiers">column modifiers</a> must be called before the <code>constrained</code> method:</p><pre><code>$table-&gt;foreignId(&#39;user_id&#39;)
      -&gt;nullable()
      -&gt;constrained();
</code></pre><p><a name="dropping-foreign-keys"></a></p><h4 id="dropping-foreign-keys" tabindex="-1"><a class="header-anchor" href="#dropping-foreign-keys" aria-hidden="true">#</a> Dropping Foreign Keys</h4><p>To drop a foreign key, you may use the <code>dropForeign</code> method, passing the name of the foreign key constraint to be deleted as an argument. Foreign key constraints use the same naming convention as indexes. In other words, the foreign key constraint name is based on the name of the table and the columns in the constraint, followed by a &quot;_foreign&quot; suffix:</p><pre><code>$table-&gt;dropForeign(&#39;posts_user_id_foreign&#39;);
</code></pre><p>Alternatively, you may pass an array containing the column name that holds the foreign key to the <code>dropForeign</code> method. The array will be converted to a foreign key constraint name using Laravel&#39;s constraint naming conventions:</p><pre><code>$table-&gt;dropForeign([&#39;user_id&#39;]);
</code></pre><p><a name="toggling-foreign-key-constraints"></a></p><h4 id="toggling-foreign-key-constraints" tabindex="-1"><a class="header-anchor" href="#toggling-foreign-key-constraints" aria-hidden="true">#</a> Toggling Foreign Key Constraints</h4><p>You may enable or disable foreign key constraints within your migrations by using the following methods:</p><pre><code>Schema::enableForeignKeyConstraints();

Schema::disableForeignKeyConstraints();

Schema::withoutForeignKeyConstraints(function () {
    // Constraints disabled within this closure...
});
</code></pre>`,69),g=e("strong",null,"Warning",-1),b=e("br",null,null,-1),f=e("a",{href:"./database#configuration"},"enable foreign key support",-1),y={href:"https://www.sqlite.org/omitted.html",target:"_blank",rel:"noopener noreferrer"},v=a('<p><a name="events"></a></p><h2 id="events" tabindex="-1"><a class="header-anchor" href="#events" aria-hidden="true">#</a> Events</h2><p>For convenience, each migration operation will dispatch an <a href="./events">event</a>. All of the following events extend the base <code>Illuminate\\Database\\Events\\MigrationEvent</code> class:</p><table><thead><tr><th>Class</th><th>Description</th></tr></thead><tbody><tr><td><code>Illuminate\\Database\\Events\\MigrationsStarted</code></td><td>A batch of migrations is about to be executed.</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationsEnded</code></td><td>A batch of migrations has finished executing.</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationStarted</code></td><td>A single migration is about to be executed.</td></tr><tr><td><code>Illuminate\\Database\\Events\\MigrationEnded</code></td><td>A single migration has finished executing.</td></tr><tr><td><code>Illuminate\\Database\\Events\\SchemaDumped</code></td><td>A database schema dump has completed.</td></tr><tr><td><code>Illuminate\\Database\\Events\\SchemaLoaded</code></td><td>An existing database schema dump has loaded.</td></tr></tbody></table>',4);function x(T,I){const o=d("ExternalLinkIcon");return i(),c("div",null,[s,h,m,u,p,e("blockquote",null,[e("p",null,[g,b,t(" SQLite disables foreign key constraints by default. When using SQLite, make sure to "),f,t(" in your database configuration before attempting to create them in your migrations. In addition, SQLite only supports foreign keys upon creation of the table and "),e("a",y,[t("not when tables are altered"),r(o)]),t(".")])]),v])}const q=n(l,[["render",x],["__file","migrations.html.vue"]]);export{q as default};
